// AUTO-GENERATED by gen/serialize.mjs — do not edit by hand.
// Visual Basic extended-branch levels. Keyed by CourseNode.id; each array's
// length === that node's lessonCount. Tests are static code-pattern checks.
import { lesson, sec, test, includesAll, matches, matchesAll, type Level } from './kit'
import type { BranchLevelsByNode } from './assemble'

export const VISUAL_BASIC_BRANCH_LEVELS: BranchLevelsByNode = {
  "vb-excel-ranges": [
    lesson({
      title: "读取单个单元格的值",
      badge: "单元格",
      icon: "VB",
      objective: "用 ws.Range(\"A1\").Value 把 A1 的值读进一个变量，并用 MsgBox 显示出来。",
      starterCode: "Sub ReadCell()\n    Dim ws As Worksheet\n    Set ws = ThisWorkbook.Worksheets(\"Sheet1\")\n\n    ' TODO: 用 ws.Range(\"A1\").Value 读出 A1 的值，存进变量 result\n    Dim result As Variant\n\n    ' TODO: 用 MsgBox 把 result 显示出来\nEnd Sub\n",
      hints: ["Range(\"A1\") 拿到的是一个单元格对象，真正的内容在它的 .Value 属性里。", "用变量声明成 Variant，因为单元格里可能是数字、文本或日期，类型不固定。", "MsgBox result 就能弹窗显示，不要漏掉先 Set ws 这一步。"],
      tests: [
      test("vb-excel-ranges-1-range-value", "用 Range(\"A1\").Value 读取单元格", matches(new RegExp("Range\\s*\\(\\s*\"A1\"\\s*\\)\\s*\\.Value", "i")), "需要写 ws.Range(\"A1\").Value 来取出 A1 的内容。"),
      test("vb-excel-ranges-1-msgbox", "用 MsgBox 显示读到的值", matches(new RegExp("MsgBox\\s+result", "i")), "把读到的 result 传给 MsgBox 弹窗显示。"),
      ],
      sections: [
      sec("Range 是 Excel 操作的核心", "在 VBA 里几乎所有对单元格的操作都从 Range 对象开始。Range(\"A1\") 表示「这个工作表上的 A1 单元格」。注意它本身是一个对象，不是值——你要再访问它的属性才能拿到里面的内容。最常用的属性就是 .Value，代表单元格当前显示的数据。", { code: "Dim ws As Worksheet\nSet ws = ThisWorkbook.Worksheets(\"Sheet1\")\nDim result As Variant\nresult = ws.Range(\"A1\").Value", caption: "先定位工作表，再取 A1 的 .Value", tip: "养成习惯：永远先 Set ws，再用 ws.Range，别裸写 Range——那样会落到「当前激活的表」上，很容易读错表。" }),
      sec("为什么用 Variant 接住", "单元格可以装数字、文本、日期、甚至错误值。如果你声明成 Long 去接一个文本单元格，运行时就会报「类型不匹配」。进阶阶段我们追求稳：用 Variant 接住，先拿到再说，后续需要时再判断类型。", { code: "Dim result As Variant\nresult = ws.Range(\"A1\").Value\nMsgBox result", caption: "Variant 能接住任意单元格内容", warning: "别直接 MsgBox ws.Range(\"A1\")——少了 .Value，VBA 会试图把整个对象转成字符串，结果常常不是你要的。" }),
      ],
    }),
    lesson({
      title: "写入单元格与设置区域",
      badge: "写入",
      icon: "VB",
      objective: "用赋值给 .Value 往 B2 写入一个数，再用 Range(\"A1:C1\").Value 给一整行三个单元格同时赋同一个值。",
      starterCode: "Sub WriteCells()\n    Dim ws As Worksheet\n    Set ws = ThisWorkbook.Worksheets(\"Sheet1\")\n\n    ' TODO: 往 B2 写入数字 100\n\n    ' TODO: 用 A1:C1 这个区域，一次性把三个单元格都设成 0\nEnd Sub\n",
      hints: ["写入和读取是同一个属性 .Value，只是放在等号的哪一边不同：读在右边，写在左边。", "Range(\"A1:C1\") 用冒号表示一个连续区域，给它的 .Value 赋一个标量，整片都会变成那个值。", "别用循环挨个写 A1、B1、C1——一次给整个区域赋值更快也更清楚。"],
      tests: [
      test("vb-excel-ranges-2-write-b2", "给 B2 的 .Value 赋值 100", matches(new RegExp("Range\\s*\\(\\s*\"B2\"\\s*\\)\\s*\\.Value\\s*=\\s*100", "i")), "用 ws.Range(\"B2\").Value = 100 写入。"),
      test("vb-excel-ranges-2-fill-row", "给区域 A1:C1 一次性赋值", matches(new RegExp("Range\\s*\\(\\s*\"A1:C1\"\\s*\\)\\s*\\.Value\\s*=", "i")), "用 ws.Range(\"A1:C1\").Value = 0 把整行设成 0。"),
      ],
      sections: [
      sec("写入就是给 .Value 赋值", "读单元格时 .Value 出现在等号右边；写单元格时把它放到左边即可。VBA 会自动把数据写进单元格并触发重算。同一个属性双向使用，是 Range 操作最顺手的地方。", { code: "ws.Range(\"B2\").Value = 100\nws.Range(\"C5\").Value = \"完成\"", caption: "数字和文本都能直接写进去", tip: "写文本时要加引号；写日期可以用 DateSerial(2026, 1, 1) 这种值，避免被当成除法算式。" }),
      sec("一个区域，一次赋值", "Range(\"A1:C1\") 用冒号连起左上角和右下角，代表一片连续单元格。给它的 .Value 赋一个标量值，整片都会被填成同一个值——这比写三遍赋值或开循环都干净。", { code: "ws.Range(\"A1:C1\").Value = 0          ' 三个单元格都变 0\nws.Range(\"A2:A10\").Value = \"待办\"      ' 一整列连续区域都填同一文本", caption: "整个区域共享同一个赋值", warning: "给区域赋单个值是「填充」；如果你想往区域里塞不同的值，得用数组，下一课会讲。" }),
      ],
    }),
    lesson({
      title: "把区域一次性读进数组",
      badge: "数组",
      icon: "VB",
      objective: "用 data = ws.Range(\"A1:B3\").Value 把区域一次性读进二维数组，再用 data(2, 1) 取出其中一格。",
      starterCode: "Sub ReadRangeToArray()\n    Dim ws As Worksheet\n    Set ws = ThisWorkbook.Worksheets(\"Sheet1\")\n\n    Dim data As Variant\n    ' TODO: 用 ws.Range(\"A1:B3\").Value 一次性读进 data（不要逐格循环）\n\n    ' TODO: 取出第 2 行第 1 列的值，赋给 cellValue\n    Dim cellValue As Variant\nEnd Sub\n",
      hints: ["把一个 Variant 直接 = Range(\"A1:B3\").Value，VBA 会自动塞成一个二维数组，省掉一格一格读的循环。", "这个数组的下标从 1 开始，不是 0：data(1, 1) 对应区域左上角的 A1。", "第一个下标是行、第二个是列，所以 data(2, 1) 是第 2 行第 1 列，也就是 A2。"],
      tests: [
      test("vb-excel-ranges-3-bulk-read", "把区域整体读进 Variant 数组", matches(new RegExp("data\\s*=\\s*ws\\.Range\\s*\\(\\s*\"A1:B3\"\\s*\\)\\s*\\.Value", "i")), "写 data = ws.Range(\"A1:B3\").Value，一次性读入。"),
      test("vb-excel-ranges-3-index", "用 (行, 列) 下标取出一格", matches(new RegExp("cellValue\\s*=\\s*data\\s*\\(\\s*2\\s*,\\s*1\\s*\\)", "i")), "用 data(2, 1) 取第 2 行第 1 列，并赋给 cellValue。"),
      ],
      sections: [
      sec("一次读一片，比逐格快几十倍", "每次访问 Range(...).Value 都要跨越 VBA 和 Excel 之间的边界，逐格循环会反复跨界，慢得吓人。正确做法是把整片区域一次读进一个 Variant 变量——VBA 会自动把它变成一个二维数组，之后所有计算都在内存里跑，飞快。", { code: "Dim data As Variant\ndata = ws.Range(\"A1:B3\").Value   ' 一次跨界，6 个值全进来", caption: "整块区域 → 一个二维数组", tip: "处理上千行数据时，这个技巧能把几秒甚至几十秒压到一瞬间——进阶分支里几乎是必备习惯。" }),
      sec("下标从 1 开始，先行后列", "由 Range 读出来的数组有个坑：下标从 1 开始而不是 0。data(1, 1) 是区域的左上角（这里就是 A1），data(2, 1) 是它正下方的 A2。记住顺序是 (行, 列)，和你平时读表的方向一致。", { code: "Dim cellValue As Variant\ncellValue = data(2, 1)   ' 第 2 行第 1 列 = A2\n' data(1, 2) 则是第 1 行第 2 列 = B1", caption: "(行, 列) 定位数组里的某一格", warning: "别套用普通数组的 0 起点习惯。还有：单格区域 Range(\"A1\") 读出来不是数组，是标量，对它用 (1,1) 会报错。" }),
      ],
    }),
    lesson({
      title: "遍历表格区域并汇总",
      badge: "汇总",
      icon: "VB",
      objective: "用 UsedRange 拿到表格的全部数据，读进数组后用 For 循环遍历每一行，把第 2 列累加进 total。",
      starterCode: "Sub TotalTable()\n    Dim ws As Worksheet\n    Set ws = ThisWorkbook.Worksheets(\"Sheet1\")\n\n    ' 表格在 A1 起，第 1 列是名称，第 2 列是金额，第 1 行是表头\n    Dim data As Variant\n    ' TODO: 用 ws.UsedRange.Value 把整张表读进 data\n\n    Dim total As Double\n    total = 0\n\n    ' TODO: 用 For 从第 2 行遍历到 UBound(data, 1)，跳过表头\n    ' TODO: 把每行第 2 列 data(i, 2) 累加进 total\n\n    MsgBox \"合计: \" & total\nEnd Sub\n",
      hints: ["UsedRange 是这张表上「用过的」全部单元格，对它取 .Value 同样得到一个二维数组。", "UBound(data, 1) 返回数组第一维（行）的最大下标，正好是数据的总行数。", "从 i = 2 开始循环就能跳过第 1 行表头；累加用 total = total + data(i, 2)。"],
      tests: [
      test("vb-excel-ranges-4-usedrange", "用 UsedRange.Value 读取整张表", matches(new RegExp("data\\s*=\\s*ws\\.UsedRange\\.Value", "i")), "写 data = ws.UsedRange.Value 拿到全部数据。", 25),
      test("vb-excel-ranges-4-loop", "用 For + UBound 遍历所有行", matchesAll([new RegExp("For\\s+\\w+\\s*=\\s*2\\s+To\\s+UBound\\s*\\(\\s*data\\s*,\\s*1\\s*\\)"), new RegExp("Next")]), "用 For i = 2 To UBound(data, 1) 遍历，从第 2 行跳过表头。", 25),
      test("vb-excel-ranges-4-accumulate", "把每行第 2 列累加进 total", matches(new RegExp("total\\s*=\\s*total\\s*\\+\\s*data\\s*\\(\\s*\\w+\\s*,\\s*2\\s*\\)", "i")), "用 total = total + data(i, 2) 把金额累加起来。", 25),
      test("vb-excel-ranges-4-show", "用 MsgBox 显示合计结果", matches(new RegExp("MsgBox[^\\n]*total", "i")), "用 MsgBox 把 total 拼成结果显示出来。", 25),
      ],
      sections: [
      sec("UsedRange：不用手写区域范围", "前面几课的区域都是写死的（\"A1:B3\"）。但真实表格行数经常变。ws.UsedRange 会自动框住这张表上所有用过的单元格——表头加全部数据行都在里面。对它取 .Value，又是熟悉的二维数组。", { code: "Dim data As Variant\ndata = ws.UsedRange.Value\n' data 的行数 = 表头 + 所有数据行，列数 = 表格宽度", caption: "UsedRange 自动适配表格大小", tip: "若表格不是从 A1 开始，UsedRange 仍会从真正的左上角起算——这时 data(1,1) 不一定是 A1，注意核对。" }),
      sec("UBound 定上界，循环遍历每一行", "拿到数组后，UBound(data, 1) 告诉你它有多少行（第一维上界），UBound(data, 2) 告诉你多少列。要汇总，就从第 2 行开始循环到 UBound(data, 1)，跳过第 1 行表头，把每行的金额列 data(i, 2) 累加进 total。", { code: "Dim i As Long\nFor i = 2 To UBound(data, 1)\n    total = total + data(i, 2)\nNext i\nMsgBox \"合计: \" & total", caption: "跳过表头，逐行累加第 2 列", warning: "如果某行金额列是空或文本，total = total + data(i,2) 会类型不匹配；真实场景里该用 IsNumeric(data(i,2)) 先判断再加。" }),
      sec("把四课串起来", "你已经走完一条完整链路：读单格 → 写单格与区域 → 整片读进数组 → 用 UsedRange 自动取表并循环汇总。这正是 Office 自动化里最常见的套路——「批量读进内存、在内存里算、必要时再写回去」。掌握它，处理几千行报表也只是一瞬间。", { tip: "下一步可以把 total 写回某个单元格（ws.Range(\"D1\").Value = total），就完成了「读—算—写」的闭环。" }),
      ],
    }),
  ],
  "vb-report-export": [
    lesson({
      title: "用 Dictionary 按类别汇总",
      badge: "汇总",
      icon: "VB",
      objective: "把明细数组读进来，用 Scripting.Dictionary 以「类别」为键，累加每个类别的金额。",
      starterCode: "Sub SummarizeByCategory()\n    Dim ws As Worksheet\n    Set ws = ThisWorkbook.Worksheets(\"明细\")\n\n    ' 明细表：第 1 列=类别，第 2 列=金额，第 1 行是表头\n    Dim data As Variant\n    data = ws.UsedRange.Value\n\n    ' TODO: 用 CreateObject(\"Scripting.Dictionary\") 建一个汇总用的字典 totals\n    Dim totals As Object\n\n    Dim i As Long\n    For i = 2 To UBound(data, 1)\n        Dim key As String\n        key = CStr(data(i, 1))\n        ' TODO: 如果 totals 还没有这个 key，先用 totals.Add key, 0 建一个\n        ' TODO: 把本行金额 data(i, 2) 累加到 totals(key) 上\n    Next i\n\n    MsgBox \"共 \" & totals.Count & \" 个类别\"\nEnd Sub\n",
      hints: ["Scripting.Dictionary 用 CreateObject(\"Scripting.Dictionary\") 创建，键唯一、查得快，正是分组汇总的利器。", "用 totals.Exists(key) 判断键在不在；不在就 totals.Add key, 0 先建一个零值桶。", "累加写成 totals(key) = totals(key) + data(i, 2)——字典像数组一样能按键读写。"],
      tests: [
      test("vb-report-export-1-create-dict", "创建 Scripting.Dictionary", matches(new RegExp("CreateObject\\s*\\(\\s*\"Scripting\\.Dictionary\"\\s*\\)", "i")), "用 Set totals = CreateObject(\"Scripting.Dictionary\") 建字典。", 34),
      test("vb-report-export-1-exists-add", "用 Exists 判断后再 Add 新键", matchesAll([new RegExp("totals\\.Exists\\s*\\(\\s*key\\s*\\)"), new RegExp("totals\\.Add\\s+key\\s*,\\s*0")]), "先 If Not totals.Exists(key) Then totals.Add key, 0。", 33),
      test("vb-report-export-1-accumulate", "按键累加金额", matches(new RegExp("totals\\s*\\(\\s*key\\s*\\)\\s*=\\s*totals\\s*\\(\\s*key\\s*\\)\\s*\\+\\s*data\\s*\\(\\s*\\w+\\s*,\\s*2\\s*\\)", "i")), "用 totals(key) = totals(key) + data(i, 2) 累加。", 33),
      ],
      sections: [
      sec("报表的第一步：把明细压成汇总", "一张报表几乎不会原样列出几千条流水，而是按某个维度(类别、地区、月份)分组,把同组的数加起来。VBA 里做分组最顺手的工具是 Scripting.Dictionary:它的键天生唯一,按键读写是常数时间,比「在数组里反复 Find 看有没有出现过」快得多。本课只做一件事——把明细按类别汇总进一个字典。", { code: "Dim totals As Object\nSet totals = CreateObject(\"Scripting.Dictionary\")\n' totals(\"办公\") = 1200，totals(\"差旅\") = 800 …", caption: "字典:类别 → 累计金额", tip: "用 CreateObject 的「后期绑定」写法,不用在「工具→引用」里勾 Microsoft Scripting Runtime,换台电脑也能直接跑。" }),
      sec("Exists 判断,Add 建桶,再累加", "遍历每一行时,先看这个类别在不在字典里:不在就用 Add 建一个初值为 0 的桶,在就直接往上加。这套「不存在则初始化,存在则累加」的模式,是所有分组汇总的骨架。注意 key 要先 CStr 转成字符串,避免类别是纯数字时被当成不同类型对待。", { code: "If Not totals.Exists(key) Then totals.Add key, 0\ntotals(key) = totals(key) + data(i, 2)", caption: "分组累加的标准两行", warning: "别跳过 Exists 直接写 totals(key)=totals(key)+x:对不存在的键读取会先自动建一个空值,空值 + 数字在某些数据下会报类型不匹配。先 Add 0 最稳。" }),
      ],
    }),
    lesson({
      title: "把汇总写进报表工作表",
      badge: "写表",
      icon: "VB",
      objective: "新建(或清空)一张「报表」工作表,写好表头,再用 Keys/Items 把字典里的每个类别和金额逐行写出去。",
      starterCode: "Sub WriteReportSheet(totals As Object)\n    ' totals 是上一课算好的字典:键=类别,值=金额\n    Dim rpt As Worksheet\n    Set rpt = ThisWorkbook.Worksheets(\"报表\")\n    rpt.Cells.Clear\n\n    ' TODO: 在 A1、B1 写表头\"类别\"和\"金额\"\n\n    Dim ks As Variant\n    ks = totals.Keys\n    Dim r As Long\n    r = 2\n\n    Dim i As Long\n    For i = 0 To totals.Count - 1\n        ' TODO: 把 ks(i) 写到 A 列第 r 行(用 rpt.Cells(r, 1))\n        ' TODO: 把 totals(ks(i)) 写到 B 列第 r 行\n        r = r + 1\n    Next i\nEnd Sub\n",
      hints: ["Cells(r, c) 用行号、列号定位单元格,写循环时比 Range(\"A2\") 这种字面地址灵活得多。", "totals.Keys 一次性返回所有键的数组,下标从 0 开始;totals(ks(i)) 取对应的值。", "用一个行游标 r,每写一行就 r = r + 1,这样不管多少类别都能顺序排下去。"],
      tests: [
      test("vb-report-export-2-header", "在 A1、B1 写表头", matchesAll([new RegExp("Cells\\s*\\(\\s*1\\s*,\\s*1\\s*\\)\\s*\\.Value\\s*="), new RegExp("Cells\\s*\\(\\s*1\\s*,\\s*2\\s*\\)\\s*\\.Value\\s*=")]), "用 rpt.Cells(1, 1).Value = \"类别\" 和 rpt.Cells(1, 2).Value = \"金额\"。"),
      test("vb-report-export-2-write-rows", "用 Cells(r, c) 逐行写类别与金额", matchesAll([new RegExp("Cells\\s*\\(\\s*r\\s*,\\s*1\\s*\\)\\s*\\.Value\\s*=\\s*ks\\s*\\(\\s*i\\s*\\)"), new RegExp("Cells\\s*\\(\\s*r\\s*,\\s*2\\s*\\)\\s*\\.Value\\s*=\\s*totals\\s*\\(\\s*ks\\s*\\(\\s*i\\s*\\)\\s*\\)")]), "用 rpt.Cells(r, 1).Value = ks(i) 和 rpt.Cells(r, 2).Value = totals(ks(i))。"),
      ],
      sections: [
      sec("先清场,再写,报表才干净", "报表工作表通常是「每次重新生成」的:如果上次留下了旧数据,这次只覆盖一部分,就会出现「下半截还是上次的数」这种诡异 bug。所以生成前先 rpt.Cells.Clear 把整张表清空,从一张白纸开始写。这一步看着多余,却是报表可信的前提。", { code: "Set rpt = ThisWorkbook.Worksheets(\"报表\")\nrpt.Cells.Clear\nrpt.Cells(1, 1).Value = \"类别\"\nrpt.Cells(1, 2).Value = \"金额\"", caption: "清空后先铺表头", tip: "Cells.Clear 连格式一起清;只想清内容、保留边框配色,用 Cells.ClearContents。" }),
      sec("Keys/Items 把字典摊成行", "字典本身没有「行」的概念,要写成报表就得把它摊平。totals.Keys 返回全部键的数组(从 0 起),配一个从 2 开始的行游标 r,每个键写一行:类别进 A 列、对应金额 totals(ks(i)) 进 B 列,然后 r 加一。这样不管有 3 个还是 300 个类别,都能自动顺序排满。", { code: "Dim ks As Variant: ks = totals.Keys\nDim r As Long: r = 2\nFor i = 0 To totals.Count - 1\n    rpt.Cells(r, 1).Value = ks(i)\n    rpt.Cells(r, 2).Value = totals(ks(i))\n    r = r + 1\nNext i", caption: "键数组 + 行游标 = 逐行落表", warning: "Keys 的下标从 0 开始,Cells 的行号从 1 开始,两套起点别搞混:循环用 i(0 起)取键,落表用 r(2 起)定行。" }),
      ],
    }),
    lesson({
      title: "加合计行并格式化",
      badge: "格式",
      icon: "VB",
      objective: "在数据末尾加一行「合计」,用 WorksheetFunction.Sum 求和,再给表头加粗、金额列套千分位货币格式、列宽自适应。",
      starterCode: "Sub FormatReport(rpt As Worksheet, lastRow As Long)\n    ' lastRow 是最后一行数据所在行;合计行写在它的下一行\n    Dim sumRow As Long\n    sumRow = lastRow + 1\n\n    rpt.Cells(sumRow, 1).Value = \"合计\"\n    ' TODO: 用 WorksheetFunction.Sum 对 B2:B<lastRow> 求和,写到合计行的 B 列\n\n    ' TODO: 让表头第 1 行加粗:rpt.Rows(1).Font.Bold = True\n\n    ' TODO: 给金额列 B 列套货币格式:NumberFormat = \"#,##0.00\"\n\n    ' TODO: 让所有列宽自适应内容:Columns.AutoFit\nEnd Sub\n",
      hints: ["求和不用自己写循环:Application.WorksheetFunction.Sum(rpt.Range(\"B2:B\" & lastRow)) 直接返回合计。", "NumberFormat 是「显示格式」,不改底层数值:\"#,##0.00\" 表示千分位加两位小数。", "rpt.Columns.AutoFit 一次性让所有列宽贴合内容,免得长文字被截断。"],
      tests: [
      test("vb-report-export-3-sum", "用 WorksheetFunction.Sum 求合计", matches(new RegExp("WorksheetFunction\\.Sum\\s*\\(\\s*rpt\\.Range\\s*\\(\\s*\"B2:B\"\\s*&\\s*lastRow\\s*\\)", "i")), "用 WorksheetFunction.Sum(rpt.Range(\"B2:B\" & lastRow)) 求和并写入合计行。", 34),
      test("vb-report-export-3-bold", "表头行加粗", matches(new RegExp("Rows\\s*\\(\\s*1\\s*\\)\\.Font\\.Bold\\s*=\\s*True", "i")), "用 rpt.Rows(1).Font.Bold = True 加粗第 1 行。", 33),
      test("vb-report-export-3-numfmt-autofit", "金额列套货币格式并自适应列宽", matchesAll([new RegExp("NumberFormat\\s*=\\s*\"#,##0\\.00\""), new RegExp("Columns\\.AutoFit")]), "设 NumberFormat = \"#,##0.00\",再调用 Columns.AutoFit。", 33),
      ],
      sections: [
      sec("合计行:让人一眼看到总数", "汇总表的读者第一眼往往就找「一共多少」。在最后一行数据之下加一行「合计」,用 WorksheetFunction.Sum 对金额列求和——直接调 Excel 内置函数,比自己再循环一遍快也更不易出错。注意求和区间是 B2:B<lastRow>,用字符串拼出来:\"B2:B\" & lastRow。", { code: "Dim sumRow As Long: sumRow = lastRow + 1\nrpt.Cells(sumRow, 1).Value = \"合计\"\nrpt.Cells(sumRow, 2).Value = _\n    Application.WorksheetFunction.Sum(rpt.Range(\"B2:B\" & lastRow))", caption: "调内置 Sum 写出合计行", tip: "想让公式留在单元格里(随数据自动重算),可改写 .Formula = \"=SUM(B2:B\" & lastRow & \")\";想要固定的快照值就用 WorksheetFunction.Sum。" }),
      sec("格式不是装饰,是可读性", "一张能交付的报表,数字要看得清:表头加粗区分标题与数据;金额列套 \"#,##0.00\" 让 1234567 显示成 1,234,567.00,不用眯着眼数零;最后 Columns.AutoFit 让每列宽度贴合内容,避免 ### 或文字被截。这三步成本极低,却直接决定别人愿不愿意看你的表。", { code: "rpt.Rows(1).Font.Bold = True\nrpt.Range(\"B2:B\" & sumRow).NumberFormat = \"#,##0.00\"\nrpt.Columns.AutoFit", caption: "加粗 + 货币格式 + 自适应列宽", warning: "NumberFormat 只改显示,不改真实数值——别指望它能四舍五入参与计算;真要改值得用 Round。" }),
      ],
    }),
    lesson({
      title: "把报表导出成 PDF 文件",
      badge: "导出",
      icon: "VB",
      objective: "用 ExportAsFixedFormat 把报表工作表导出为 PDF,文件名带上当天日期,存到工作簿同一个目录。",
      starterCode: "Sub ExportReportPdf(rpt As Worksheet)\n    Dim folder As String\n    folder = ThisWorkbook.Path\n\n    ' TODO: 用 Format(Date, \"yyyymmdd\") 拼出文件名,例如 销售报表_20260616.pdf\n    Dim fileName As String\n    Dim fullPath As String\n    ' fullPath 应为 folder & \"\\\" & fileName\n\n    ' TODO: 调 rpt.ExportAsFixedFormat,Type 用 xlTypePDF,Filename 传 fullPath\n\n    MsgBox \"已导出: \" & fullPath\nEnd Sub\n",
      hints: ["Format(Date, \"yyyymmdd\") 把今天变成 20260616 这样的串,拼进文件名就能区分每天的报表。", "ThisWorkbook.Path 是当前工作簿所在目录;路径分隔符在 VBA 里写成 \"\\\\\"(转义后是一个反斜杠)。", "导出 PDF 用 rpt.ExportAsFixedFormat Type:=xlTypePDF, Filename:=fullPath,这是 Excel 内置的导出接口。"],
      tests: [
      test("vb-report-export-4-date-name", "用 Format(Date, \"yyyymmdd\") 拼日期文件名", matches(new RegExp("Format\\s*\\(\\s*Date\\s*,\\s*\"yyyymmdd\"\\s*\\)", "i")), "用 Format(Date, \"yyyymmdd\") 把当天日期拼进 fileName。", 34),
      test("vb-report-export-4-fullpath", "用工作簿路径拼出完整文件路径", matches(new RegExp("fullPath\\s*=\\s*folder\\s*&\\s*\"\\\\\"\\s*&\\s*fileName", "i")), "用 fullPath = folder & \"\\\\\" & fileName 组合目录与文件名。", 33),
      test("vb-report-export-4-export", "调 ExportAsFixedFormat 导出 PDF", matchesAll([new RegExp("ExportAsFixedFormat"), new RegExp("Type\\s*:=\\s*xlTypePDF"), new RegExp("Filename\\s*:=\\s*fullPath")]), "用 rpt.ExportAsFixedFormat Type:=xlTypePDF, Filename:=fullPath 导出。", 33),
      ],
      sections: [
      sec("PDF 是给人看的最终形态", "报表算完、排好版,最后一步通常是导出一个谁都能打开、谁都改不动的文件——PDF 正合适。Excel 内置的 ExportAsFixedFormat 就是干这个的:对一张工作表(或一个区域)调用它,Type 指定 xlTypePDF,就把当前可见内容渲染成 PDF。不需要装任何额外插件。", { code: "rpt.ExportAsFixedFormat _\n    Type:=xlTypePDF, _\n    Filename:=fullPath, _\n    Quality:=xlQualityStandard", caption: "一张工作表 → 一个 PDF", tip: "想顺便打开看看,加一个参数 OpenAfterPublish:=True;批量生成几十份时记得设成 False,免得弹出几十个阅读器窗口。" }),
      sec("文件名带日期,路径跟着工作簿走", "每天都生成一份的报表,文件名一定要带日期,否则后一份会覆盖前一份。用 Format(Date, \"yyyymmdd\") 拿到 20260616 这样排序友好的串拼进文件名。存到哪?最稳的是 ThisWorkbook.Path——和源数据放一起,换台电脑、换个盘符都不会失效。把目录、分隔符、文件名拼成 fullPath 再传给导出接口。", { code: "Dim fileName As String\nfileName = \"销售报表_\" & Format(Date, \"yyyymmdd\") & \".pdf\"\nDim fullPath As String\nfullPath = folder & \"\\\" & fileName", caption: "目录 + 反斜杠 + 带日期文件名", warning: "如果工作簿还没保存过,ThisWorkbook.Path 会是空串,导出就会失败。生产代码里应先判断 Len(folder) > 0,空了就退回到桌面或弹窗提示先保存。" }),
      ],
    }),
    lesson({
      title: "一键生成完整报表",
      badge: "整合",
      icon: "VB",
      objective: "把前四步串成一个入口 Sub:读明细→字典汇总→写报表→加合计与格式→导出 PDF,关掉屏幕刷新提速并在最后恢复。",
      starterCode: "Sub GenerateSalesReport()\n    ' TODO: 在这里先关掉屏幕刷新来提速(用 Application 的 ScreenUpdating 属性,设为 False)\n\n    Dim src As Worksheet\n    Set src = ThisWorkbook.Worksheets(\"明细\")\n    Dim data As Variant\n    data = src.UsedRange.Value\n\n    ' --- 汇总 ---\n    Dim totals As Object\n    Set totals = CreateObject(\"Scripting.Dictionary\")\n    Dim i As Long\n    For i = 2 To UBound(data, 1)\n        Dim key As String\n        key = CStr(data(i, 1))\n        If Not totals.Exists(key) Then totals.Add key, 0\n        totals(key) = totals(key) + data(i, 2)\n    Next i\n\n    ' --- 写报表 ---\n    Dim rpt As Worksheet\n    Set rpt = ThisWorkbook.Worksheets(\"报表\")\n    rpt.Cells.Clear\n    rpt.Cells(1, 1).Value = \"类别\"\n    rpt.Cells(1, 2).Value = \"金额\"\n    Dim ks As Variant: ks = totals.Keys\n    Dim r As Long: r = 2\n    For i = 0 To totals.Count - 1\n        rpt.Cells(r, 1).Value = ks(i)\n        rpt.Cells(r, 2).Value = totals(ks(i))\n        r = r + 1\n    Next i\n    Dim lastRow As Long\n    lastRow = r - 1\n\n    ' TODO: 调用上一节的格式化子过程,把报表表和最后数据行号传进去\n    ' TODO: 调用导出子过程,把报表表传进去生成 PDF\n\n    ' TODO: 在最后把屏幕刷新恢复回来(同一个属性,设回 True)\nEnd Sub\n",
      hints: ["Application.ScreenUpdating = False 关掉每步重绘,几千行报表能快几倍;结尾务必改回 True。", "前四课的逻辑已经写好成 FormatReport、ExportReportPdf,这里只要按顺序调用它们。", "lastRow = r - 1:循环结束后 r 已经多加了一次,减 1 才是真正最后一行数据的行号。"],
      tests: [
      test("vb-report-export-5-screen-off", "开头关闭屏幕刷新", matches(new RegExp("Application\\.ScreenUpdating\\s*=\\s*False", "i")), "在开头写 Application.ScreenUpdating = False。", 25),
      test("vb-report-export-5-call-format", "调用格式化子过程", matches(new RegExp("FormatReport\\s+rpt\\s*,\\s*lastRow", "i")), "用 FormatReport rpt, lastRow 调用上一节的格式化过程。", 25),
      test("vb-report-export-5-call-export", "调用导出 PDF 子过程", matches(new RegExp("ExportReportPdf\\s+rpt", "i")), "用 ExportReportPdf rpt 调用导出过程。", 25),
      test("vb-report-export-5-screen-on", "结尾恢复屏幕刷新", matches(new RegExp("Application\\.ScreenUpdating\\s*=\\s*True", "i")), "结尾写 Application.ScreenUpdating = True 恢复。", 25),
      ],
      sections: [
      sec("一个入口,串起整条流水线", "前四课各做一件事:汇总、写表、格式化、导出。真正交付时,它们应该是「点一下就全做完」的一条流水线。GenerateSalesReport 就是这个入口:读明细 → 字典汇总 → 逐行写报表 → 调 FormatReport 加合计和格式 → 调 ExportReportPdf 出 PDF。把每步拆成独立子过程、入口只负责按序调用,既好读又好单独调试——这正是 Office 自动化里「读—算—写—导出」的标准闭环。", { code: "lastRow = r - 1            ' 循环里 r 多加了一次,减 1 才对\nFormatReport rpt, lastRow\nExportReportPdf rpt", caption: "入口只管编排,细节交给子过程", tip: "把 \"明细\"\"报表\" 这种表名、\"销售报表_\" 这种前缀抽成常量放在模块顶部,换数据源或改命名时只改一处。" }),
      sec("ScreenUpdating:慢与快的分水岭", "默认情况下,你每写一个单元格 Excel 都重绘一次屏幕——几千次写入就是几千次重绘,慢得肉眼可见。开头 Application.ScreenUpdating = False 把重绘停掉,所有写入在后台完成,结束再 = True 一次性刷新,通常能快好几倍。关键是:无论中途是否出错,结尾都必须把它改回 True,否则用户的 Excel 会一直「卡住不动」。", { code: "Application.ScreenUpdating = False\n' … 全部读、算、写、导出 …\nApplication.ScreenUpdating = True", caption: "关掉重绘,跑完再恢复", warning: "生产代码里应配 On Error GoTo Cleanup,在 Cleanup 标签下统一把 ScreenUpdating 改回 True——这样即使中途报错,屏幕也不会被永久关掉。" }),
      ],
    }),
  ],
  "vb-macro-buttons": [
    lesson({
      title: "宏的入口：Public Sub",
      badge: "入口",
      icon: "VB",
      objective: "写一个无参数的 Public Sub 宏 RunReport，让它能出现在 Excel 的宏对话框里被运行。",
      starterCode: "' 把它放进标准模块（Module1），不要放在 ThisWorkbook 或工作表代码区\nOption Explicit\n\n' TODO: 写一个 Public Sub RunReport（不带参数）\n' TODO: 在里面用 MsgBox 提示\"报表已生成\"\n\n",
      hints: ["只有“无参数”的 Public Sub 才会出现在 Excel 的宏列表（Alt+F8）里。带 ByVal 参数的过程不会出现。", "宏要写在标准模块里，写在 ThisWorkbook 或工作表对象里照样能跑但不会进宏列表。", "结构是 Public Sub 名字() ... End Sub，名字别用空格或数字开头。"],
      tests: [
      test("vb-macro-buttons-1-publicsub", "声明 Public Sub RunReport 且不带参数", matches(new RegExp("public\\s+sub\\s+runreport\\s*\\(\\s*\\)", "i")), "用 Public Sub RunReport() 声明，括号里留空。"),
      test("vb-macro-buttons-1-endsub", "用 End Sub 闭合过程", matches(new RegExp("end\\s+sub", "i")), "每个 Sub 都要有对应的 End Sub。"),
      ],
      sections: [
      sec("宏就是一个能被点的过程", "在 Office 里，所谓“宏”不是什么特殊语法，它就是一个写在标准模块里、Public、且不带参数的 Sub。Excel 扫描所有标准模块，把符合这三个条件的 Sub 收进宏对话框（Alt+F8），用户点“运行”就执行它。这是把代码挂到用户面前的第一步：先有一个合格的入口。", { code: "Public Sub RunReport()\n    MsgBox \"报表已生成\"\nEnd Sub", caption: "最简单的可运行宏", tip: "Sub 默认就是 Public，但显式写 Public 能让意图清楚，也避免别人误改成 Private 后宏从列表里消失。" }),
      sec("为什么参数会让它从列表里消失", "Excel 的宏对话框只列“能直接调用、不需要外部传值”的过程。一旦你写成 Public Sub RunReport(ByVal path As String)，Excel 不知道该传什么给 path，于是干脆不显示它。这是新手最常踩的坑：明明写了 Sub，宏列表里却找不到。需要参数的逻辑，应该拆成内部过程，由这个无参入口去调用。", { warning: "别给宏入口加参数。要传值就在 Sub 内部用变量或读单元格，别指望宏对话框替你填。" }),
      sec("放对位置", "插入 → 模块，新建的 Module1 才是标准模块。写在 ThisWorkbook、Sheet1 这类对象模块里的 Public Sub 同样能运行，但 Excel 不会把它收进宏列表——因为对象模块往往装的是事件处理程序，不是给用户点的入口。养成习惯：给用户用的宏，放标准模块。"),
      ],
    }),
    lesson({
      title: "按钮的事件入口：Click",
      badge: "事件",
      icon: "VB",
      objective: "为表单控件按钮写出 Private Sub CommandButton1_Click() 事件过程，让点击按钮时触发逻辑。",
      starterCode: "' 这段写在工作表对象模块里（比如 Sheet1 的代码区），不是标准模块\nOption Explicit\n\n' 假设表里已经放了一个名为 CommandButton1 的 ActiveX 按钮\n' TODO: 为这个按钮写出它的“点击”事件过程（私有、无参，名字是 控件名_事件名）\n' TODO: 在事件里调用上一课那个公开宏（也可以先弹个提示给自己看反馈）\n\n",
      hints: ["ActiveX 按钮的事件命名是固定的：控件名_事件名，所以是 CommandButton1_Click。名字写错就永远不触发。", "事件过程必须是 Private Sub，且写在该控件所在工作表的对象模块里，不能放标准模块。", "事件里别堆业务逻辑，调用一个 Public Sub 即可——按钮负责“触发”，逻辑放别处。"],
      tests: [
      test("vb-macro-buttons-2-clickhandler", "声明 CommandButton1_Click 事件过程", matches(new RegExp("sub\\s+commandbutton1_click\\s*\\(\\s*\\)", "i")), "事件名必须是 控件名_Click，这里是 CommandButton1_Click。", 40),
      test("vb-macro-buttons-2-private", "事件过程用 Private 声明", matches(new RegExp("private\\s+sub\\s+commandbutton1_click", "i")), "控件事件过程要写成 Private Sub。", 30),
      test("vb-macro-buttons-2-delegates", "事件里调用了 RunReport 或弹出提示", matches(new RegExp("runreport|msgbox", "i")), "在 Click 事件里调用 RunReport，或用 MsgBox 给用户反馈。", 30),
      ],
      sections: [
      sec("按钮怎么知道你点了它", "ActiveX 按钮（来自“开发工具 → 插入 → ActiveX 控件”)有一套事件。当用户点击，Excel 会去找一个名字精确等于 控件名_Click 的 Private Sub。找到就执行，找不到就什么都不发生。所以 CommandButton1 的点击入口固定叫 CommandButton1_Click，这是约定，不是你随便起的名字。", { code: "Private Sub CommandButton1_Click()\n    RunReport\nEnd Sub", caption: "把按钮点击接到上一课的 RunReport", tip: "在 VBE 里双击工作表上的按钮，编辑器会自动帮你生成正确名字的 _Click 空壳，省得手打错。" }),
      sec("事件过程必须 Private、必须在对象模块", "两个硬性要求：一，事件过程是 Private——它是控件的私有响应，不该出现在宏列表里被人手动跑;二,它必须写在控件所属那张表的对象模块里(双击 Sheet1 看到的代码区)。放进标准模块,Excel 不会把它和按钮关联,点了也没反应。这正是“点了按钮没动静”的头号原因。", { warning: "别把 _Click 事件搬进标准模块,也别把它改成 Public——两种都会让按钮失联。" }),
      sec("按钮只负责触发，别塞逻辑", "好的结构是：按钮事件里只写一行调用,真正的活儿放在标准模块的 Public Sub 里。好处有三:同一段逻辑能被多个按钮、菜单、其他宏复用;事件过程保持干净易读;以后想把入口从按钮换成功能区按钮,逻辑一行都不用改。这就是“入口”与“动作”分离。"),
      ],
    }),
    lesson({
      title: "点了别炸：On Error 兜底",
      badge: "错误码",
      icon: "VB",
      objective: "在宏里用 On Error GoTo 跳到错误处理标签，用 Err.Number/Err.Description 给用户友好提示，而不是弹出 VBA 调试框。",
      starterCode: "Option Explicit\n\nPublic Sub RunReport()\n    ' TODO: 在最前面打开错误拦截，让运行期错误跳到下面你自己写的标签\n    Dim total As Double\n    total = 100 / Worksheets(\"Data\").Range(\"A1\").Value  ' A1 为 0 或表不存在就会出错\n    MsgBox \"合计：\" & total\n\n    ' TODO: 在正常路径末尾提前退出本过程，别让代码掉进错误处理块\n    ' TODO: 写一个以冒号结尾的错误处理标签，用 MsgBox 把错误码和文字说明显示出来\nEnd Sub\n",
      hints: ["On Error GoTo 标签 要写在过程靠前的位置，它之后的语句出错才会被接住。", "正常流程结束后要 Exit Sub，否则代码会顺势执行到错误处理块，把没出错也当成出错。", "Err.Number 是错误码（0 表示无错），Err.Description 是文字说明，拼进 MsgBox 给用户看。"],
      tests: [
      test("vb-macro-buttons-3-onerror", "用 On Error GoTo 跳到标签", matches(new RegExp("on\\s+error\\s+goto\\s+\\w+", "i")), "在过程开头写 On Error GoTo ErrHandler。", 30),
      test("vb-macro-buttons-3-label", "定义错误处理标签", matches(new RegExp("errhandler\\s*:", "i")), "要有一个以冒号结尾的标签，例如 ErrHandler:", 25),
      test("vb-macro-buttons-3-exitsub", "正常路径用 Exit Sub 提前退出", matches(new RegExp("exit\\s+sub", "i")), "在错误标签前加 Exit Sub，避免正常执行掉进错误块。", 20),
      test("vb-macro-buttons-3-err", "用 Err.Number 与 Err.Description 报告", matchesAll([new RegExp("err\\.number"), new RegExp("err\\.description")]), "在 MsgBox 里同时用上 Err.Number 和 Err.Description。", 25),
      ],
      sections: [
      sec("用户面前不该出现调试框", "宏挂在按钮上,点的人是普通用户,不是开发者。一旦运行期出错(单元格是 0、工作表名打错、文件没打开),默认 Excel 会弹出带“调试 / 结束”的黄底错误框——这对用户既吓人又没用。On Error GoTo 让你拦下错误,跳到自己写的处理块,换成一句人话提示。这是“能被点”的入口必须配的安全带。", { code: "Public Sub RunReport()\n    On Error GoTo ErrHandler\n    Dim total As Double\n    total = 100 / Worksheets(\"Data\").Range(\"A1\").Value\n    MsgBox \"合计：\" & total\n    Exit Sub\nErrHandler:\n    MsgBox \"运行出错 (\" & Err.Number & \")：\" & Err.Description\nEnd Sub", caption: "On Error + 标签 + Exit Sub 的标准骨架", tip: "把 On Error GoTo 写在过程第一行附近，它之前的语句不受保护。" }),
      sec("Exit Sub 是关键的一道隔断", "标签 ErrHandler: 只是个普通位置,代码从上往下执行时不会“因为没出错就跳过它”。所以正常路径跑完后,必须用 Exit Sub 立刻退出,否则会一路执行进 ErrHandler,在没有任何错误时也弹出错误提示(此时 Err.Number 是 0)。新手常忘这一行,结果“成功也报错”。记住顺序:正常代码 → Exit Sub → 标签 → 处理代码 → End Sub。", { warning: "漏掉 Exit Sub，宏每次成功都会再弹一个空错误,用户会以为坏了。" }),
      sec("Err 对象告诉你出了什么事", "进入错误处理块时,VBA 自动填好全局 Err 对象:Err.Number 是错误码(比如 11 是除零、9 是下标越界、1004 是 Excel 通用运行期错误),Err.Description 是对应的文字。把这两个拼进 MsgBox,用户能描述清楚“按钮报了什么”,你远程排查也快得多。处理完若想清空状态,可调用 Err.Clear。"),
      ],
    }),
    lesson({
      title: "整合：可点的报表按钮",
      badge: "成品",
      icon: "VB",
      objective: "把前三课整合成一条完整链路：按钮 Click 事件 → 调用带 On Error 兜底的 Public Sub GenerateReport，成功提示、失败提示，互不混淆。",
      starterCode: "' === 工作表对象模块（Sheet1 代码区）===\nOption Explicit\n\n' TODO: 写 CommandButton1 的点击事件（私有、无参），里面只放一行：调用下面那个动作宏\n\n\n' === 标准模块（Module1）：把下面整段取消注释并补完 ===\n' Option Explicit\n'\n' 动作宏（公开、无参，自己取名比如生成报表）：\n'   1) 第一行打开错误拦截，跳到本过程末尾的标签\n'   2) 做点活：读 Worksheets(\"Data\").Range(\"A1\") 算个值并写回某格\n'   3) 成功后弹出“报表已生成…”的提示，然后提前退出本过程\n'   4) 标签处再弹一个不同的提示，带上错误码与文字说明\n",
      hints: ["按钮事件保持极简:Private Sub CommandButton1_Click() 里只有一行 GenerateReport。", "GenerateReport 用第3课的骨架:On Error GoTo → 干活 → 成功 MsgBox → Exit Sub → 标签 → 失败 MsgBox。", "成功和失败要给不同的话,别两条路都弹一样的文字,否则用户分不清到底成没成。"],
      tests: [
      test("vb-macro-buttons-4-clickcalls", "按钮事件调用 GenerateReport", matchesAll([new RegExp("private\\s+sub\\s+commandbutton1_click\\s*\\(\\s*\\)"), new RegExp("generatereport")]), "在 CommandButton1_Click 事件里调用 GenerateReport。", 25),
      test("vb-macro-buttons-4-publicaction", "定义 Public Sub GenerateReport", matches(new RegExp("public\\s+sub\\s+generatereport\\s*\\(\\s*\\)", "i")), "把业务逻辑放进 Public Sub GenerateReport()。", 25),
      test("vb-macro-buttons-4-guard", "GenerateReport 带 On Error 兜底与 Exit Sub", matchesAll([new RegExp("on\\s+error\\s+goto\\s+\\w+"), new RegExp("exit\\s+sub")]), "在 GenerateReport 里加 On Error GoTo 标签，正常路径用 Exit Sub。", 25),
      test("vb-macro-buttons-4-bothpaths", "成功提示与基于 Err 的失败提示都有", matchesAll([new RegExp("msgbox[\\s\\S]*err\\.description"), new RegExp("errhandler\\s*:")]), "成功路径用一句 MsgBox，失败标签里另写 MsgBox 并用上 Err.Description。", 25),
      ],
      sections: [
      sec("把三层串成一条链", "到这一步,你已经有了全部零件,现在拼成成品。三层职责清清楚楚:按钮事件(触发层)只管把点击转成一次调用;Public Sub GenerateReport(动作层)装真正的业务逻辑;On Error 兜底(防护层)保证无论数据多脏,用户看到的都是人话而不是调试框。这正是节点目标——把业务动作挂到用户能点击的入口。", { code: "' Sheet1 对象模块\nPrivate Sub CommandButton1_Click()\n    GenerateReport\nEnd Sub\n\n' Module1 标准模块\nPublic Sub GenerateReport()\n    On Error GoTo ErrHandler\n    Dim rows As Long\n    rows = Worksheets(\"Data\").Range(\"A1\").CurrentRegion.Rows.Count\n    Worksheets(\"Data\").Range(\"D1\").Value = rows\n    MsgBox \"报表已生成，共 \" & rows & \" 行\"\n    Exit Sub\nErrHandler:\n    MsgBox \"生成失败 (\" & Err.Number & \")：\" & Err.Description\nEnd Sub", caption: "触发层 + 动作层 + 防护层的完整成品", tip: "动作层是 Public，所以它既能被按钮调用,也能被 Alt+F8 手动运行——一份逻辑,两个入口。" }),
      sec("成功与失败必须说不同的话", "一个常见但要命的偷懒:成功和出错都弹同一句“处理完成”。用户点完按钮根本分不清到底成没成。规矩很简单:正常路径的 MsgBox 描述结果(“共 N 行”),错误标签里的 MsgBox 描述故障(带 Err 信息)。两条路两套话,加上中间那道 Exit Sub,用户得到的反馈才是可信的。", { warning: "别让两个 MsgBox 文案一样,也别让成功路径漏掉 Exit Sub——否则成功也会再弹一个失败框。" }),
      sec("换入口不改逻辑", "这套分层的真正价值在以后:今天入口是 ActiveX 按钮,明天可能改成功能区(Ribbon)按钮、快捷键、或开工作簿时自动跑。无论入口怎么变,它们都只做同一件事——调用 GenerateReport。动作层和防护层一行都不用动。把“谁触发”和“做什么”彻底分开,宏才经得起需求变化。"),
      ],
    }),
  ],
  "vb-capstone-office": [
    lesson({
      title: "搭起 Excel 应用骨架",
      badge: "入口",
      icon: "VB",
      objective: "用后期绑定 CreateObject 拿到 Excel.Application 对象，并新建一个 Workbook 作为报表容器。",
      starterCode: "Imports System\n\nModule ReportTool\n\n    Sub Main()\n        ' 报表工具的第一步：拿到一个能操控 Excel 的对象\n        ' TODO: 用 CreateObject 创建 Excel.Application，赋给 excelApp\n        ' TODO: 用 excelApp.Workbooks.Add() 新建一个工作簿\n        ' 暂时让 Excel 可见，方便你确认它真的启动了\n        Console.WriteLine(\"报表工具启动中...\")\n    End Sub\n\nEnd Module\n",
      hints: ["后期绑定不需要添加 Interop 引用，直接 Dim excelApp As Object = CreateObject(\"Excel.Application\")。", "新建工作簿用 excelApp.Workbooks.Add()，它返回一个 Workbook，先存到变量里。", "调试时设 excelApp.Visible = True，能亲眼看到 Excel 弹出来。"],
      tests: [
      test("vb-capstone-office-1-createobject", "用 CreateObject 创建 Excel.Application", matches(new RegExp("CreateObject\\s*\\(\\s*\"Excel\\.Application\"\\s*\\)", "i")), "缺少 CreateObject(\"Excel.Application\")，这是拿到 Excel 对象的入口。"),
      test("vb-capstone-office-1-workbooks-add", "新建一个工作簿", matches(new RegExp("Workbooks\\s*\\.\\s*Add\\s*\\(", "i")), "需要调用 Workbooks.Add() 创建报表用的工作簿。"),
      ],
      sections: [
      sec("为什么用后期绑定", "自动化 Excel 有两条路：早期绑定（添加 Microsoft.Office.Interop.Excel 引用，写强类型）和后期绑定（CreateObject + Object）。本项目用后期绑定，因为它不依赖目标机器装了哪个版本的 Interop，移植性最好。代价是没有智能提示、编译期不查类型——所以方法名拼错只能等运行时才报错，写的时候要格外小心。"),
      sec("拿对象、开工作簿", "CreateObject(\"Excel.Application\") 启动一个 Excel 进程并返回它的 Application 对象。Application 是一切的根：Workbooks 是它的工作簿集合，Add() 在末尾追加一个空工作簿并返回它。把返回值接住，后面所有写数据的操作都从这个 Workbook 往下走。", { code: "Dim excelApp As Object = CreateObject(\"Excel.Application\")\nexcelApp.Visible = True\nDim wb As Object = excelApp.Workbooks.Add()", caption: "启动 Excel 并新建工作簿", tip: "Option Strict On 时，对 Object 调成员会报错；后期绑定的项目要么关掉它，要么用 Object 但接受晚期解析。" }),
      ],
    }),
    lesson({
      title: "打开源数据并读出一行",
      badge: "读取",
      icon: "VB",
      objective: "打开已有的数据工作簿，定位到第一个 Worksheet，并读出 A 列某个单元格的 Value。",
      starterCode: "Imports System\n\nModule ReadSource\n\n    Sub ReadFirstCell(excelApp As Object, path As String)\n        ' TODO: 打开 path 指向的源数据工作簿（用工作簿集合的打开方法，传入 path）\n        ' TODO: 取到第一个工作表（工作表集合的索引从 1 开始）\n        ' TODO: 读出第 2 行第 1 列单元格的内容并打印\n        Console.WriteLine(\"准备读取源数据\")\n    End Sub\n\nEnd Module\n",
      hints: ["打开已有文件用 Workbooks.Open(path)，不是 Add()——Add 是新建空表。", "Worksheets 的索引从 1 开始，不是 0。Worksheets(1) 是第一个工作表。", "Cells(row, col) 也都从 1 开始，Cells(2, 1) 就是 A2 单元格。"],
      tests: [
      test("vb-capstone-office-2-open", "用 Workbooks.Open 打开源文件", matches(new RegExp("Workbooks\\s*\\.\\s*Open\\s*\\(", "i")), "需要 Workbooks.Open(path) 打开已有的数据工作簿。"),
      test("vb-capstone-office-2-worksheet", "定位第一个工作表", matches(new RegExp("Worksheets\\s*\\(\\s*1\\s*\\)", "i")), "用 Worksheets(1) 取到第一个工作表。"),
      test("vb-capstone-office-2-cells-value", "读出单元格的 Value", matches(new RegExp("Cells\\s*\\([^)]*\\)\\s*\\.\\s*Value", "i")), "用 Cells(行, 列).Value 读出单元格内容。"),
      ],
      sections: [
      sec("对象层级：从 Workbook 到 Cell", "Excel 的对象是一层套一层的：Application → Workbook → Worksheet → Range/Cells。读数据就是顺着这条链走下去。Workbooks.Open 返回打开的 Workbook，Worksheets(1) 拿到第一个表，Cells(行, 列) 定位到一个格子，.Value 才是格子里真正的内容。", { code: "Dim wb As Object = excelApp.Workbooks.Open(path)\nDim ws As Object = wb.Worksheets(1)\nDim firstName As Object = ws.Cells(2, 1).Value\nConsole.WriteLine(firstName)", caption: "顺着对象链读出 A2" }),
      sec("索引从 1 开始的坑", "VB 写惯了数组从 0 开始，但 Excel 对象模型几乎全是 1 起步：Worksheets(1) 是第一个表，Cells(1, 1) 是 A1，Rows(1) 是第一行。把这个差异记牢，否则你会一直差一行、差一列。第 1 行通常是表头，真正的数据从第 2 行开始，所以例子里读的是 Cells(2, 1)。", { warning: "别用 Cells(0, 0)——Excel 里没有第 0 行第 0 列，会直接抛运行时错误。" }),
      ],
    }),
    lesson({
      title: "找到数据边界并循环读取",
      badge: "遍历",
      icon: "VB",
      objective: "用 UsedRange.Rows.Count 求出实际行数，再用 For 循环从第 2 行逐行读取每条记录。",
      starterCode: "Imports System\n\nModule ScanRows\n\n    Sub ScanAll(ws As Object)\n        ' TODO: 用工作表的已用区域求出实际行数，存到 lastRow（提示：已用区域 .Rows 的数量）\n        Dim lastRow As Integer = 0\n        ' TODO: 写一个从第 2 行循环到 lastRow 的 For 循环，逐行读出第 r 行的单元格内容\n        Console.WriteLine(\"扫描完成\")\n    End Sub\n\nEnd Module\n",
      hints: ["UsedRange 是工作表里被用过的矩形区域，.Rows.Count 就是它包含的行数。", "数据从第 2 行起（第 1 行是表头），所以循环写 For r As Integer = 2 To lastRow。", "别硬编码行数（比如写死 100）——下次数据多一行就漏读，UsedRange 才靠谱。"],
      tests: [
      test("vb-capstone-office-3-usedrange", "用 UsedRange.Rows.Count 求行数", matches(new RegExp("UsedRange\\s*\\.\\s*Rows\\s*\\.\\s*Count", "i")), "需要 UsedRange.Rows.Count 动态求出实际行数。"),
      test("vb-capstone-office-3-for", "从第 2 行开始的 For 循环", matches(new RegExp("For\\s+\\w+(\\s+As\\s+Integer)?\\s*=\\s*2\\s+To\\s+lastRow", "i")), "写一个 For r ... = 2 To lastRow 的循环跳过表头。"),
      test("vb-capstone-office-3-read", "循环内读单元格", matches(new RegExp("Cells\\s*\\(\\s*r\\s*,", "i")), "循环里用 Cells(r, ...).Value 读出当前行的数据。"),
      ],
      sections: [
      sec("数据有多长？别猜", "真实数据每天都在变长变短，写死「读 100 行」迟早出事：多了读到一堆空行，少了直接漏数据。UsedRange 让 Excel 自己告诉你它用了多大的区域，.Rows.Count 是其中的行数。从它算出 lastRow，循环就永远和数据对齐。", { code: "Dim lastRow As Integer = ws.UsedRange.Rows.Count\nFor r As Integer = 2 To lastRow\n    Dim name As Object = ws.Cells(r, 1).Value\n    Dim amount As Object = ws.Cells(r, 2).Value\n    Console.WriteLine($\"{name} -> {amount}\")\nNext", caption: "按实际行数逐行遍历" }),
      sec("UsedRange 的小脾气", "UsedRange 不总是从 A1 算起——如果有人删过行又留下格式，它可能虚高几行。本项目数据规整、从 A1 开头，所以直接用它的 Rows.Count 就够。要更精确可以用 ws.Cells(ws.Rows.Count, 1).End(xlUp).Row 从底往上找最后一个非空格，但那是进阶写法，先掌握 UsedRange。", { tip: "想确认边界对不对，循环里临时打印 r 和读到的值，肉眼核对一遍最稳。" }),
      ],
    }),
    lesson({
      title: "校验每一行数据",
      badge: "校验",
      icon: "VB",
      objective: "对读到的金额用 IsNumeric 判断是否为合法数字，非法则记录一条错误并 Continue For 跳过。",
      starterCode: "Imports System\nImports System.Collections.Generic\n\nModule Validate\n\n    Sub CheckRows(ws As Object, lastRow As Integer)\n        Dim errors As New List(Of String)()\n        For r As Integer = 2 To lastRow\n            Dim raw As Object = ws.Cells(r, 2).Value\n            ' TODO: 用 IsNumeric(raw) 判断金额是否为合法数字\n            ' TODO: 如果不是数字，往 errors 里加一条错误信息（带行号 r），然后 Continue For 跳过本行\n            ' 走到这里说明金额合法，可以继续汇总\n        Next\n        Console.WriteLine($\"发现 {errors.Count} 条错误\")\n    End Sub\n\nEnd Module\n",
      hints: ["IsNumeric(raw) 返回 True/False，专门判断一个值能不能当数字用，对付空格、文字、空单元格都行。", "校验失败时用 If Not IsNumeric(raw) Then ... Continue For，让循环直接进入下一行。", "错误信息带上行号 r，导出时用户才能定位是哪一行出了问题。"],
      tests: [
      test("vb-capstone-office-4-isnumeric", "用 IsNumeric 校验金额", matches(new RegExp("IsNumeric\\s*\\(", "i")), "需要 IsNumeric(...) 判断金额是不是合法数字。"),
      test("vb-capstone-office-4-continue", "非法行用 Continue For 跳过", matches(new RegExp("Continue\\s+For", "i")), "校验失败后用 Continue For 跳过这一行的后续处理。"),
      test("vb-capstone-office-4-record", "把错误记进 errors 列表", matches(new RegExp("errors\\s*\\.\\s*Add\\s*\\(", "i")), "用 errors.Add(...) 记录一条带行号的错误信息。"),
      ],
      sections: [
      sec("脏数据是常态", "报表工具最大的敌人不是逻辑，是数据：金额列里混进「待定」、空格、全角数字、空单元格。如果不先校验就直接加总，轻则结果错，重则抛类型转换异常整个程序崩掉。校验的核心是：在用一个值之前，先确认它是你以为的类型。", { code: "Dim raw As Object = ws.Cells(r, 2).Value\nIf Not IsNumeric(raw) Then\n    errors.Add($\"第 {r} 行金额非法: {raw}\")\n    Continue For\nEnd If\nDim amount As Double = CDbl(raw)", caption: "先校验，确认合法再转换", tip: "IsNumeric 通过后再 CDbl 才安全；顺序反了就失去了校验的意义。" }),
      sec("收集错误，而不是中断", "好工具遇到一行坏数据不会立刻罢工，而是记下来、跳过、继续处理后面的行——最后把所有问题一次性报告给用户。用一个 List(Of String) 累积错误信息，每条都带上行号 r。Continue For 是关键：它结束当前这一轮循环，但不退出循环，让坏行被优雅地略过。"),
      ],
    }),
    lesson({
      title: "分组汇总金额",
      badge: "汇总",
      icon: "VB",
      objective: "用 Dictionary(Of String, Double) 按部门累加金额，键不存在时初始化为 0 再相加。",
      starterCode: "Imports System\nImports System.Collections.Generic\n\nModule Summarize\n\n    Function GroupByDept(ws As Object, lastRow As Integer) As Dictionary(Of String, Double)\n        ' TODO: 创建 Dictionary(Of String, Double) 存「部门 -> 金额合计」\n        For r As Integer = 2 To lastRow\n            Dim dept As String = CStr(ws.Cells(r, 3).Value)\n            Dim amount As Double = CDbl(ws.Cells(r, 2).Value)\n            ' TODO: 如果字典里没有这个部门(ContainsKey 为 False)，先把它初始化为 0\n            ' TODO: 把 amount 累加到该部门上\n        Next\n        ' TODO: 返回这个字典\n        Return Nothing\n    End Function\n\nEnd Module\n",
      hints: ["Dictionary(Of String, Double) 的键是部门名，值是该部门的金额合计。", "累加前先 If Not totals.ContainsKey(dept) Then totals(dept) = 0，否则对不存在的键累加会抛 KeyNotFoundException。", "累加写 totals(dept) += amount，比 totals(dept) = totals(dept) + amount 更短也更清楚。"],
      tests: [
      test("vb-capstone-office-5-dict", "用 Dictionary(Of String, Double) 存合计", matches(new RegExp("Dictionary\\s*\\(\\s*Of\\s+String\\s*,\\s*Double\\s*\\)", "i")), "需要声明一个 Dictionary(Of String, Double) 来按部门累加。"),
      test("vb-capstone-office-5-containskey", "累加前用 ContainsKey 检查", matches(new RegExp("ContainsKey\\s*\\(", "i")), "用 ContainsKey 判断键是否存在，避免对缺失键累加。"),
      test("vb-capstone-office-5-accumulate", "把金额累加到对应部门", matches(new RegExp("\\(\\s*dept\\s*\\)\\s*\\+=", "i")), "用 totals(dept) += amount 把金额加到该部门合计上。"),
      ],
      sections: [
      sec("字典就是分组的天然容器", "「按部门汇总」本质是分组累加：相同的键归到一起，把它们的值加起来。Dictionary(Of String, Double) 完美对应——部门名当键，合计金额当值。遍历每一行时，把这一行的金额加到它所属部门的累计上即可。", { code: "Dim totals As New Dictionary(Of String, Double)()\nFor r As Integer = 2 To lastRow\n    Dim dept As String = CStr(ws.Cells(r, 3).Value)\n    Dim amount As Double = CDbl(ws.Cells(r, 2).Value)\n    If Not totals.ContainsKey(dept) Then totals(dept) = 0\n    totals(dept) += amount\nNext\nReturn totals", caption: "按部门把金额累加进字典" }),
      sec("为什么必须先初始化键", "对一个不存在的键直接 += 会抛 KeyNotFoundException——因为读不存在的键会失败。所以模式永远是「不在就先放 0，再累加」。ContainsKey 检查 + 初始化是这个项目里最容易写错又最常见的一步，记牢这两行的顺序。如果用 .NET 较新的写法，也可以用 TryGetValue 一步完成，但 ContainsKey 最直观。", { warning: "别假设字典默认值是 0——读未初始化的键不是返回 0，而是直接抛异常。" }),
      ],
    }),
    lesson({
      title: "把汇总写回报表表",
      badge: "写入",
      icon: "VB",
      objective: "在报表工作表写好表头，再用 For Each 遍历字典，把每个部门和合计写进 Cells，行号随循环递增。",
      starterCode: "Imports System\nImports System.Collections.Generic\n\nModule WriteReport\n\n    Sub Render(reportWs As Object, totals As Dictionary(Of String, Double))\n        ' TODO: 写表头：Cells(1, 1) = \"部门\"，Cells(1, 2) = \"金额合计\"\n        Dim rowIndex As Integer = 2\n        ' TODO: 用 For Each 遍历 totals，每个键值对写到 Cells(rowIndex, 1) 和 Cells(rowIndex, 2)\n        ' 每写一行，rowIndex 加 1\n        Console.WriteLine(\"报表已生成\")\n    End Sub\n\nEnd Module\n",
      hints: ["写单元格和读一样：Cells(行, 列).Value = 内容，只是把读改成赋值。", "遍历字典用 For Each kv As KeyValuePair(Of String, Double) In totals，kv.Key 是部门，kv.Value 是合计。", "每写完一行别忘了 rowIndex += 1，否则所有部门会覆盖在同一行。"],
      tests: [
      test("vb-capstone-office-6-header", "在第 1 行写表头", matches(new RegExp("Cells\\s*\\(\\s*1\\s*,\\s*1\\s*\\)\\s*\\.\\s*Value\\s*=", "i")), "用 Cells(1, 1).Value = ... 写出第一行的表头文字。"),
      test("vb-capstone-office-6-foreach", "用 For Each 遍历字典", matches(new RegExp("For\\s+Each\\s+\\w+.*In\\s+totals", "i")), "用 For Each ... In totals 遍历每个部门的合计。"),
      test("vb-capstone-office-6-write-cells", "把合计写进 Cells(rowIndex, ...)", matches(new RegExp("Cells\\s*\\(\\s*rowIndex\\s*,[^)]*\\)\\s*\\.\\s*Value\\s*=", "i")), "用 Cells(rowIndex, 2).Value = ... 把金额写进当前行。"),
      test("vb-capstone-office-6-advance", "每行后递增 rowIndex", matches(new RegExp("rowIndex\\s*\\+=\\s*1", "i")), "每写完一行用 rowIndex += 1 让下一个部门写到下一行。"),
      ],
      sections: [
      sec("写入是读取的镜像", "读单元格是 x = Cells(r, c).Value，写就是 Cells(r, c).Value = x，方向反过来而已。报表的样子由你掌控：第 1 行放表头让人看得懂，从第 2 行起一行一个部门。关键变量是 rowIndex——它记录「下一行写在哪」，每写一行就往下挪一格。", { code: "reportWs.Cells(1, 1).Value = \"部门\"\nreportWs.Cells(1, 2).Value = \"金额合计\"\nDim rowIndex As Integer = 2\nFor Each kv As KeyValuePair(Of String, Double) In totals\n    reportWs.Cells(rowIndex, 1).Value = kv.Key\n    reportWs.Cells(rowIndex, 2).Value = kv.Value\n    rowIndex += 1\nNext", caption: "表头 + 逐行写入字典内容" }),
      sec("遍历字典的两个值", "For Each 遍历字典时，每个元素是一个 KeyValuePair：.Key 是部门名，.Value 是合计金额。把 Key 写到第一列、Value 写到第二列，一张干净的汇总表就成形了。注意字典遍历顺序不保证有序，如果要按部门名或金额排序，得先取出键再 Sort，那是后续可以加的优化。", { tip: "先让数据正确写进去，再谈排序、加粗表头、数字格式这些美化。" }),
      ],
    }),
    lesson({
      title: "一键跑通：保存、收尾、释放",
      badge: "收尾",
      icon: "VB",
      objective: "用 SaveAs 导出报表，在 Try/Finally 里 Quit 退出 Excel，并用 ReleaseComObject 释放对象，串起完整流程。",
      starterCode: "Imports System\nImports System.Runtime.InteropServices\nImports System.Collections.Generic\n\nModule RunAll\n\n    Sub Generate(sourcePath As String, outputPath As String)\n        Dim excelApp As Object = CreateObject(\"Excel.Application\")\n        Try\n            ' 读取 -> 校验 -> 汇总 -> 写入 都已在前几关完成，这里串起来\n            Dim reportWb As Object = excelApp.Workbooks.Add()\n            Dim reportWs As Object = reportWb.Worksheets(1)\n            ' ... 假设 reportWs 已经写好汇总数据 ...\n\n            ' TODO: 把 reportWb 另存到 outputPath（用工作簿的另存为方法，传入 outputPath）\n            Console.WriteLine(\"报表已导出\")\n        Catch ex As Exception\n            Console.WriteLine($\"生成失败: {ex.Message}\")\n        Finally\n            ' TODO: 让 excelApp 退出，关闭 Excel 进程\n            ' TODO: 用 Marshal 的方法释放 excelApp 这个 COM 对象引用\n        End Try\n    End Sub\n\nEnd Module\n",
      hints: ["导出用 reportWb.SaveAs(outputPath)；想兼容旧版可传文件格式参数，先跑通基本形式。", "Quit 和释放一定要放在 Finally 里——这样即使中途抛异常，Excel 进程也不会变成幽灵进程挂在后台。", "Marshal.ReleaseComObject(excelApp) 来自 System.Runtime.InteropServices，专门释放 COM 引用，否则 EXCEL.EXE 不退出。"],
      tests: [
      test("vb-capstone-office-7-saveas", "用 SaveAs 导出报表", matches(new RegExp("SaveAs\\s*\\(", "i")), "需要 SaveAs(outputPath) 把生成的报表保存到磁盘。"),
      test("vb-capstone-office-7-finally-quit", "在 Finally 里 Quit 退出 Excel", matchesAll([new RegExp("Finally"), new RegExp("Quit\\s*\\(")]), "Finally 块里要调用 excelApp.Quit() 关闭 Excel 进程。"),
      test("vb-capstone-office-7-release", "用 ReleaseComObject 释放对象", matches(new RegExp("ReleaseComObject\\s*\\(", "i")), "用 Marshal.ReleaseComObject(...) 释放 Excel 的 COM 引用。"),
      ],
      sections: [
      sec("把四步串成一键流程", "到这一关，读取（Open + UsedRange + 循环）、校验（IsNumeric + 收集错误）、汇总（字典分组）、写入（For Each + Cells）都已就位。本关把它们装进一个 Generate(sourcePath, outputPath) 函数：打开源数据→逐行校验汇总→写进报表→SaveAs 导出。一次调用，整张报表落地，这就是「一键报表工具」。", { code: "reportWb.SaveAs(outputPath)\nConsole.WriteLine($\"报表已导出到 {outputPath}\")", caption: "汇总完成后导出报表文件" }),
      sec("幽灵进程：自动化 Excel 的头号坑", "自动化最常见的事故是：程序跑完了，任务管理器里还挂着一个看不见的 EXCEL.EXE。原因是你的代码还持有 COM 引用，Excel 以为有人在用它，不肯退出。正确收尾分两步：先 Quit() 让 Excel 自己关，再用 Marshal.ReleaseComObject 把你这边的引用计数减到 0。两步都必须放在 Finally，确保哪怕中途报错也一定执行。", { code: "Finally\n    If excelApp IsNot Nothing Then\n        excelApp.Quit()\n        Marshal.ReleaseComObject(excelApp)\n    End If\nEnd Try", caption: "Finally 里安全退出并释放", warning: "只 Quit 不 ReleaseComObject，进程照样残留；只 Release 不 Quit，Excel 不会主动关。两步缺一不可。" }),
      ],
    }),
  ],
  "vb-form-events": [
    lesson({
      title: "窗体打开时跑一次：Load 事件",
      badge: "入口",
      icon: "VB",
      objective: "在 Form1_Load 里把 Label1 的初始文字设成「欢迎」，让窗体一显示就完成初始化。",
      starterCode: "Public Class Form1\n\n    ' Load 事件在窗体第一次显示之前触发，正好用来做初始化\n    Private Sub Form1_Load(sender As Object, e As EventArgs) Handles MyBase.Load\n        ' TODO: 把 Label1.Text 设成 \"欢迎\"\n\n    End Sub\n\nEnd Class\n",
      hints: ["Load 只在窗体生命周期里跑一次，是放初始默认值的最佳位置，别等用户点了按钮才设。", "控件的文字通过 .Text 属性赋值：Label1.Text = \"欢迎\"。", "事件方法靠末尾的 Handles MyBase.Load 绑定到窗体的 Load，名字叫什么不是关键。"],
      tests: [
      test("vb-form-events-1-handles-load", "方法用 Handles MyBase.Load 绑定 Load 事件", matches(new RegExp("Handles\\s+MyBase\\.Load", "i")), "事件处理方法末尾要写 Handles MyBase.Load 才会在窗体加载时被调用。"),
      test("vb-form-events-1-set-label", "在 Load 里把 Label1.Text 设成 欢迎", matches(new RegExp("Label1\\.Text\\s*=\\s*\"欢迎\"", "i")), "在 Form1_Load 内写 Label1.Text = \"欢迎\"。"),
      ],
      sections: [
      sec("事件流：界面是被动响应的", "WinForms 程序不是从上到下顺序执行的脚本。窗体显示出来后，它就坐在那里「等」——等用户点击、等文字改变、等关闭。每发生一件事，运行时就去调用你为那件事写的方法。这套「事件发生 → 调用对应处理方法」的机制，就是窗体事件流。这一节我们从最先触发的 Load 开始。"),
      sec("Load 在显示之前触发，且只跑一次", "Load 事件在窗体即将第一次显示给用户之前触发。它在整个窗体生命周期里只跑一次，所以是设置初始值、填充下拉框、读配置的标准位置。关键是结尾的 Handles MyBase.Load——MyBase 指当前窗体本身，这行才是把方法挂到 Load 事件上的开关。方法名叫不叫 Form1_Load 都行。", { code: "Private Sub Form1_Load(sender As Object, e As EventArgs) Handles MyBase.Load\n    Label1.Text = \"欢迎\"\nEnd Sub", caption: "窗体一加载就把标签初始化好", tip: "sender 是触发事件的对象，e 携带事件细节——现在用不到，但每个事件处理方法都带着它俩，记住这个固定签名。", warning: "别把初始化写在构造函数 New() 里去碰控件——那时控件可能还没建好。需要碰界面的初始化，放进 Load 才稳。" }),
      ],
    }),
    lesson({
      title: "用户点一下就响应：Click 事件",
      badge: "点击",
      icon: "VB",
      objective: "为 Button1.Click 写处理方法，每次点击都把 Label1.Text 改成「已点击」。",
      starterCode: "Public Class Form1\n\n    Private Sub Form1_Load(sender As Object, e As EventArgs) Handles MyBase.Load\n        Label1.Text = \"等待点击\"\n    End Sub\n\n    ' TODO: 写一个方法处理 Button1 的 Click 事件\n    ' 提示：方法签名是 (sender As Object, e As EventArgs)，末尾用 Handles Button1.Click\n    ' 在方法体里把 Label1.Text 改成 \"已点击\"\n\nEnd Class\n",
      hints: ["Click 处理方法和 Load 长得一样，区别只在 Handles 后面挂的是 Button1.Click。", "Load 一辈子只触发一次，Click 则是每点一下就触发一次，可以反复跑。", "方法体里直接 Label1.Text = \"已点击\" 即可，不用判断点了几次。"],
      tests: [
      test("vb-form-events-2-handles-click", "方法用 Handles Button1.Click 绑定点击事件", matches(new RegExp("Handles\\s+Button1\\.Click", "i")), "事件方法末尾要写 Handles Button1.Click，否则点击不会调用它。"),
      test("vb-form-events-2-set-clicked", "点击后把 Label1.Text 设成 已点击", matches(new RegExp("Label1\\.Text\\s*=\\s*\"已点击\"", "i")), "在 Click 方法里写 Label1.Text = \"已点击\"。"),
      ],
      sections: [
      sec("Click 与 Load 的根本差别：触发次数", "上一节的 Load 在整个生命周期里只跑一次，做的是「开场布置」。Click 完全不同：它由用户的鼠标驱动，点一次跑一次，点一百次跑一百次。这就是事件流的本质——代码不主动跑，而是等用户的动作把它「唤起」。理解这两者的触发节奏不同，是看懂事件流的第一步。"),
      sec("把方法挂到某个控件的事件上", "Load 用 Handles MyBase.Load，因为 Load 是窗体自己的事件。Click 属于按钮，所以写 Handles Button1.Click——前面是控件名，后面是事件名。方法签名仍是固定的 (sender As Object, e As EventArgs)。运行时一旦检测到 Button1 被点击，就照着 Handles 的登记表找到这个方法并执行它。", { code: "Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click\n    Label1.Text = \"已点击\"\nEnd Sub", caption: "Handles Button1.Click 把方法接到按钮的点击上", tip: "一个方法可以同时 Handles Button1.Click, Button2.Click 接管多个按钮；用 sender 判断是谁触发的，能省掉重复代码。", warning: "别在 Click 里做耗时操作（比如循环几秒），那会卡死界面——点击线程就是界面线程，它忙着你的代码就没法重绘。" }),
      ],
    }),
    lesson({
      title: "每敲一个字就触发：TextChanged",
      badge: "联动",
      icon: "VB",
      objective: "处理 TextBox1.TextChanged，把 Label1.Text 实时同步成 TextBox1 里当前的文字。",
      starterCode: "Public Class Form1\n\n    Private Sub Form1_Load(sender As Object, e As EventArgs) Handles MyBase.Load\n        Label1.Text = \"\"\n    End Sub\n\n    ' TODO: 写一个方法处理 TextBox1 的 TextChanged 事件\n    ' 末尾用 Handles TextBox1.TextChanged\n    ' 在方法体里把 Label1.Text 设成 TextBox1.Text，实现实时同步\n\nEnd Class\n",
      hints: ["TextChanged 在文本框内容每次改变时都触发——敲一个字、删一个字、粘贴，都算一次。", "实时同步就是把读到的 TextBox1.Text 直接赋给 Label1.Text。", "不用等用户按回车或点按钮，TextChanged 让界面随输入即时变化。"],
      tests: [
      test("vb-form-events-3-handles-changed", "方法用 Handles TextBox1.TextChanged 绑定", matches(new RegExp("Handles\\s+TextBox1\\.TextChanged", "i")), "事件方法末尾要写 Handles TextBox1.TextChanged。"),
      test("vb-form-events-3-sync", "把 Label1.Text 同步成 TextBox1.Text", matches(new RegExp("Label1\\.Text\\s*=\\s*TextBox1\\.Text", "i")), "在 TextChanged 里写 Label1.Text = TextBox1.Text。"),
      ],
      sections: [
      sec("TextChanged：比 Click 更频繁的事件", "把三种事件按触发频率排个序就一目了然：Load 一辈子一次，Click 每点一下一次，TextChanged 每改一个字一次。在文本框里打「你好」，TextChanged 会触发两次——「你」一次、「你好」一次。删字、粘贴同样触发。它专门用来做「输入即响应」的实时联动，比如边打字边显示字数、边搜索边出结果。"),
      sec("用赋值实现实时同步", "处理方法依旧是熟悉的形状，只是 Handles 后面换成 TextBox1.TextChanged。方法体里只要一行——把文本框当前的文字读出来，立刻写进标签。因为这个方法在每次改动后都会被调用，标签就会像影子一样跟着文本框走，完全不用等任何确认动作。", { code: "Private Sub TextBox1_TextChanged(sender As Object, e As EventArgs) Handles TextBox1.TextChanged\n    Label1.Text = TextBox1.Text\nEnd Sub", caption: "文本框一变，标签立刻跟上", tip: "想做「实时字数」就改成 Label1.Text = TextBox1.Text.Length & \" 字\"——同一个事件，换个表达式就是另一个功能。", warning: "别在 TextChanged 里反过来改 TextBox1.Text 自己——那会再次触发 TextChanged，可能引起无限循环。要改它，先用一个布尔标志位挡住。" }),
      ],
    }),
    lesson({
      title: "三种事件协作：迷你登记表单",
      badge: "整合",
      icon: "VB",
      objective: "Load 里禁用提交按钮，TextChanged 里根据文本框是否为空启用/禁用按钮，Click 里把姓名拼进 Label1 显示。",
      starterCode: "Public Class Form1\n\n    ' 控件：TextBox1 输入姓名，Button1 提交，Label1 显示结果\n\n    Private Sub Form1_Load(sender As Object, e As EventArgs) Handles MyBase.Load\n        Label1.Text = \"请输入姓名\"\n        ' TODO: 窗体一打开就禁用按钮（Button1.Enabled = False），因为还没输入\n\n    End Sub\n\n    ' TODO: 处理 TextBox1.TextChanged\n    ' 当 TextBox1.Text 不为空字符串时 Button1.Enabled = True，否则 = False\n\n    ' TODO: 处理 Button1.Click\n    ' 把 Label1.Text 设成 \"你好, \" & TextBox1.Text\n\nEnd Class\n",
      hints: ["三个事件各管一段：Load 定初始状态、TextChanged 维护按钮可用性、Click 出结果。这正是窗体事件流的完整链路。", "按钮的可点性由 Button1.Enabled 控制，True 可点、False 灰掉不可点。", "判断为空用 If TextBox1.Text <> \"\" Then ... Else ...（空串就是两个挨着的引号）；拼字符串用 & 连接：\"你好, \" & TextBox1.Text。"],
      tests: [
      test("vb-form-events-4-load-disable", "Load 里禁用按钮", matches(new RegExp("Button1\\.Enabled\\s*=\\s*False", "i")), "在 Form1_Load 里写 Button1.Enabled = False。", 25),
      test("vb-form-events-4-changed-handler", "处理 TextChanged 并按空判断启用按钮", matchesAll([new RegExp("Handles\\s+TextBox1\\.TextChanged"), new RegExp("If\\s+TextBox1\\.Text\\s*<>\\s*\"\""), new RegExp("Button1\\.Enabled\\s*=\\s*True")]), "写 Handles TextBox1.TextChanged，并用 If 判断后设 Button1.Enabled = True。", 25),
      test("vb-form-events-4-click-handler", "Click 里把姓名拼进 Label1", matchesAll([new RegExp("Handles\\s+Button1\\.Click"), new RegExp("Label1\\.Text\\s*=\\s*\"你好, \"\\s*&\\s*TextBox1\\.Text")]), "写 Handles Button1.Click，并设 Label1.Text = \"你好, \" & TextBox1.Text。", 25),
      test("vb-form-events-4-all-three", "三个事件都被处理", includesAll(["Handles MyBase.Load", "Handles TextBox1.TextChanged", "Handles Button1.Click"]), "确保 MyBase.Load、TextBox1.TextChanged、Button1.Click 三个 Handles 都写齐。", 25),
      ],
      sections: [
      sec("把三种事件接成一条流水线", "现在把前三节学的拼起来，做一个真实的小表单：输入姓名 → 提交 → 打招呼。三个事件分工明确：Load 在窗体一出现时把按钮禁用，因为还没填名字不该让点；TextChanged 在每次输入变化时检查文本框，有内容就启用按钮、没内容就再禁用；Click 在用户真正提交时把姓名拼成问候语显示。这就是一个完整窗体的事件流。"),
      sec("状态由事件维护，而不是某处一次算死", "注意按钮的「能不能点」不是某一行代码决定的，而是被两个事件持续维护：Load 给它一个安全的初始值(禁用)，TextChanged 随着输入不断把它纠正到正确状态。这是事件驱动界面的核心思路——界面状态是一连串事件的累积结果，你要做的是为每个事件写好「该如何更新状态」，而不是企图在一个地方算出最终样子。", { code: "Private Sub TextBox1_TextChanged(sender As Object, e As EventArgs) Handles TextBox1.TextChanged\n    If TextBox1.Text <> \"\" Then\n        Button1.Enabled = True\n    Else\n        Button1.Enabled = False\n    End If\nEnd Sub\n\nPrivate Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click\n    Label1.Text = \"你好, \" & TextBox1.Text\nEnd Sub", caption: "TextChanged 守住按钮状态，Click 负责出结果", tip: "Button1.Enabled = (TextBox1.Text <> \"\") 这一行能替掉整个 If/Else——比较的结果本就是布尔值，直接赋给 Enabled 更利落。", warning: "别忘了 Load 里的初始禁用。若漏掉，空表单时按钮仍可点，用户点出一句「你好, 」——状态从一开始就没设对，后面 TextChanged 也救不回第一帧。" }),
      ],
    }),
  ],
  "vb-grid-binding": [
    lesson({
      title: "为表格准备业务模型类",
      badge: "模型",
      icon: "VB",
      objective: "定义一个带公开属性的 Product 类（Id、Name、Price），DataGridView 之后会按这些属性自动生成列。",
      starterCode: "Public Class Product\n\n    ' TODO: 声明三个公开属性，作为表格的三列\n    ' Id   As Integer\n    ' Name As String\n    ' Price As Decimal\n\nEnd Class\n\nModule Demo\n    Sub Main()\n        Dim p As New Product()\n        ' TODO: 给三个属性赋值（用对象初始化器 With {...} 也可以）\n\n        Console.WriteLine(p.Name)\n    End Sub\nEnd Module\n",
      hints: ["DataGridView 绑定时是按「公开属性」生成列的，不是按字段。一定要写成 Public Property，不能只写 Public 变量。", "VB 的自动属性最省事：Public Property Name As String，编译器会替你生成 getter/setter。", "价格用 Decimal 而不是 Double——金额计算用 Decimal 才不会出现 0.1 + 0.2 那种浮点误差。"],
      tests: [
      test("vb-grid-binding-1-prop-id", "声明 Id 属性", matches(new RegExp("Public\\s+Property\\s+Id\\s+As\\s+Integer", "i")), "写 Public Property Id As Integer。", 25),
      test("vb-grid-binding-1-prop-name", "声明 Name 属性", matches(new RegExp("Public\\s+Property\\s+Name\\s+As\\s+String", "i")), "写 Public Property Name As String。", 25),
      test("vb-grid-binding-1-prop-price", "用 Decimal 声明 Price 属性", matches(new RegExp("Public\\s+Property\\s+Price\\s+As\\s+Decimal", "i")), "写 Public Property Price As Decimal，金额别用 Double。", 25),
      test("vb-grid-binding-1-assign", "给某个属性赋值", matches(new RegExp("p\\.(Id|Name|Price)\\s*=", "i")), "给 p.Name（或其它属性）赋一个值。", 25),
      ],
      sections: [
      sec("表格的每一列，来自模型的一个属性", "WinForms 的 DataGridView 不像你想的那样要你手工建每一列。你只要给它一份对象列表，它会反射对象的「公开属性」，每个属性自动变成一列、属性名变成列标题。所以第一步永远是先把业务模型写对：要在表格里看到哪几列，模型上就有哪几个 Public Property。", { code: "Public Class Product\n    Public Property Id As Integer\n    Public Property Name As String\n    Public Property Price As Decimal\nEnd Class", caption: "三个属性 → 表格里三列", tip: "属性的声明顺序，决定默认的列顺序——把最重要的字段写在前面。" }),
      sec("属性，不是字段——这是绑定能否工作的关键", "新手最常踩的坑:写成 Public Id As Integer(这是字段),结果表格里一列都不出现。数据绑定底层走的是反射属性那套机制,只认 Property。VB 的自动属性一行就够,编译器替你补上 get/set。", { code: "Dim p As New Product With {\n    .Id = 1,\n    .Name = \"机械键盘\",\n    .Price = 299.0D\n}", caption: "用对象初始化器一次填好三个属性", warning: "别写成 Public Id As Integer 这种字段形式——它不会变成表格的列,绑定时会被静默忽略。" }),
      ],
    }),
    lesson({
      title: "把列表绑定到 DataGridView",
      badge: "绑定",
      icon: "VB",
      objective: "构造一个 List(Of Product)，赋给 grid.DataSource，让表格一次性显示整份业务列表。",
      starterCode: "Public Class MainForm\n    Inherits System.Windows.Forms.Form\n\n    Private grid As New DataGridView()\n\n    Public Sub LoadData()\n        ' 准备一份业务列表\n        Dim products As New List(Of Product)()\n        products.Add(New Product With {.Id = 1, .Name = \"机械键盘\", .Price = 299D})\n        products.Add(New Product With {.Id = 2, .Name = \"无线鼠标\", .Price = 99D})\n\n        ' TODO: 把 products 赋给 grid.DataSource，让表格显示它\n\n        ' TODO: 把列宽自动撑满，设 grid.AutoSizeColumnsMode 为 Fill\n    End Sub\nEnd Class\n",
      hints: ["绑定就一句话：grid.DataSource = products。表格会立刻读出 List 里每个 Product 的属性铺成行。", "DataSource 接受任何「能枚举的对象集合」,List(Of Product) 完全合格,不需要你手动加行。", "grid.AutoSizeColumnsMode = DataGridViewAutoSizeColumnsMode.Fill 能让列宽自动撑满表格宽度。"],
      tests: [
      test("vb-grid-binding-2-datasource", "把列表赋给 grid.DataSource", matches(new RegExp("grid\\.DataSource\\s*=\\s*products", "i")), "写 grid.DataSource = products,这一步才真正把数据交给表格。"),
      test("vb-grid-binding-2-autosize", "设置列宽自动撑满", matches(new RegExp("AutoSizeColumnsMode\\s*=\\s*DataGridViewAutoSizeColumnsMode\\.Fill", "i")), "设 grid.AutoSizeColumnsMode = DataGridViewAutoSizeColumnsMode.Fill。"),
      ],
      sections: [
      sec("DataSource:一句话点亮整张表", "DataGridView 真正的威力在 DataSource 属性。你把一份对象集合赋给它,表格自动:1) 反射出列,2) 为集合里每个元素生成一行,3) 把属性值填进对应单元格。你不需要写任何 grid.Rows.Add——那是没绑定数据源时的老做法,既啰嗦又容易和真实数据脱节。", { code: "Dim products As New List(Of Product)()\nproducts.Add(New Product With {.Id = 1, .Name = \"机械键盘\", .Price = 299D})\ngrid.DataSource = products", caption: "List 进,表格行自动出", tip: "DataSource 可以是 List、数组、DataTable……只要能被枚举即可。先用 List 把概念跑通。" }),
      sec("让表格好看一点:列宽自适应", "默认列宽是固定像素,数据多了就被截断、右边还留一片空白。把 AutoSizeColumnsMode 设成 Fill,所有列会按比例自动撑满表格宽度,这是展示业务列表最实用的一档。", { code: "grid.AutoSizeColumnsMode = DataGridViewAutoSizeColumnsMode.Fill", caption: "Fill 模式:列宽随表格宽度自适应", warning: "别每次刷新数据都重设 DataSource 又重建列——那会丢掉用户调过的列宽、排序。后面会学更聪明的更新方式。" }),
      ],
    }),
    lesson({
      title: "用 BindingList 让增删实时刷新",
      badge: "实时",
      icon: "VB",
      objective: "把数据源换成 BindingList(Of Product),之后对它 Add 一项,表格会立即多出一行而不用重新赋 DataSource。",
      starterCode: "Imports System.ComponentModel\n\nPublic Class MainForm\n    Inherits System.Windows.Forms.Form\n\n    Private grid As New DataGridView()\n\n    ' TODO: 把字段类型从 List(Of Product) 改成 BindingList(Of Product)\n    Private products As New List(Of Product)()\n\n    Public Sub LoadData()\n        grid.DataSource = products\n    End Sub\n\n    ' 用户点「新增」按钮时调用\n    Public Sub OnAddClicked()\n        ' TODO: 直接对 products 调 Add，表格应立即出现新行\n        ' New Product With {.Id = 3, .Name = \"显示器\", .Price = 1299D}\n    End Sub\nEnd Class\n",
      hints: ["普通 List 不会通知表格“我变了”,所以 Add 之后表格没反应。换成 BindingList(Of Product) 就自带变更通知。", "BindingList 在 System.ComponentModel 命名空间,起手要 Imports System.ComponentModel。", "用法和 List 一模一样:products.Add(...) 即可,差别只在它会自动推动表格刷新。"],
      tests: [
      test("vb-grid-binding-3-bindinglist-field", "字段改用 BindingList(Of Product)", matches(new RegExp("products\\s+As\\s+New\\s+BindingList\\s*\\(\\s*Of\\s+Product\\s*\\)", "i")), "把 products 声明改成 Private products As New BindingList(Of Product)()。"),
      test("vb-grid-binding-3-add", "对 products 调 Add 新增一项", matches(new RegExp("products\\.Add\\s*\\(\\s*New\\s+Product", "i")), "在 OnAddClicked 里写 products.Add(New Product With {...})。"),
      ],
      sections: [
      sec("List 绑得上,却“通知不了”", "上一课用 List 绑定能正常显示,但有个隐藏短板:你对 List 做 Add/Remove,表格根本不知道。因为 List 不会向外发出“集合变了”的信号。结果就是数据变了、界面不动,你只能笨办法重新赋一次 DataSource。", { code: "Private products As New List(Of Product)()\nproducts.Add(New Product With {.Id = 3, .Name = \"显示器\", .Price = 1299D})\n' 表格毫无反应——List 没有变更通知", caption: "List 的 Add 不会反映到表格", warning: "别用「Add 后重新 grid.DataSource = products」来硬刷——这会重建整张表,丢失选中行和列宽。" }),
      sec("BindingList:集合一变,表格就动", "BindingList(Of T) 实现了 IBindingList,会在 Add/Remove 时自动发出变更通知,DataGridView 监听到就立刻加行/删行。用法跟 List 完全一样,只是把类型一换,界面就活了。这是 WinForms 里做“可编辑业务列表”的标准容器。", { code: "Imports System.ComponentModel\n\nPrivate products As New BindingList(Of Product)()\n' ...\nproducts.Add(New Product With {.Id = 3, .Name = \"显示器\", .Price = 1299D})\n' 表格立刻多出一行", caption: "换成 BindingList,Add 即刻见效", tip: "想让“修改某个属性”也实时反映,模型再实现 INotifyPropertyChanged 即可——增删用 BindingList,改值用属性通知,两者配合。" }),
      ],
    }),
    lesson({
      title: "定制列:标题、格式与只读",
      badge: "列",
      icon: "VB",
      objective: "关掉自动建列,改用 AutoGenerateColumns = False,把 Price 列的标题设成中文、格式化成货币,并把 Id 列设为只读。",
      starterCode: "Public Class MainForm\n    Inherits System.Windows.Forms.Form\n\n    Private grid As New DataGridView()\n    Private products As New BindingList(Of Product)()\n\n    Public Sub SetupColumns()\n        ' TODO: 关闭自动建列，改成手工控制\n        ' grid.AutoGenerateColumns = ...\n\n        grid.DataSource = products\n\n        ' 绑定后，列已存在，可以按属性名 grid.Columns(\"Price\") 拿到它\n        ' TODO: 把 Price 列的 HeaderText 设成 \"单价\"\n\n        ' TODO: 把 Price 列的 DefaultCellStyle.Format 设成货币格式 \"C2\"\n\n        ' TODO: 把 Id 列设为只读（ReadOnly = True）\n    End Sub\nEnd Class\n",
      hints: ["AutoGenerateColumns = False 之后,如果你没手动加列、又没绑数据,表格会空。但这里我们仍绑 products,框架会按属性建列,只是不再“自动”——本练习重点是绑定后改列的属性。", "绑定完成后,用列名(就是属性名)索引:grid.Columns(\"Price\").HeaderText = \"单价\"。", "货币格式靠单元格样式:grid.Columns(\"Price\").DefaultCellStyle.Format = \"C2\",C 代表 Currency,2 是小数位。"],
      tests: [
      test("vb-grid-binding-4-no-autogen", "关闭自动建列", matches(new RegExp("AutoGenerateColumns\\s*=\\s*False", "i")), "设 grid.AutoGenerateColumns = False。", 25),
      test("vb-grid-binding-4-header", "改 Price 列标题为中文", matches(new RegExp("Columns\\s*\\(\\s*\"Price\"\\s*\\)\\s*\\.HeaderText\\s*=", "i")), "写 grid.Columns(\"Price\").HeaderText = \"单价\"。", 25),
      test("vb-grid-binding-4-format", "把 Price 列格式化成货币", matches(new RegExp("Columns\\s*\\(\\s*\"Price\"\\s*\\)\\s*\\.DefaultCellStyle\\.Format\\s*=\\s*\"C2\"", "i")), "写 grid.Columns(\"Price\").DefaultCellStyle.Format = \"C2\"。", 25),
      test("vb-grid-binding-4-readonly", "把 Id 列设为只读", matches(new RegExp("Columns\\s*\\(\\s*\"Id\"\\s*\\)\\s*\\.ReadOnly\\s*=\\s*True", "i")), "写 grid.Columns(\"Id\").ReadOnly = True。", 25),
      ],
      sections: [
      sec("自动建列方便,但属性名当列名太丑", "自动生成列省事,可它直接拿属性名做列标题——用户看到的是 Price、Id 这种英文,价格也只是裸数字。真实软件要中文标题、货币格式、某些列禁止编辑。做法是绑定之后,通过 grid.Columns(\"属性名\") 拿到对应列对象,逐项调它的属性。", { code: "grid.AutoGenerateColumns = False\ngrid.DataSource = products\ngrid.Columns(\"Price\").HeaderText = \"单价\"", caption: "绑定后按属性名取列、改标题", tip: "列名默认等于属性名,所以 grid.Columns(\"Price\") 能直接命中——这也是第 1 课模型属性命名要规范的原因。" }),
      sec("货币格式与只读列", "DefaultCellStyle.Format 用的是 .NET 标准数字格式串:\"C2\" 表示按当前区域的货币符号、保留 2 位小数显示(如 ¥299.00)。再把 Id 列 ReadOnly = True,主键就不会被用户误改。这两步把一张“能看”的表变成一张“专业”的表。", { code: "grid.Columns(\"Price\").DefaultCellStyle.Format = \"C2\"\ngrid.Columns(\"Id\").ReadOnly = True", caption: "Price 显示货币、Id 锁定只读", warning: "Format 只改显示,不改底层 Decimal 值——别误以为格式化会把数据本身四舍五入了。" }),
      ],
    }),
    lesson({
      title: "处理选择行:读出选中的业务对象",
      badge: "选择",
      icon: "VB",
      objective: "处理 SelectionChanged 事件,通过 CurrentRow.DataBoundItem 取出当前选中的 Product,把它的名称显示到一个详情标签——完成“列表选行 → 看详情”的主从联动。",
      starterCode: "Public Class MainForm\n    Inherits System.Windows.Forms.Form\n\n    Private grid As New DataGridView()\n    Private products As New BindingList(Of Product)()\n    Private detailLabel As New Label()\n\n    ' 选中行变化时触发（已在别处用 AddHandler grid.SelectionChanged, AddressOf OnSelectionChanged 挂好）\n    Private Sub OnSelectionChanged(sender As Object, e As EventArgs)\n        ' TODO: 没有选中行时（CurrentRow Is Nothing）直接 Return，避免空引用\n\n        ' TODO: 用 grid.CurrentRow.DataBoundItem 拿到选中的对象，\n        '       用 TryCast 转成 Product，赋给 selected\n        Dim selected As Product = Nothing\n\n        ' TODO: 若 selected 不为 Nothing，把它的 Name 显示到 detailLabel.Text\n    End Sub\nEnd Class\n",
      hints: ["grid.CurrentRow 是当前选中的那一行;它可能是 Nothing(还没选),先判一下再用,否则空引用崩溃。", "关键是 DataBoundItem:它直接给你这一行背后的业务对象,不用再去一格格读单元格文本。", "DataBoundItem 类型是 Object,用 TryCast(..., Product) 安全转型;转失败返回 Nothing,不会抛异常。"],
      tests: [
      test("vb-grid-binding-5-guard", "选中行为空时先返回", matches(new RegExp("grid\\.CurrentRow\\s+Is\\s+Nothing", "i")), "开头加 If grid.CurrentRow Is Nothing Then Return,避免空引用。", 34),
      test("vb-grid-binding-5-databounditem", "用 DataBoundItem + TryCast 取出对象", matches(new RegExp("TryCast\\s*\\(\\s*grid\\.CurrentRow\\.DataBoundItem\\s*,\\s*Product\\s*\\)", "i")), "写 Dim selected As Product = TryCast(grid.CurrentRow.DataBoundItem, Product)。", 33),
      test("vb-grid-binding-5-show", "把选中对象的 Name 显示到标签", matches(new RegExp("detailLabel\\.Text\\s*=\\s*selected\\.Name", "i")), "写 detailLabel.Text = selected.Name。", 33),
      ],
      sections: [
      sec("选中行,要的是对象不是文本", "用户在表格里点一行,你通常要拿到这一行代表的业务对象,去做编辑、删除、看详情。新手会去读 CurrentRow.Cells(1).Value 这种单元格文本——又脆又丢类型。绑定过的表格有更直接的出口:CurrentRow.DataBoundItem,它就是当初放进 DataSource 的那个 Product 实例本身。", { code: "Private Sub OnSelectionChanged(sender As Object, e As EventArgs)\n    If grid.CurrentRow Is Nothing Then Return\n    Dim selected As Product = TryCast(grid.CurrentRow.DataBoundItem, Product)\n    If selected IsNot Nothing Then\n        detailLabel.Text = selected.Name\n    End If\nEnd Sub", caption: "从选中行直接拿回 Product 对象", tip: "拿到 selected 后,Id、Price 全都在手——下一步删除、打开编辑窗,都从这个对象开始。" }),
      sec("两个必须的防御:空行与转型", "SelectionChanged 在表格刚加载、清空、或用户取消选择时也会触发,这时 CurrentRow 是 Nothing,不防就直接崩。先 If grid.CurrentRow Is Nothing Then Return。再者 DataBoundItem 是 Object,用 TryCast 转 Product:转得动就拿到对象,转不动返回 Nothing,绝不抛异常。这两道防御让事件处理稳如老狗。", { code: "If grid.CurrentRow Is Nothing Then Return\nDim selected As Product = TryCast(grid.CurrentRow.DataBoundItem, Product)\nIf selected IsNot Nothing Then detailLabel.Text = selected.Name", caption: "空行返回 + 安全转型,组成完整的主从联动", warning: "别用 DirectCast 代替 TryCast——DirectCast 在类型不符时直接抛 InvalidCastException,事件里炸了很难查。" }),
      ],
    }),
  ],
  "vb-form-validation": [
    lesson({
      title: "必填校验：拦住空输入",
      badge: "必填",
      icon: "VB",
      objective: "在 ValidateName 里用 String.IsNullOrWhiteSpace 判断姓名文本框是否为空，空就返回 False。",
      starterCode: "Public Class FormRegister\n\n    ' txtName 是窗体上的姓名文本框\n    Private Function ValidateName() As Boolean\n        ' TODO: 取出 txtName.Text，去掉首尾空白后判断是否为空\n        ' 用 String.IsNullOrWhiteSpace(...) 判定，空就 Return False\n\n        ' TODO: 不为空时 Return True\n        Return True\n    End Function\n\nEnd Class\n",
      hints: ["用户常常只敲几个空格就以为填了，String.IsNullOrWhiteSpace 会把纯空格也算成空，正合适。", "别用 txtName.Text = \"\" 判断——那放不过空格和 Tab，IsNullOrWhiteSpace 一句话全覆盖。", "校验函数返回 Boolean：False 表示不通过，True 表示这一项 OK，方便上层串联多个校验。"],
      tests: [
      test("vb-form-validation-1-isnullorwhitespace", "用 String.IsNullOrWhiteSpace 判空", matches(new RegExp("IsNullOrWhiteSpace\\s*\\(\\s*txtName\\.Text\\s*\\)", "i")), "写 String.IsNullOrWhiteSpace(txtName.Text) 来判断是否为空白。"),
      test("vb-form-validation-1-return-false", "判空成立时返回 False", matches(new RegExp("Return\\s+False", "i")), "在空输入的分支里写 Return False。"),
      ],
      sections: [
      sec("校验从「这一项填了没」开始", "输入校验里最基础、也最容易漏的一步就是必填检查。用户随手提交一个空表单，如果你不拦,后面的数据库或计算就会拿到一堆空值。WinForms 里取文本框内容是 txtName.Text，它永远是个字符串,从不为 Null,但可能是空串或全是空格。", { code: "Dim name As String = txtName.Text\nIf String.IsNullOrWhiteSpace(name) Then\n    Return False\nEnd If", caption: "一句 IsNullOrWhiteSpace 同时挡住空串和纯空格", tip: "IsNullOrWhiteSpace 和 IsNullOrEmpty 的区别:前者把 \"   \" 也当空,后者不会。表单校验几乎总是要前者。" }),
      sec("用 Boolean 返回值表达「通过 / 不通过」", "把每一项校验写成一个返回 Boolean 的小函数,是专业表单的标准做法:False 代表这一项有问题,True 代表 OK。这样上层只要把几个校验函数 And 起来,就能知道整张表能不能提交,逻辑清清楚楚,也方便单独测试每一项。", { code: "Private Function ValidateName() As Boolean\n    If String.IsNullOrWhiteSpace(txtName.Text) Then\n        Return False\n    End If\n    Return True\nEnd Function", caption: "校验函数的标准骨架:不通过早退,通过到底", warning: "别在这一步就弹 MsgBox 报错。先把「判断」和「提示」分开,下一课再加错误提示——一个函数只干一件事。" }),
      ],
    }),
    lesson({
      title: "数字校验:把文本安全转成数值",
      badge: "数字",
      icon: "VB",
      objective: "在 ValidateAge 里用 Integer.TryParse 把年龄文本框转成整数,转换失败就返回 False。",
      starterCode: "Public Class FormRegister\n\n    ' txtAge 是窗体上的年龄文本框\n    Private Function ValidateAge() As Boolean\n        Dim age As Integer\n\n        ' TODO: 用 Integer.TryParse(txtAge.Text, age) 尝试转换\n        ' 转换失败(返回 False)时,这一项也 Return False\n\n        ' TODO: 转换成功后,要求 age 在 1 到 120 之间,否则 Return False\n\n        Return True\n    End Function\n\nEnd Class\n",
      hints: ["别用 CInt(txtAge.Text)——用户敲个 \"abc\" 它会直接抛异常崩溃。TryParse 不抛异常,用返回值告诉你成没成。", "TryParse 的第二个参数是 ByRef 的:转换成功时结果会被写进 age,失败时 age 是 0。", "转得动还不够,还要在合理范围内。年龄 -5 或 9999 语法上能解析,业务上是非法的。"],
      tests: [
      test("vb-form-validation-2-tryparse", "用 Integer.TryParse 转换文本", matches(new RegExp("Integer\\.TryParse\\s*\\(\\s*txtAge\\.Text\\s*,\\s*age\\s*\\)", "i")), "写 Integer.TryParse(txtAge.Text, age) 来安全转换。"),
      test("vb-form-validation-2-guard", "失败时拦住并做范围校验", matchesAll([new RegExp("Return\\s+False"), new RegExp("age\\s*(<|>|<=|>=)")]), "转换失败要 Return False,还要判断 age 是否落在 1 到 120 之间。"),
      ],
      sections: [
      sec("永远别相信文本框里是个数字", "文本框给你的永远是字符串。要当数字用,就得转换。新手最爱 CInt / CDbl,但它们遇到 \"\"、\"abc\"、\"12.5x\" 这种脏输入会直接抛 InvalidCastException,程序当场崩。专业做法是 TryParse:它返回一个 Boolean 告诉你成不成,绝不抛异常。", { code: "Dim age As Integer\nIf Not Integer.TryParse(txtAge.Text, age) Then\n    Return False   ' 不是合法整数,拦下\nEnd If", caption: "TryParse 失败返回 False,age 留作 0,程序不崩", tip: "小数金额用 Decimal.TryParse,带千分位或货币符号用带 NumberStyles 的重载——下一课会用到。" }),
      sec("解析成功 ≠ 业务合法,再加范围", "TryParse 只保证「这是个整数」,不保证「这是个合理的年龄」。-5、0、9999 都能解析,但显然不是人的年龄。所以转换成功后还要补一道范围检查:把上下界写清楚,落在外面同样 Return False。把「类型对不对」和「值合不合理」分成两层判断,是校验逻辑清晰的关键。", { code: "If age < 1 OrElse age > 120 Then\n    Return False\nEnd If\nReturn True", caption: "类型过了,再卡业务范围", warning: "用 OrElse 而不是 Or:OrElse 短路,左边成立就不算右边,效率高也更安全。表单校验里几乎总用 OrElse / AndAlso。" }),
      ],
    }),
    lesson({
      title: "金额校验 + ErrorProvider 提示",
      badge: "金额",
      icon: "VB",
      objective: "在 ValidateAmount 里用 Decimal.TryParse 校验金额必须大于 0,并用 errProvider.SetError 在出错的文本框旁显示提示。",
      starterCode: "Public Class FormRegister\n\n    ' txtAmount 是金额文本框; errProvider 是窗体上的 ErrorProvider 控件\n    Private Function ValidateAmount() As Boolean\n        Dim amount As Decimal\n\n        ' TODO: 用 Decimal.TryParse(txtAmount.Text, amount) 转换金额\n        ' 失败,或金额 <= 0 时:\n        '   用 errProvider.SetError(txtAmount, \"请输入大于 0 的金额\") 显示错误\n        '   然后 Return False\n\n        ' TODO: 校验通过时,用 errProvider.SetError(txtAmount, \"\") 清掉旧错误\n        Return True\n    End Function\n\nEnd Class\n",
      hints: ["金额用 Decimal,别用 Double——Double 算钱会出 0.1+0.2=0.30000004 这种精度坑,Decimal 不会。", "ErrorProvider.SetError(控件, \"信息\") 会在那个控件右边冒出一个红色感叹号,鼠标悬停显示信息,比弹窗友好得多。", "通过时一定要 SetError(控件, \"\") 把图标清掉,否则用户改对了红点还赖着不走。"],
      tests: [
      test("vb-form-validation-3-decimal-tryparse", "用 Decimal.TryParse 校验金额", matches(new RegExp("Decimal\\.TryParse\\s*\\(\\s*txtAmount\\.Text\\s*,\\s*amount\\s*\\)", "i")), "写 Decimal.TryParse(txtAmount.Text, amount) 来转换金额。", 34),
      test("vb-form-validation-3-positive", "要求金额大于 0", matches(new RegExp("amount\\s*<=?\\s*0", "i")), "判断 amount <= 0 时拦下,Return False。", 33),
      test("vb-form-validation-3-seterror", "用 ErrorProvider.SetError 提示", matches(new RegExp("errProvider\\.SetError\\s*\\(\\s*txtAmount\\s*,", "i")), "出错时 errProvider.SetError(txtAmount, \"...\"),通过时 SetError(txtAmount, \"\") 清空。", 33),
      ],
      sections: [
      sec("钱用 Decimal,不用 Double", "金额校验和普通数字校验思路一样:先 TryParse,再判范围。区别在类型——钱必须用 Decimal。Double 是二进制浮点,表示 0.1 这类十进制小数会有微小误差,累加起来钱就对不上账。Decimal 是十进制定点,专为货币和财务设计,该是 19.99 就是 19.99。", { code: "Dim amount As Decimal\nIf Not Decimal.TryParse(txtAmount.Text, amount) OrElse amount <= 0 Then\n    errProvider.SetError(txtAmount, \"请输入大于 0 的金额\")\n    Return False\nEnd If", caption: "一个条件同时挡住「不是数」和「不大于 0」", tip: "金额常要求最多两位小数。可以再加 Decimal.Round(amount, 2) = amount 这类判断,或解析后限定格式——按业务需要补。" }),
      sec("ErrorProvider:把错误贴在出错的地方", "前两课只返回 True/False,用户并不知道哪错了。WinForms 的 ErrorProvider 控件专门解决这个:SetError(控件, \"信息\") 会在那个控件旁边显示一个红色感叹号图标,悬停就看到提示。比起打断流程的 MsgBox,它让用户一眼看清是哪一格错了、错在哪。关键是——校验通过时记得用空字符串把图标清掉。", { code: "' 通过时清掉这个控件上的错误图标\nerrProvider.SetError(txtAmount, \"\")\nReturn True", caption: "通过即清错,红点不残留", warning: "别图省事每个字段都弹 MsgBox——连弹好几个对话框最烦用户。ErrorProvider 能同时标出多个错误字段,体验天差地别。" }),
      ],
    }),
    lesson({
      title: "整合:提交前一次性校验全表单",
      badge: "提交",
      icon: "VB",
      objective: "在 btnSubmit_Click 里调用前面三个校验函数,全部通过(用 AndAlso 串联)才允许提交,否则 MsgBox 提示并提前返回。",
      starterCode: "Public Class FormRegister\n\n    ' 假设 ValidateName / ValidateAge / ValidateAmount 已在前几课写好\n\n    Private Sub btnSubmit_Click(sender As Object, e As EventArgs) Handles btnSubmit.Click\n        ' TODO: 用 AndAlso 把三个校验串起来,得到 allValid\n        Dim allValid As Boolean\n\n        ' TODO: 任一不通过(Not allValid)时,MsgBox 提示后 Return,不要继续提交\n\n        ' 全部通过才走到这里\n        MsgBox(\"提交成功\")\n    End Sub\n\nEnd Class\n",
      hints: ["把三个 Boolean 函数用 AndAlso 串起来:ValidateName() AndAlso ValidateAge() AndAlso ValidateAmount()。", "AndAlso 会短路:前一个 False 后面就不算了。想让每个字段都标红,改用 And(全部都跑);想快点退出,用 AndAlso。", "不通过时先 MsgBox 再 Return,让事件处理函数提前结束,绝不能继续往下走真正的提交逻辑。"],
      tests: [
      test("vb-form-validation-4-andalso", "用 AndAlso 串联三个校验", matchesAll([new RegExp("ValidateName\\s*\\(\\s*\\)\\s+AndAlso"), new RegExp("AndAlso\\s+ValidateAmount\\s*\\(\\s*\\)")]), "写 ValidateName() AndAlso ValidateAge() AndAlso ValidateAmount()。", 34),
      test("vb-form-validation-4-early-return", "不通过时提示并提前返回", matchesAll([new RegExp("If\\s+Not\\s+allValid"), new RegExp("MsgBox"), new RegExp("Return")]), "Not allValid 时先 MsgBox(...) 再 Return,阻止继续提交。", 33),
      test("vb-form-validation-4-collect", "把结果汇总到 allValid", matches(new RegExp("allValid\\s*=\\s*ValidateName", "i")), "把 AndAlso 串联的结果赋给 allValid:allValid = ...。", 33),
      ],
      sections: [
      sec("提交按钮是校验的最后一道闸", "前三课各写了一个独立的校验函数。现在到收口的地方:用户点「提交」,btnSubmit_Click 触发,我们在这里把三道校验串起来,只有全过才放行。这是表单校验的标准入口——不管前面字段级提示做得多漂亮,提交瞬间必须再统一把关一次,绝不让非法数据溜进真正的处理逻辑。", { code: "Dim allValid As Boolean\nallValid = ValidateName() AndAlso ValidateAge() AndAlso ValidateAmount()", caption: "一行把三道校验汇成一个结论", tip: "也可以接 e.Cancel / Validating 事件做逐控件校验。但「提交时统一校验」更直观,也最适合新表单起步。" }),
      sec("AndAlso 短路 vs And 全跑", "AndAlso 是短路与:左边 False 就直接得 False,右边的函数根本不执行。想尽快退出、不在意标红几个字段时用它最省。但如果你希望「所有错的字段都同时标上红点」,反而该用 And(每个校验都执行一遍,把所有 ErrorProvider 都设上)。两种各有用武之地,按你想要的体验选。", { code: "If Not allValid Then\n    MsgBox(\"请修正标红的字段后再提交\")\n    Return            ' 提前退出,不执行下面的提交\nEnd If\n\n' 走到这里说明全部合法\nMsgBox(\"提交成功\")", caption: "不合法就提前 Return,合法才往下走", warning: "千万别漏了 Return。少了它,即使校验不过,代码照样往下跑到提交逻辑——这是表单最常见的低级 bug。" }),
      sec("四课串成一条校验链", "你已经搭好一条完整的输入校验链:必填(IsNullOrWhiteSpace)→ 数字(Integer.TryParse + 范围)→ 金额(Decimal.TryParse + ErrorProvider 提示)→ 提交时用 AndAlso 统一把关。这套「每项一个 Boolean 函数、提交处汇总、错误就地提示」的结构,正是专业 WinForms 表单的通用骨架,换任何字段都照搬得上。", { tip: "下一步可以把校验结果连同提示信息一起返回(比如返回一个 (Boolean, String) 或自定义结果类型),让提示文案也从校验函数里来,而不是写死在按钮里。" }),
      ],
    }),
  ],
  "vb-capstone-crm": [
    lesson({
      title: "定义客户数据结构",
      badge: "建模",
      icon: "VB",
      objective: "用一个 Class 定义 Customer，包含 Id、Name、Phone、Email 四个属性，并提供一个返回 \"姓名 <电话>\" 的 ToString 重写。",
      starterCode: "' 客户管理小工具：第 1 关 —— 客户模型\n' 整个项目都围绕一条客户记录展开，先把这条记录建好。\n\nPublic Class Customer\n    ' TODO: 定义四个属性 Id (Integer)、Name (String)、Phone (String)、Email (String)\n\n    ' TODO: 重写 ToString，返回形如  张三 <13800000000>  的字符串\n\nEnd Class\n",
      hints: ["自动属性写法：Public Property Name As String，VB 会替你生成后台字段。", "重写要写 Public Overrides Function ToString() As String，里面用 & 拼字符串。", "拼接尖括号用字符串字面量：Name & \" <\" & Phone & \">\"。"],
      tests: [
      test("vb-capstone-crm-1-class", "定义 Customer 类", matches(new RegExp("Public\\s+Class\\s+Customer", "i")), "用 Public Class Customer 开头、End Class 结尾。"),
      test("vb-capstone-crm-1-props", "四个属性齐全", matchesAll([new RegExp("Property\\s+Id\\s+As\\s+Integer"), new RegExp("Property\\s+Name\\s+As\\s+String"), new RegExp("Property\\s+Phone\\s+As\\s+String"), new RegExp("Property\\s+Email\\s+As\\s+String")]), "Id、Name、Phone、Email 四个 Property 都要声明。"),
      test("vb-capstone-crm-1-tostring", "重写 ToString", matchesAll([new RegExp("Overrides\\s+Function\\s+ToString"), new RegExp("Name\\s*&"), new RegExp("&\\s*Phone")]), "需要 Overrides Function ToString，并在里面拼出 Name 和 Phone。"),
      ],
      sections: [
      sec("一条记录就是一个类", "客户管理工具的核心是「一条客户记录」。在 VB 里把它建成一个 Class 最自然：每个字段是一个属性，一个 Customer 实例就是表里的一行。先把模型定准，后面的录入、查询、编辑全靠它。", { code: "Public Class Customer\n    Public Property Id As Integer\n    Public Property Name As String\n    Public Property Phone As String\n    Public Property Email As String\nEnd Class", caption: "四个自动属性即可", tip: "VB 的自动属性不用手写后台字段，写 Property 就够了。" }),
      sec("ToString 决定它怎么显示", "把对象丢进 ListBox 或 Console.WriteLine 时,默认会调用 ToString。重写它，让一个客户一眼能读出来——这一步在第 5 关把客户塞进列表时会立刻回报你。", { code: "Public Overrides Function ToString() As String\n    Return Name & \" <\" & Phone & \">\"\nEnd Function", caption: "可读的字符串表示", warning: "别忘了 Overrides 关键字，漏了它编译器会报「没有可重写的成员」。" }),
      ],
    }),
    lesson({
      title: "用集合管理多个客户",
      badge: "仓库",
      icon: "VB",
      objective: "建一个 CustomerStore 类，内部用 List(Of Customer) 存数据，提供 Add 方法把客户加进去并通过 Count 暴露数量。",
      starterCode: "Public Class Customer\n    Public Property Id As Integer\n    Public Property Name As String\n    Public Property Phone As String\n    Public Property Email As String\nEnd Class\n\n' 客户管理小工具：第 2 关 —— 客户仓库\n' 一个客户没意义，要能装一堆。\n\nPublic Class CustomerStore\n    ' TODO: 用 List(Of Customer) 声明一个私有字段 _items\n\n    ' TODO: Add(c As Customer) 把客户加入 _items\n\n    ' TODO: 只读属性 Count 返回 _items 里的数量\n\nEnd Class\n",
      hints: ["声明时直接初始化：Private _items As New List(Of Customer)，省得后面忘了 New。", "List 的方法就叫 .Add，直接 _items.Add(c)。", "只读属性用 ReadOnly Property Count As Integer，Get 里返回 _items.Count。"],
      tests: [
      test("vb-capstone-crm-2-list", "用 List(Of Customer) 做后端存储", matches(new RegExp("List\\s*\\(\\s*Of\\s+Customer\\s*\\)", "i")), "声明一个 List(Of Customer) 字段并 New 出来。"),
      test("vb-capstone-crm-2-add", "Add 方法调用 List.Add", matchesAll([new RegExp("Sub\\s+Add\\s*\\(\\s*\\w+\\s+As\\s+Customer"), new RegExp("\\.Add\\s*\\(")]), "Sub Add(c As Customer) 里要调用 _items.Add(c)。"),
      test("vb-capstone-crm-2-count", "只读 Count 属性", matchesAll([new RegExp("ReadOnly\\s+Property\\s+Count"), new RegExp("\\.Count")]), "用 ReadOnly Property Count，返回列表的 .Count。"),
      ],
      sections: [
      sec("为什么不用数组", "客户数量随时在变,数组得提前定长、扩容麻烦。List(Of Customer) 是泛型动态列表:类型安全、能随意 Add/Remove、自带 Count。这是 .NET 里管理同类对象的默认选择。", { code: "Private _items As New List(Of Customer)\n\nPublic Sub Add(c As Customer)\n    _items.Add(c)\nEnd Sub", caption: "私有列表 + 公开 Add", tip: "字段用下划线前缀 _items 是 VB 社区约定，一眼区分字段和参数。" }),
      sec("封装:外面别直接碰列表", "把 _items 设成 Private,外部只能通过 Add 和 Count 跟它打交道。这样将来你想改成数据库、加去重逻辑,外部代码一行都不用改。封装的价值在第 4、6 关会非常明显。", { code: "Public ReadOnly Property Count As Integer\n    Get\n        Return _items.Count\n    End Get\nEnd Property", caption: "只读暴露数量", warning: "别把 _items 直接设成 Public,那样别人能绕过你的规则乱改数据。" }),
      ],
    }),
    lesson({
      title: "校验录入：拒绝空名和坏电话",
      badge: "校验",
      icon: "VB",
      objective: "在 CustomerStore 里写 Validate(c) 函数：姓名为空或电话不是 11 位数字就返回错误信息字符串，合法则返回空字符串。",
      starterCode: "Public Class Customer\n    Public Property Id As Integer\n    Public Property Name As String\n    Public Property Phone As String\n    Public Property Email As String\nEnd Class\n\nPublic Class CustomerStore\n    Private _items As New List(Of Customer)\n\n    ' 客户管理小工具：第 3 关 —— 录入校验\n    ' 脏数据进了仓库，后面查询导出全乱套。挡在门口。\n    Public Function Validate(c As Customer) As String\n        ' TODO: 姓名 Trim 后为空，返回 \"姓名不能为空\"\n\n        ' TODO: 电话不是 11 位纯数字，返回 \"电话必须是11位数字\"\n\n        ' TODO: 都合法，返回 \"\"\n        Return \"\"\n    End Function\nEnd Class\n",
      hints: ["判空用 String.IsNullOrWhiteSpace(c.Name)，比手写 = \"\" 更稳。", "11 位数字可以 c.Phone.Length = 11 加上每个字符 IsDigit，或用正则 ^\\d{11}$。", "返回空字符串 \"\" 代表「无错误」,调用方靠它判断是否放行。"],
      tests: [
      test("vb-capstone-crm-3-func", "Validate 返回 String", matches(new RegExp("Function\\s+Validate\\s*\\(\\s*\\w+\\s+As\\s+Customer\\s*\\)\\s+As\\s+String", "i")), "签名应为 Function Validate(c As Customer) As String。"),
      test("vb-capstone-crm-3-name", "检查姓名为空", matches(new RegExp("IsNullOrWhiteSpace\\s*\\(\\s*\\w+\\.Name", "i")), "用 IsNullOrWhiteSpace 判断姓名是否为空。"),
      test("vb-capstone-crm-3-phone", "检查电话 11 位", matches(new RegExp("(Length\\s*(=|<>)\\s*11|\\\\d\\{11\\})", "i")), "判断电话长度为 11，或用正则限制 11 位数字。"),
      test("vb-capstone-crm-3-msg", "返回中文错误信息", matchesAll([new RegExp("姓名"), new RegExp("电话")]), "把不合法的原因作为字符串返回,便于界面提示。"),
      ],
      sections: [
      sec("校验要挡在入口", "录入是整个系统唯一能挡脏数据的关口。空姓名、瞎填的电话一旦进库,后面查询、导出全跟着出错。把校验集中在一个 Validate 函数里,返回一句人话错误信息——空串代表通过。", { code: "If String.IsNullOrWhiteSpace(c.Name) Then\n    Return \"姓名不能为空\"\nEnd If", caption: "姓名非空检查", tip: "IsNullOrWhiteSpace 同时挡住 Nothing、空串和一堆空格,比 = \"\" 周全。" }),
      sec("电话:长度 + 全数字", "中国手机号是 11 位纯数字。两步判断最直观:先看长度,再确认每一位都是数字。也可以用正则 ^\\d{11}$ 一行搞定,看你喜欢哪种风格。", { code: "Dim allDigit = c.Phone IsNot Nothing AndAlso _\n    c.Phone.All(Function(ch) Char.IsDigit(ch))\nIf c.Phone Is Nothing OrElse c.Phone.Length <> 11 OrElse Not allDigit Then\n    Return \"电话必须是11位数字\"\nEnd If", caption: "长度与数字双重校验", warning: "先判 Nothing 再取 .Length,否则空电话会直接抛 NullReferenceException。" }),
      ],
    }),
    lesson({
      title: "自增 ID 与安全录入",
      badge: "录入",
      icon: "VB",
      objective: "给 CustomerStore 加 TryAdd(c) 方法：先调用 Validate，有错就返回错误信息；合法则分配自增 Id 后加入列表并返回空字符串。",
      starterCode: "Public Class Customer\n    Public Property Id As Integer\n    Public Property Name As String\n    Public Property Phone As String\n    Public Property Email As String\nEnd Class\n\nPublic Class CustomerStore\n    Private _items As New List(Of Customer)\n    Private _nextId As Integer = 1\n\n    Public Function Validate(c As Customer) As String\n        If String.IsNullOrWhiteSpace(c.Name) Then Return \"姓名不能为空\"\n        If c.Phone Is Nothing OrElse c.Phone.Length <> 11 Then Return \"电话必须是11位数字\"\n        Return \"\"\n    End Function\n\n    ' 客户管理小工具：第 4 关 —— 带校验和自增 ID 的录入\n    Public Function TryAdd(c As Customer) As String\n        ' TODO: 调用 Validate(c)，错误信息非空就直接 Return 它\n\n        ' TODO: 把 _nextId 赋给 c.Id，然后 _nextId 加 1\n\n        ' TODO: 加入 _items，返回 \"\"\n        Return \"\"\n    End Function\nEnd Class\n",
      hints: ["先把校验结果存进变量：Dim err = Validate(c)，再判断 err <> \"\"。", "分配 ID 用 c.Id = _nextId,紧接着 _nextId += 1,顺序别反。", "全部通过后 _items.Add(c) 再 Return \"\",空串告诉调用方成功了。"],
      tests: [
      test("vb-capstone-crm-4-tryadd", "定义 TryAdd 函数", matches(new RegExp("Function\\s+TryAdd\\s*\\(\\s*\\w+\\s+As\\s+Customer\\s*\\)\\s+As\\s+String", "i")), "签名 Function TryAdd(c As Customer) As String。"),
      test("vb-capstone-crm-4-callvalidate", "录入前先校验", matches(new RegExp("Validate\\s*\\(\\s*\\w+\\s*\\)", "i")), "在 TryAdd 里调用 Validate(c)，错误时提前返回。"),
      test("vb-capstone-crm-4-autoid", "分配自增 ID", matchesAll([new RegExp("\\.Id\\s*=\\s*_nextId"), new RegExp("_nextId\\s*(\\+=\\s*1|=\\s*_nextId\\s*\\+\\s*1)")]), "把 _nextId 赋给 c.Id 并让 _nextId 递增。"),
      test("vb-capstone-crm-4-add", "合法后加入列表", matches(new RegExp("_items\\.Add\\s*\\(\\s*\\w+\\s*\\)", "i")), "校验通过后调用 _items.Add(c)。"),
      ],
      sections: [
      sec("TryXxx 模式:不抛异常,返回结果", ".NET 里 TryParse、TryGetValue 都遵循一个套路:能失败的操作不抛异常,而是把成败传回去。我们让 TryAdd 返回错误信息字符串:空串=成功,非空=失败原因。界面拿到非空串直接弹给用户看。", { code: "Dim err = Validate(c)\nIf err <> \"\" Then Return err", caption: "校验失败,提前退出", tip: "把校验结果存进变量再判断,别调用两次 Validate 浪费。" }),
      sec("ID 由仓库统一发放", "ID 绝不能让用户填——会重、会乱。让仓库持有 _nextId,每录入一条就发一个、再加一。这样每条客户都有唯一编号,第 6、7 关的编辑和删除靠它精确定位。", { code: "c.Id = _nextId\n_nextId += 1\n_items.Add(c)\nReturn \"\"", caption: "先发号,再入库", warning: "顺序别写反:先递增再赋值会让第一条客户的 Id 从 2 开始。" }),
      ],
    }),
    lesson({
      title: "按关键字查询客户",
      badge: "查询",
      icon: "VB",
      objective: "给 CustomerStore 加 Search(keyword) 方法，返回姓名或电话包含关键字（忽略大小写）的 List(Of Customer)；关键字为空则返回全部。",
      starterCode: "Public Class Customer\n    Public Property Id As Integer\n    Public Property Name As String\n    Public Property Phone As String\n    Public Property Email As String\n    Public Overrides Function ToString() As String\n        Return Name & \" <\" & Phone & \">\"\n    End Function\nEnd Class\n\nPublic Class CustomerStore\n    Private _items As New List(Of Customer)\n\n    Public Sub Add(c As Customer)\n        _items.Add(c)\n    End Sub\n\n    ' 客户管理小工具：第 5 关 —— 关键字查询\n    ' 客户多了，得能按姓名或电话片段找出来。\n    Public Function Search(keyword As String) As List(Of Customer)\n        ' TODO: keyword Trim 后为空，返回全部（_items 的副本）\n\n        ' TODO: 否则用 Where 过滤：Name 或 Phone 包含 keyword（忽略大小写）\n        Return New List(Of Customer)()\n    End Function\nEnd Class\n",
      hints: ["忽略大小写最稳的写法：x.Name.IndexOf(keyword, StringComparison.OrdinalIgnoreCase) >= 0。", "过滤用 LINQ：_items.Where(Function(x) ...).ToList()。", "空关键字别返回 _items 本身，用 _items.ToList() 返回副本,避免外面改到原表。"],
      tests: [
      test("vb-capstone-crm-5-sig", "Search 返回 List(Of Customer)", matches(new RegExp("Function\\s+Search\\s*\\(\\s*\\w+\\s+As\\s+String\\s*\\)\\s+As\\s+List\\s*\\(\\s*Of\\s+Customer", "i")), "签名 Function Search(keyword As String) As List(Of Customer)。"),
      test("vb-capstone-crm-5-where", "用 Where 做过滤", matchesAll([new RegExp("\\.Where\\s*\\(\\s*Function"), new RegExp("\\.ToList\\s*\\(")]), "用 _items.Where(Function(x) ...) 过滤再 .ToList()。"),
      test("vb-capstone-crm-5-ignorecase", "查询忽略大小写", matches(new RegExp("(IgnoreCase|ToLower|ToUpper)", "i")), "用 OrdinalIgnoreCase 或 ToLower 实现忽略大小写匹配。"),
      test("vb-capstone-crm-5-fields", "同时匹配姓名和电话", matchesAll([new RegExp("\\.Name"), new RegExp("\\.Phone")]), "过滤条件要覆盖 Name 和 Phone 两个字段。"),
      ],
      sections: [
      sec("LINQ 一行表达过滤意图", "不用手写 For 循环 + 临时列表。LINQ 的 Where 接一个返回布尔的函数,留下满足条件的元素,.ToList() 收尾。读起来就像在说「留下姓名或电话含关键字的」。", { code: "Return _items.Where(Function(x) _\n    x.Name.IndexOf(keyword, StringComparison.OrdinalIgnoreCase) >= 0 OrElse _\n    x.Phone.IndexOf(keyword, StringComparison.OrdinalIgnoreCase) >= 0 _\n).ToList()", caption: "姓名或电话命中即保留", tip: "用 OrElse 而非 Or:第一个条件成立就短路,省一次 IndexOf。" }),
      sec("空关键字 = 显示全部", "搜索框清空时,用户期望看到完整列表,而不是空白。先处理这个边界:keyword Trim 后为空就返回全部的副本。返回副本是为了让外面随便改返回值,也动不了仓库里的真数据。", { code: "If String.IsNullOrWhiteSpace(keyword) Then\n    Return _items.ToList()\nEnd If", caption: "空查询返回全部副本", warning: "别直接 Return _items;外面对它 Add/Remove 会污染你的仓库。" }),
      ],
    }),
    lesson({
      title: "按 ID 查找并编辑",
      badge: "编辑",
      icon: "VB",
      objective: "加 FindById(id) 返回匹配客户或 Nothing；再加 Update(c) 用 FindById 定位同 Id 的记录并覆盖姓名、电话、邮箱，找不到返回 False，成功返回 True。",
      starterCode: "Public Class Customer\n    Public Property Id As Integer\n    Public Property Name As String\n    Public Property Phone As String\n    Public Property Email As String\nEnd Class\n\nPublic Class CustomerStore\n    Private _items As New List(Of Customer)\n\n    Public Sub Add(c As Customer)\n        _items.Add(c)\n    End Sub\n\n    ' 客户管理小工具：第 6 关 —— 按 ID 定位并编辑\n    Public Function FindById(id As Integer) As Customer\n        ' TODO: 用 FirstOrDefault 找出 Id 相同的客户，找不到返回 Nothing\n        Return Nothing\n    End Function\n\n    Public Function Update(c As Customer) As Boolean\n        ' TODO: 用 FindById(c.Id) 找到目标，找不到返回 False\n\n        ' TODO: 把 c 的 Name/Phone/Email 覆盖到目标上，返回 True\n        Return False\n    End Function\nEnd Class\n",
      hints: ["FirstOrDefault 找不到会返回 Nothing,正好当「没找到」的信号。", "Update 里先 Dim target = FindById(c.Id)，再 If target Is Nothing Then Return False。", "覆盖字段时改的是 target 对象本身,因为它就是列表里那条记录的引用。"],
      tests: [
      test("vb-capstone-crm-6-findbyid", "FindById 用 FirstOrDefault", matchesAll([new RegExp("Function\\s+FindById\\s*\\(\\s*\\w+\\s+As\\s+Integer\\s*\\)\\s+As\\s+Customer"), new RegExp("FirstOrDefault\\s*\\(\\s*Function")]), "在 FindById 里用 _items.FirstOrDefault(Function(x) x.Id = id)。"),
      test("vb-capstone-crm-6-notfound", "找不到返回 False", matchesAll([new RegExp("Is\\s+Nothing"), new RegExp("Return\\s+False")]), "Update 里 target Is Nothing 时返回 False。"),
      test("vb-capstone-crm-6-overwrite", "覆盖姓名电话邮箱", matchesAll([new RegExp("\\.Name\\s*=\\s*\\w+\\.Name"), new RegExp("\\.Phone\\s*=\\s*\\w+\\.Phone"), new RegExp("\\.Email\\s*=\\s*\\w+\\.Email")]), "把传入 c 的三个字段赋给找到的目标对象。"),
      test("vb-capstone-crm-6-success", "成功返回 True", matches(new RegExp("Return\\s+True", "i")), "覆盖完成后 Return True。"),
      ],
      sections: [
      sec("FirstOrDefault:找到就给，找不到给 Nothing", "按 ID 定位是编辑和删除的共同前提。FirstOrDefault 接一个条件,返回第一个满足的元素;一个都没有就返回引用类型的默认值 Nothing。调用方靠 Is Nothing 判断有没有命中。", { code: "Public Function FindById(id As Integer) As Customer\n    Return _items.FirstOrDefault(Function(x) x.Id = id)\nEnd Function", caption: "按 Id 精确定位", tip: "复用 FindById,Update 和将来的 Delete 都不必再写一遍查找逻辑。" }),
      sec("引用即原件:改 target 就是改库里的记录", "FindById 返回的不是拷贝,而是列表里那条记录的引用。所以直接给 target.Name、target.Phone 赋新值,仓库里的数据当场就变了——不用再 Remove 旧的、Add 新的。这正是引用类型的威力。", { code: "Dim target = FindById(c.Id)\nIf target Is Nothing Then Return False\ntarget.Name = c.Name\ntarget.Phone = c.Phone\ntarget.Email = c.Email\nReturn True", caption: "就地覆盖三个字段", warning: "别把 target 整个换成 c(target = c),那只改了局部变量,列表纹丝不动。" }),
      ],
    }),
    lesson({
      title: "删除客户与导出 CSV",
      badge: "导出",
      icon: "VB",
      objective: "加 Remove(id) 用 RemoveAll 删除指定 Id 并返回是否删掉了；再加 ToCsv() 输出含表头、每行 Id,Name,Phone,Email 的 CSV 文本。",
      starterCode: "Public Class Customer\n    Public Property Id As Integer\n    Public Property Name As String\n    Public Property Phone As String\n    Public Property Email As String\nEnd Class\n\nPublic Class CustomerStore\n    Private _items As New List(Of Customer)\n\n    Public Sub Add(c As Customer)\n        _items.Add(c)\n    End Sub\n\n    ' 客户管理小工具：第 7 关 —— 删除与导出\n    Public Function Remove(id As Integer) As Boolean\n        ' TODO: 用 RemoveAll 删除 Id 相同的客户，返回是否真的删掉了\n        Return False\n    End Function\n\n    Public Function ToCsv() As String\n        ' TODO: 用 StringBuilder 拼出 CSV，第一行表头 Id,Name,Phone,Email\n\n        ' TODO: 遍历 _items，每行 Id,Name,Phone,Email\n        Return \"\"\n    End Function\nEnd Class\n",
      hints: ["RemoveAll 返回删掉的条数,> 0 就说明命中了。", "拼大量字符串用 StringBuilder + AppendLine，别用 & 在循环里硬接。", "表头先 AppendLine(\"Id,Name,Phone,Email\")，再 For Each 每条记录拼一行。"],
      tests: [
      test("vb-capstone-crm-7-removeall", "Remove 用 RemoveAll", matches(new RegExp("RemoveAll\\s*\\(\\s*Function\\s*\\(\\s*\\w+\\s*\\)[^)]*\\.Id\\s*=", "i")), "用 _items.RemoveAll(Function(x) x.Id = id)。"),
      test("vb-capstone-crm-7-removebool", "Remove 报告是否删除", matches(new RegExp("RemoveAll\\s*\\([^\\n]*\\)\\s*>\\s*0", "i")), "用 RemoveAll 的返回条数 > 0 判断是否删掉了。"),
      test("vb-capstone-crm-7-sb", "用 StringBuilder 拼 CSV", matchesAll([new RegExp("StringBuilder"), new RegExp("AppendLine")]), "ToCsv 里用 New StringBuilder 和 AppendLine。"),
      test("vb-capstone-crm-7-header", "CSV 含正确表头", matches(new RegExp("Id\\s*,\\s*Name\\s*,\\s*Phone\\s*,\\s*Email", "i")), "第一行必须是 Id,Name,Phone,Email。"),
      ],
      sections: [
      sec("RemoveAll:条件删除还报数", "删除不必先 FindById 再 Remove 对象。RemoveAll 接一个条件,删掉所有满足的元素,并返回删了几条。返回 0 就说明那个 Id 根本不存在——正好用来告诉界面「没找到要删的客户」。", { code: "Public Function Remove(id As Integer) As Boolean\n    Return _items.RemoveAll(Function(x) x.Id = id) > 0\nEnd Function", caption: "删除并返回是否命中", tip: "Id 唯一时最多删一条,但 RemoveAll 的写法天然防住了「重复 Id」的意外。" }),
      sec("导出:StringBuilder 拼 CSV", "导出是这个工具交付价值的出口。CSV 第一行是表头,之后每行四个字段逗号隔开。循环里拼字符串别用 &——每次都新建一个字符串很费;StringBuilder 在一块缓冲区上追加,数据多时差距明显。", { code: "Dim sb As New StringBuilder()\nsb.AppendLine(\"Id,Name,Phone,Email\")\nFor Each c In _items\n    sb.AppendLine($\"{c.Id},{c.Name},{c.Phone},{c.Email}\")\nNext\nReturn sb.ToString()", caption: "表头 + 逐行追加", warning: "真实场景里姓名含逗号要加引号转义;本关先掌握结构,转义留作进阶。" }),
      ],
    }),
    lesson({
      title: "整合：命令行 CRM 主程序",
      badge: "整合",
      icon: "VB",
      objective: "在 Module Program 的 Sub Main 里组装前面的能力：录入两条客户、按关键字查询、编辑其中一条、删除另一条，最后把 ToCsv 的结果用 Console.WriteLine 打印出来。",
      starterCode: "Imports System.Text\n\nPublic Class Customer\n    Public Property Id As Integer\n    Public Property Name As String\n    Public Property Phone As String\n    Public Property Email As String\nEnd Class\n\nPublic Class CustomerStore\n    Private _items As New List(Of Customer)\n    Private _nextId As Integer = 1\n\n    Public Function TryAdd(c As Customer) As String\n        If String.IsNullOrWhiteSpace(c.Name) Then Return \"姓名不能为空\"\n        If c.Phone Is Nothing OrElse c.Phone.Length <> 11 Then Return \"电话必须是11位数字\"\n        c.Id = _nextId\n        _nextId += 1\n        _items.Add(c)\n        Return \"\"\n    End Function\n\n    Public Function Search(keyword As String) As List(Of Customer)\n        If String.IsNullOrWhiteSpace(keyword) Then Return _items.ToList()\n        Return _items.Where(Function(x) _\n            x.Name.IndexOf(keyword, StringComparison.OrdinalIgnoreCase) >= 0 OrElse _\n            x.Phone.IndexOf(keyword, StringComparison.OrdinalIgnoreCase) >= 0).ToList()\n    End Function\n\n    Public Function Update(c As Customer) As Boolean\n        Dim target = _items.FirstOrDefault(Function(x) x.Id = c.Id)\n        If target Is Nothing Then Return False\n        target.Name = c.Name\n        target.Phone = c.Phone\n        target.Email = c.Email\n        Return True\n    End Function\n\n    Public Function Remove(id As Integer) As Boolean\n        Return _items.RemoveAll(Function(x) x.Id = id) > 0\n    End Function\n\n    Public Function ToCsv() As String\n        Dim sb As New StringBuilder()\n        sb.AppendLine(\"Id,Name,Phone,Email\")\n        For Each c In _items\n            sb.AppendLine($\"{c.Id},{c.Name},{c.Phone},{c.Email}\")\n        Next\n        Return sb.ToString()\n    End Function\nEnd Class\n\n' 客户管理小工具：第 8 关 —— 主程序整合\n' 把前 7 关的零件接成一条完整流程。\nModule Program\n    Sub Main()\n        Dim store As New CustomerStore()\n\n        ' TODO: 录入两条客户（张三 / 李四），各给 11 位电话\n\n        ' TODO: 用 store.Search 按关键字查询并遍历结果\n\n        ' TODO: 用 store.Update 编辑一条；用 store.Remove 删除另一条\n\n        ' TODO: 用 Console.WriteLine 打印 store.ToCsv()\n    End Sub\nEnd Module\n",
      hints: ["录入用对象初始化器：store.TryAdd(New Customer With {.Name = \"张三\", .Phone = \"13800000000\"})。", "查询结果遍历：For Each c In store.Search(\"张\") ... Console.WriteLine(c.ToString())。", "编辑要带上目标的 Id：store.Update(New Customer With {.Id = 1, .Name = \"张三丰\", ...})。"],
      tests: [
      test("vb-capstone-crm-8-tryadd", "录入两条客户", matchesAll([new RegExp("\\.TryAdd\\s*\\(.*\\.TryAdd\\s*\\(")]), "至少调用两次 store.TryAdd 加入客户。"),
      test("vb-capstone-crm-8-search", "调用 Search 查询", matches(new RegExp("\\.Search\\s*\\(", "i")), "用 store.Search(...) 做一次查询。"),
      test("vb-capstone-crm-8-editdel", "编辑并删除", matchesAll([new RegExp("\\.Update\\s*\\("), new RegExp("\\.Remove\\s*\\(")]), "整合里要各调用一次 Update 和 Remove。"),
      test("vb-capstone-crm-8-print", "打印导出的 CSV", matches(new RegExp("Console\\.WriteLine\\s*\\([^)]*ToCsv", "i")), "用 Console.WriteLine 打印 store.ToCsv() 的结果。"),
      ],
      sections: [
      sec("主程序就是把零件接起来", "前 7 关每一关都是一个能力:建模、存储、校验、录入、查询、编辑、删除导出。这一关不写新逻辑,只在 Sub Main 里按真实流程把它们串成一条线:录入 → 查询 → 编辑 → 删除 → 导出。能跑通,说明你的 CRM 内核完整了。", { code: "Dim store As New CustomerStore()\nstore.TryAdd(New Customer With {.Name = \"张三\", .Phone = \"13800000000\", .Email = \"z@a.com\"})\nstore.TryAdd(New Customer With {.Name = \"李四\", .Phone = \"13900000000\", .Email = \"l@a.com\"})", caption: "用对象初始化器录入", tip: "With {} 初始化器让一行就填满所有字段,比逐句赋值清爽。" }),
      sec("走完一遍真实操作", "查询用关键字过滤并打印每个匹配;编辑要带上目标的 Id(自增从 1 起,第一条是 1);删除传 Id;最后打印 ToCsv 看全貌。这一串就是用户在真实界面上的点击顺序,只是换成了代码。", { code: "For Each c In store.Search(\"张\")\n    Console.WriteLine(c.ToString())\nNext\nstore.Update(New Customer With {.Id = 1, .Name = \"张三丰\", .Phone = \"13800000000\", .Email = \"z@a.com\"})\nstore.Remove(2)\nConsole.WriteLine(store.ToCsv())", caption: "查询→编辑→删除→导出", warning: "Update 漏填 Id 会找不到目标返回 False;编辑前先确认你拿的是正确的 Id。" }),
      ],
    }),
  ],
  "vb-read-legacy": [
    lesson({
      title: "找到事件入口",
      badge: "入口",
      icon: "VB",
      objective: "在事件处理子过程里加一行注释，标出『程序从这里开始执行』，让事件成为你读代码的起点。",
      starterCode: "' 遗留订单窗体的一段代码：用户点了『提交』按钮\n' TODO: 这段逻辑到底从哪一行开始跑？先把起点标出来\nPublic Class OrderForm\n\n    Private Sub btnSubmit_Click(sender As Object, e As EventArgs) Handles btnSubmit.Click\n        ' TODO: 在这里加注释，写明执行从此处开始(把它当作起点)\n        Dim total As Decimal = ParsePrice(txtPrice.Text) * CInt(txtQty.Text)\n        MessageBox.Show(\"\\u603b\\u4ef7: \" & total.ToString())\n    End Sub\n\n    Private Function ParsePrice(raw As String) As Decimal\n        Return Decimal.Parse(raw)\n    End Function\n\nEnd Class\n",
      hints: ["VB 窗体里没有 Main 当『入口』，真正的入口是用户操作触发的事件，比如 Button.Click。", "认准 Handles 关键字：它后面跟的就是触发这段代码的事件，例如 Handles btnSubmit.Click。", "注释用英文单引号 ' 开头，写中文也没关系。"],
      tests: [
      test("vb-read-legacy-1-handles", "保留 Handles btnSubmit.Click 事件绑定", matches(new RegExp("Handles\\s+btnSubmit\\.Click", "i")), "事件入口靠 Handles btnSubmit.Click 标识，别把它删了。"),
      test("vb-read-legacy-1-entry-comment", "在事件里加上标明入口的注释", matches(new RegExp("'[^\\n]*入口", "i")), "在 btnSubmit_Click 里加一行以 ' 开头、含『入口』二字的注释。"),
      ],
      sections: [
      sec("遗留 VB 没有显眼的 Main", "读控制台程序你会先找 Sub Main，但 WinForms 遗留代码不是这样跑的。窗体加载后就在那儿『等』，真正让代码跑起来的是用户操作——点按钮、改文本框、关窗体。所以读这种代码，第一步永远是：找到被触发的那个事件处理过程，把它当作执行的起点。", { code: "Private Sub btnSubmit_Click(sender As Object, e As EventArgs) _\n        Handles btnSubmit.Click\n    ' 执行从这里开始\nEnd Sub", caption: "Handles 后面的就是真正的入口事件", tip: "一个过程可以 Handles 多个事件（用逗号隔开），别以为一个事件只对应一段代码。" }),
      sec("认 Handles，不要认方法名", "方法名 btnSubmit_Click 只是约定，改名也不影响绑定；真正决定『谁触发它』的是 Handles 子句。遗留项目里经常有人把方法改了名却没改注释，所以你要相信 Handles，不要相信名字。先用 Handles 把每个入口列出来，整张『事件地图』就有了。", { warning: "别假设没有 Handles 的过程就没人调用——它可能被 AddHandler 在代码里动态绑定，下一课会讲。" }),
      ],
    }),
    lesson({
      title: "理清事件的触发顺序",
      badge: "顺序",
      icon: "VB",
      objective: "在 Form_Load 里用注释列出窗体启动时事件触发的先后顺序，并保留一处 AddHandler 动态绑定的证据。",
      starterCode: "' 这个窗体启动时会连锁触发好几个事件，业务规则就藏在顺序里\nPublic Class CheckoutForm\n\n    Private Sub CheckoutForm_Load(sender As Object, e As EventArgs) Handles MyBase.Load\n        ' TODO: 在这里用注释写出启动时的事件触发顺序(1. ... 2. ...)\n        LoadCustomers()\n        ' TODO: 用 AddHandler 把 cboCustomer 的 SelectedIndexChanged 动态绑到 OnCustomerChanged\n    End Sub\n\n    Private Sub LoadCustomers()\n        cboCustomer.Items.Add(\"\\u9ed8\\u8ba4\\u5ba2\\u6237\")\n    End Sub\n\n    Private Sub OnCustomerChanged(sender As Object, e As EventArgs)\n        RefreshDiscount()\n    End Sub\n\n    Private Sub RefreshDiscount()\n        ' \\u6839\\u636e\\u5ba2\\u6237\\u7b97\\u6298\\u6263\n    End Sub\n\nEnd Class\n",
      hints: ["Form 的启动顺序大致是 Load → 子过程里手动调用的逻辑 → 之后用户操作触发的事件。", "AddHandler 的写法是 AddHandler 对象.事件, AddressOf 方法名，它在运行时才建立绑定。", "用 AddHandler 绑定的事件不会出现在 Handles 子句里，光看声明会漏掉它。"],
      tests: [
      test("vb-read-legacy-2-order-comment", "用注释标出触发顺序的步骤", matchesAll([new RegExp("'[^\\n]*1\\."), new RegExp("'[^\\n]*2\\.")]), "在 Form_Load 里写带编号的注释，例如『1. Load』『2. ...』。"),
      test("vb-read-legacy-2-addhandler", "用 AddHandler 动态绑定 SelectedIndexChanged", matchesAll([new RegExp("AddHandler\\s+cboCustomer\\.SelectedIndexChanged"), new RegExp("AddressOf\\s+OnCustomerChanged")]), "写 AddHandler cboCustomer.SelectedIndexChanged, AddressOf OnCustomerChanged。"),
      ],
      sections: [
      sec("顺序就是业务规则", "遗留事件代码最容易踩的坑，是以为每个事件各管各的。其实启动时往往是一串连锁：Load 里调一个填下拉框的过程，填值又触发 SelectedIndexChanged，后者再去算折扣。你如果不按顺序读，就会看到『折扣莫名其妙被改了』而找不到源头。把顺序逐条写成注释，是把隐式流程显式化的最便宜手段。", { code: "' 1. CheckoutForm_Load 触发\n' 2. LoadCustomers 填充下拉框\n' 3. 填充导致 SelectedIndexChanged 触发\n' 4. OnCustomerChanged -> RefreshDiscount", caption: "把连锁顺序写成注释，源头一目了然" }),
      sec("AddHandler 是隐藏的绑定", "Handles 子句是编译期绑定，一眼能看到；AddHandler 是运行期绑定，藏在过程体里，不读完代码根本发现不了。遗留项目里大量条件性绑定（比如『只有管理员才挂这个事件』）都靠它。读代码时，搜一遍 AddHandler / RemoveHandler，才能补全 Handles 漏掉的那半张事件地图。", { warning: "有 AddHandler 却没有对应的 RemoveHandler，是遗留代码里典型的内存泄漏来源，看到要记下来。" }),
      ],
    }),
    lesson({
      title: "追踪全局状态",
      badge: "全局",
      icon: "VB",
      objective: "在模块里找出那个被多处读写的共享变量，加注释标明它是『全局状态』，并写出哪个事件会修改它。",
      starterCode: "' 这是个公共模块，里面有个变量被好几个窗体共用\nModule AppState\n\n    ' TODO: 给这个变量加注释，标明它是全局状态，并写出谁会改它\n    Public CurrentUserId As Integer = 0\n\n    Public Function IsLoggedIn() As Boolean\n        Return CurrentUserId > 0\n    End Function\n\nEnd Module\n\nPublic Class LoginForm\n\n    Private Sub btnLogin_Click(sender As Object, e As EventArgs) Handles btnLogin.Click\n        ' TODO: 这里写入了全局状态——把这次写入用注释标出来\n        CurrentUserId = AuthService.Verify(txtUser.Text, txtPass.Text)\n    End Sub\n\n    Private Sub btnLogout_Click(sender As Object, e As EventArgs) Handles btnLogout.Click\n        CurrentUserId = 0\n    End Sub\n\nEnd Class\n",
      hints: ["Module 里的 Public 变量是全程序共享的——任何窗体都能读它、改它，作用域横跨整个工程。", "追全局变量最有效的方法：分别找『谁读它』和『谁写它』，写操作（赋值）才是改变流程的关键。", "注释里写明这是全局状态，并点名 btnLogin_Click 这种会赋值的地方。"],
      tests: [
      test("vb-read-legacy-3-global-comment", "给共享变量加上『全局状态』注释", matches(new RegExp("'[^\\n]*全局", "i")), "在 Public CurrentUserId 上方或旁边加一行含『全局』的注释。"),
      test("vb-read-legacy-3-writer-noted", "注释里点名写入它的事件", matches(new RegExp("'[^\\n]*btnLogin", "i")), "用注释写明谁会赋值给它，比如点名 btnLogin_Click 这个写入点。"),
      ],
      sections: [
      sec("Module 里的 Public 就是全局变量", "VB 的 Module 不需要 New 就能用，里面的 Public 成员等于全局符号。遗留系统最爱用它存『当前用户』『当前订单』这类状态，方便是真方便，混乱也是真混乱——任何一处事件都可能悄悄把它改了，而读代码的人完全看不出来。所以遇到 Module + Public 变量，第一反应应该是警惕：这是一个全局状态，得专门追踪。", { code: "Module AppState\n    ' 全局状态：登录后写入，登出清零\n    Public CurrentUserId As Integer = 0\nEnd Module", caption: "标注全局状态，顺手记下它的生命周期" }),
      sec("读者和写者要分开追", "理解一个全局变量，关键不是看它被读了多少次，而是看它在哪些地方被『写』。读操作只是消费，写操作才会改变后续所有读者看到的值。把所有赋值点列出来——登录写入、登出清零、某个定时器偷偷重置——你就掌握了这个状态的完整生命周期，业务流程也就显形了。", { tip: "把全局变量的所有写入点列成一张小表，比读十遍逻辑都管用。" }),
      ],
    }),
    lesson({
      title: "拆开表单依赖",
      badge: "依赖",
      icon: "VB",
      objective: "找出一个窗体直接读另一个窗体控件值的耦合点，加注释标明这种跨窗体依赖，并写出它依赖的具体控件。",
      starterCode: "' 报表窗体直接伸手去拿主窗体上的控件值——典型的表单耦合\nPublic Class ReportForm\n\n    Private Sub btnGenerate_Click(sender As Object, e As EventArgs) Handles btnGenerate.Click\n        ' TODO: 下面这行跨窗体读了 MainForm 的控件，加注释标明这是表单依赖,并写出依赖哪个控件\n        Dim region As String = MainForm.cboRegion.Text\n        BuildReport(region)\n    End Sub\n\n    Private Sub BuildReport(region As String)\n        ' \\u6309\\u533a\\u57df\\u751f\\u6210\\u62a5\\u8868\n    End Sub\n\nEnd Class\n",
      hints: ["VB 里直接用窗体名 MainForm.控件 访问的是『默认实例』，等于把另一个窗体的内部控件当公共数据用。", "这种依赖很脆：MainForm 没打开、控件被改名或删掉，这行就炸。注释里要点名具体控件 cboRegion。", "注释里写明这是『表单依赖/跨窗体依赖』，并指出它读的是 MainForm.cboRegion。"],
      tests: [
      test("vb-read-legacy-4-dep-comment", "加上标明表单依赖的注释", matches(new RegExp("'[^\\n]*依赖", "i")), "在跨窗体那行旁加一行以 ' 开头、含『依赖』的注释。"),
      test("vb-read-legacy-4-control-named", "注释里点名依赖的控件 MainForm.cboRegion", matches(new RegExp("'[^\\n]*MainForm\\.cboRegion", "i")), "注释里要写清依赖的是 MainForm.cboRegion 这个控件。"),
      ],
      sections: [
      sec("默认实例：方便的陷阱", "VB.NET 允许你直接写 MainForm.cboRegion.Text，不用先 New 一个窗体——这就是『默认实例』。遗留代码里这种写法满天飞，因为它省事。代价是：ReportForm 现在死死依赖 MainForm 的存在、依赖 cboRegion 这个名字、依赖它当前的值。三者任何一个变了，报表就出错。读代码时，凡是看到『窗体名.控件』的跨窗体访问，都要当成一条硬依赖记下来。", { code: "' 表单依赖：本窗体直接读 MainForm.cboRegion，耦合到主窗体\nDim region As String = MainForm.cboRegion.Text", caption: "把跨窗体读取标注成一条硬依赖", warning: "别在 MainForm 还没打开时调用这行——默认实例会被现造出来，控件值是空的，报表数据就错了。" }),
      sec("依赖方向告诉你阅读顺序", "理出窗体之间『谁读谁』，其实就画出了一张依赖图。ReportForm 读 MainForm，说明你想看懂报表逻辑，得先搞清 MainForm 怎么给 cboRegion 赋值。依赖方向决定了阅读顺序：先读被依赖的一方，再读依赖的一方。把这些跨窗体访问点全标出来，散落在各窗体里的业务流程就被你串成了一条线。", { tip: "把每个『窗体A 读 窗体B 的控件』记成 A→B，整张图画出来，模块边界就清楚了。" }),
      ],
    }),
    lesson({
      title: "还原一条业务流程",
      badge: "整合",
      icon: "VB",
      objective: "把前面学的串起来：在一个总入口事件里，用编号注释完整还原『从点击到下单』的业务流程，覆盖事件入口、全局状态写入和表单依赖三处。",
      starterCode: "' 综合关卡：下单逻辑横跨事件、全局状态、跨窗体依赖\n' TODO: 在 btnPlaceOrder_Click 里用编号注释还原完整业务流程\nModule SessionState\n    ' 全局状态：当前登录用户\n    Public CurrentUserId As Integer = 0\nEnd Module\n\nPublic Class OrderForm\n\n    Private Sub btnPlaceOrder_Click(sender As Object, e As EventArgs) Handles btnPlaceOrder.Click\n        ' TODO: 写编号注释 1. 入口 / 2. 校验全局状态 / 3. 读主窗体控件 / 4. 写全局\n        If CurrentUserId = 0 Then\n            MessageBox.Show(\"\\u8bf7\\u5148\\u767b\\u5f55\")\n            Return\n        End If\n        Dim region As String = MainForm.cboRegion.Text\n        Dim orderId As Integer = OrderService.Place(CurrentUserId, region)\n        SessionState.CurrentUserId = SessionState.CurrentUserId ' \\u4fdd\\u6301\\u4f1a\\u8bdd\n        MessageBox.Show(\"\\u8ba2\\u5355\\u53f7: \" & orderId)\n    End Sub\n\nEnd Class\n",
      hints: ["把这一关当成前四关的总复盘：先认入口事件，再看它读了哪个全局状态，最后看它向哪个窗体伸手。", "编号注释 1./2./3./4. 要对应真实代码行的顺序，别凭空编，照着 If、MainForm.cboRegion 这些实际语句写。", "注释里要同时出现 CurrentUserId（全局状态）和 MainForm.cboRegion（表单依赖），证明你把两条线都理清了。"],
      tests: [
      test("vb-read-legacy-5-numbered-flow", "用至少三步编号注释还原流程", matchesAll([new RegExp("'[^\\n]*1\\."), new RegExp("'[^\\n]*2\\."), new RegExp("'[^\\n]*3\\.")]), "在事件里写 1./2./3. 这样的编号注释，逐步还原流程。"),
      test("vb-read-legacy-5-global-noted", "注释里点出全局状态 CurrentUserId", matches(new RegExp("'[^\\n]*CurrentUserId", "i")), "在注释里写明这步读/写的是全局状态 CurrentUserId。"),
      test("vb-read-legacy-5-dep-noted", "注释里点出表单依赖 MainForm.cboRegion", matches(new RegExp("'[^\\n]*MainForm\\.cboRegion", "i")), "在注释里写明这步依赖了 MainForm.cboRegion 控件。"),
      ],
      sections: [
      sec("三条线在入口汇合", "前四关你练的是三种独立技能：找事件入口、追全局状态、拆表单依赖。真实业务流程从来不是单独一种——一个『下单』事件里，往往同时校验全局登录态、读另一个窗体的选区、再写回会话状态。还原流程的本质，就是站在那个入口事件上，把它牵动的全局变量和外部窗体一条条标出来，按代码真实执行的顺序排好。", { code: "' 1. 入口：用户点『下单』\n' 2. 校验全局状态 CurrentUserId 是否已登录\n' 3. 读表单依赖 MainForm.cboRegion 取选区\n' 4. 调服务下单并写回全局会话", caption: "一段编号注释 = 一份可复查的流程文档" }),
      sec("注释就是你交付的文档", "维护遗留系统时，你读懂的流程如果只留在脑子里，下一个人（或者三个月后的你自己）还得从头再来。把还原出来的流程写成编号注释，钉在入口事件上——它便宜、就地、永远跟代码在一起，比写在 Wiki 里更不容易过时。这就是『阅读遗留代码』这项技能最终要交付的东西：不是一次性的理解，而是留得下来的、可复查的流程文档。", { warning: "别把注释写成废话（如『点了按钮』）——要写出『为什么』和『牵动了什么』,否则等于没写。" }),
      ],
    }),
  ],
  "vb-refactor-module": [
    lesson({
      title: "认清坏味道：臃肿的 Click",
      badge: "识别",
      icon: "VB",
      objective: "把混在 Button1_Click 里的「读输入 → 算钱 → 显示」三段职责，用注释明确标出 ' 读取、' 计算、' 显示 三块边界，先看清再动手。",
      starterCode: "Public Class Form1\n\n    ' 这是一个真实项目里常见的「上帝事件」：一个 Click 干了三件事。\n    ' 价格 = 单价 * 数量，满 200 打九折，再加 6 元运费。\n    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click\n        Dim price As Double = CDbl(txtPrice.Text)\n        Dim qty As Integer = CInt(txtQty.Text)\n        Dim total As Double = price * qty\n        If total >= 200 Then total = total * 0.9\n        total = total + 6\n        lblResult.Text = \"应付：\" & total.ToString(\"0.00\")\n    End Sub\n\n    ' TODO: 暂时不要改逻辑。在上面三段代码前各加一行单引号注释，\n    ' TODO: 第一段(CDbl/CInt 那两行)标成读取职责，\n    ' TODO: 第二段(乘法、打折、加运费)标成计算职责，\n    ' TODO: 第三段(写 lblResult)标成显示职责。三个中文标签自己填。\n    ' TODO: 这一步只「看清缝隙」，下一节才真正下刀拆分。\n\nEnd Class\n",
      hints: ["重构前的第一件事不是改代码，是认出一段代码同时干了几件事——这里是「读、算、显示」三件。", "用三行注释 ' 读取 ' 计算 ' 显示 把三段隔开，缝隙画出来了，下一步抽函数就知道沿哪刀切。", "别现在就动逻辑。CDbl/CInt 的读取、乘法打折的计算、lblResult 的显示，先原样保留，只加注释。"],
      tests: [
      test("vb-refactor-module-1-mark-read", "标出「读取」段边界", matches(new RegExp("'\\s*读取", "i")), "在读取 txtPrice/txtQty 那几行前加一行注释 ' 读取。", 34),
      test("vb-refactor-module-1-mark-calc", "标出「计算」段边界", matches(new RegExp("'\\s*计算", "i")), "在打折与加运费那几行前加一行注释 ' 计算。", 33),
      test("vb-refactor-module-1-mark-show", "标出「显示」段边界", matches(new RegExp("'\\s*显示", "i")), "在赋值 lblResult.Text 那行前加一行注释 ' 显示。", 33),
      ],
      sections: [
      sec("为什么事件处理器会越长越烂", "WinForms 给你的方便也是陷阱：双击一个按钮，IDE 直接把你扔进 Button1_Click。于是「读控件、算业务、改界面」全顺手写在这里。一开始三行，半年后三百行。问题不在于长，而在于「职责混在一起」——你想改打折规则，却要在一堆读控件、改标签的代码里翻找;想给计算写单元测试，又没法测，因为它和 txtPrice、lblResult 这些界面控件焊死了。重构模块的第一步，永远是先认出这种混杂，再决定沿哪条缝切开。"),
      sec("三段职责，三条缝", "盯着这段 Click 看，它清清楚楚分三层:第一层把界面上的字符串读成数字(输入);第二层只跟数字打交道、算出应付金额(纯计算);第三层把结果写回标签(输出)。这三层之间就是天然的拆分缝。本节先用注释把缝标出来——这不是凑数的练习，工程上「先注释分段、再抽取」是真实重构手法:你先确认每段确实只干一件事，注释通过了，抽函数就不会切错位置。", { code: "Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click\n    ' 读取\n    Dim price As Double = CDbl(txtPrice.Text)\n    Dim qty As Integer = CInt(txtQty.Text)\n    ' 计算\n    Dim total As Double = price * qty\n    If total >= 200 Then total = total * 0.9\n    total = total + 6\n    ' 显示\n    lblResult.Text = \"应付：\" & total.ToString(\"0.00\")\nEnd Sub", caption: "缝标好了，逻辑一行没动", tip: "判断一段代码该不该拆，问自己:它能不能脱离界面控件单独说清楚?「算应付金额」能，「读 txtPrice」不能——前者就是该被抽走的纯逻辑。", warning: "别在这一步顺手「优化」逻辑(比如把 0.9 改成常量)。重构纪律是:一次只做一件事。先分段，逻辑等价不变，跑通了再谈下一步。" }),
      ],
    }),
    lesson({
      title: "抽出纯计算：Function",
      badge: "函数",
      icon: "VB",
      objective: "把「算应付金额」抽成 Function CalcTotal(price As Double, qty As Integer) As Double，参数进、返回值出，不碰任何界面控件。",
      starterCode: "Public Class Form1\n\n    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click\n        Dim price As Double = CDbl(txtPrice.Text)\n        Dim qty As Integer = CInt(txtQty.Text)\n        ' 计算（待抽出）：total = price*qty，满 200 打九折，再加 6 运费\n        Dim total As Double = price * qty\n        If total >= 200 Then total = total * 0.9\n        total = total + 6\n        lblResult.Text = \"应付：\" & total.ToString(\"0.00\")\n    End Sub\n\n    ' TODO: 写一个 Function CalcTotal(price As Double, qty As Integer) As Double\n    ' TODO: 把上面「计算」三行原样搬进来，用 Return 返回算出的金额\n    ' TODO: 然后把 Click 里的计算三行换成 Dim total As Double = CalcTotal(price, qty)\n    ' 注意：CalcTotal 内部绝不能出现 txtPrice / lblResult 这些控件名\n\nEnd Class\n",
      hints: ["纯函数的标志:输入只来自参数，输出只靠 Return，中间不读不写任何界面控件——这样它才可被测、可被复用。", "Function 必须声明返回类型 As Double，并在函数体里用 Return 把算出的 total 交出去。", "抽完别忘了在 Click 里「调用」它:Dim total As Double = CalcTotal(price, qty)，否则旧的重复逻辑还在。"],
      tests: [
      test("vb-refactor-module-2-signature", "声明带参数和返回类型的 CalcTotal", matches(new RegExp("function\\s+calctotal\\s*\\(\\s*price\\s+as\\s+double\\s*,\\s*qty\\s+as\\s+integer\\s*\\)\\s+as\\s+double", "i")), "写 Function CalcTotal(price As Double, qty As Integer) As Double。", 34),
      test("vb-refactor-module-2-return", "函数体里用 Return 返回金额", matches(new RegExp("\\breturn\\b\\s+\\w+", "i")), "在 CalcTotal 里用 Return 把算好的 total 返回，不要用 lblResult 显示。", 33),
      test("vb-refactor-module-2-call", "Click 里调用 CalcTotal", matches(new RegExp("calctotal\\s*\\(\\s*price\\s*,\\s*qty\\s*\\)", "i")), "把 Click 里的计算换成 CalcTotal(price, qty) 的调用。", 33),
      ],
      sections: [
      sec("Function vs Sub:看它要不要「交答案」", "VB 里两种过程:Sub 干活但不返回值，Function 干活并交回一个结果。「算应付金额」天生是 Function——你给它单价和数量，它还你一个金额。把它抽成 Function 的好处是巨大的:第一，它再也不依赖窗体，给两个数就能算，可以在测试里直接 CalcTotal(100, 3) 验证;第二，别的地方(另一个按钮、一个批量结算)想算价，直接调它，不必复制粘贴。这就是「拆函数」的核心收益:逻辑从界面里解放出来。", { code: "Private Function CalcTotal(price As Double, qty As Integer) As Double\n    Dim total As Double = price * qty\n    If total >= 200 Then total = total * 0.9\n    total = total + 6\n    Return total\nEnd Function", caption: "整段计算搬进来，参数进、Return 出", tip: "参数名最好沿用原变量名(price、qty)，搬代码时一个字不用改，最不容易出错——这正是「机械式抽取」的安全做法。" }),
      sec("调用点:让 Click 变薄", "抽出函数只是一半，另一半是回到 Click 把原来那三行计算「删掉」，换成一行调用。很多人抽完函数却忘了删旧代码，结果逻辑写了两份——这叫「抽取未完成」，比不抽更危险，因为以后改打折规则只改了一处。抽取的终点永远是:原地只剩一句调用，旧逻辑彻底搬走、不留副本。", { code: "Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click\n    Dim price As Double = CDbl(txtPrice.Text)\n    Dim qty As Integer = CInt(txtQty.Text)\n    Dim total As Double = CalcTotal(price, qty)   ' 计算这一段现在只剩一行调用\n    lblResult.Text = \"应付：\" & total.ToString(\"0.00\")\nEnd Sub", caption: "三行计算缩成一句调用", tip: "抽完跑一遍程序，确认结果数字和之前一模一样。重构的金标准是「行为不变」——界面看不出任何变化，才说明你只是搬家、没改逻辑。", warning: "别把 txtPrice.Text 或 lblResult 写进 CalcTotal。一旦函数里出现控件名，它就又被界面绑死，测试和复用全泡汤——这是「假抽取」，换了个位置但没真正解耦。" }),
      ],
    }),
    lesson({
      title: "抽出副作用:Sub 与 ByVal",
      badge: "过程",
      icon: "VB",
      objective: "把「读输入」抽成 Function ReadQuantity() As Integer，把「显示结果」抽成 Sub ShowTotal(ByVal total As Double)，让 Click 只剩协调三步的骨架。",
      starterCode: "Public Class Form1\n\n    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click\n        Dim price As Double = CDbl(txtPrice.Text)\n        Dim qty As Integer = CInt(txtQty.Text)\n        Dim total As Double = CalcTotal(price, qty)\n        lblResult.Text = \"应付：\" & total.ToString(\"0.00\")\n    End Sub\n\n    Private Function CalcTotal(price As Double, qty As Integer) As Double\n        Dim total As Double = price * qty\n        If total >= 200 Then total = total * 0.9\n        total = total + 6\n        Return total\n    End Function\n\n    ' TODO: 写一个读数量的 Function：无参数、返回 Integer，函数体里读 txtQty.Text 并 CInt 后 Return。\n    ' TODO: 写一个显示结果的 Sub：用 ByVal 接收一个 Double 金额，函数体里把它写进 lblResult。\n    ' TODO: 改写 Click，把读数量改成调用你的读取函数，把显示改成调用你的显示过程。\n\nEnd Class\n",
      hints: ["Function 用来「拿东西」(读数量回来)，Sub 用来「干件事而不回值」(把结果显示出去)——按要不要返回值选。", "ShowTotal(ByVal total As Double):ByVal 表示传进来的是 total 的一份拷贝，函数内改它也不影响外面，这正是「只读用」的安全默认。", "ReadQuantity 可以碰 txtQty(它的职责就是读控件),但 CalcTotal 不行——区分「界面适配层」和「纯逻辑层」。"],
      tests: [
      test("vb-refactor-module-3-read-fn", "声明 Function ReadQuantity() As Integer", matches(new RegExp("function\\s+readquantity\\s*\\(\\s*\\)\\s+as\\s+integer", "i")), "写 Function ReadQuantity() As Integer 并 Return CInt(txtQty.Text)。", 25),
      test("vb-refactor-module-3-show-sub", "声明 Sub ShowTotal(ByVal total As Double)", matches(new RegExp("sub\\s+showtotal\\s*\\(\\s*byval\\s+total\\s+as\\s+double\\s*\\)", "i")), "写 Sub ShowTotal(ByVal total As Double)，明确用 ByVal。", 25),
      test("vb-refactor-module-3-show-body", "ShowTotal 内部写 lblResult", matches(new RegExp("lblResult\\.Text\\s*=", "i")), "把显示逻辑搬进 ShowTotal:lblResult.Text = \"应付：\" & total.ToString(\"0.00\")。", 25),
      test("vb-refactor-module-3-click-uses", "Click 调用 ReadQuantity 和 ShowTotal", matchesAll([new RegExp("readquantity\\s*\\(\\s*\\)"), new RegExp("showtotal\\s*\\(\\s*total\\s*\\)")]), "改写 Click，让它调用 ReadQuantity() 取数量、调用 ShowTotal(total) 显示。", 25),
      ],
      sections: [
      sec("不是所有过程都该返回值", "上一节把「计算」抽成了 Function，因为它要交出金额。但「显示结果」呢?它把字写到标签上就完了，不需要交回任何东西——这种「只产生副作用、不返回值」的活，正是 Sub 的本职。硬要把显示写成 Function 返回一个用不上的值，反而别扭。判断标准很朴素:调用方需不需要它给的结果?需要就 Function，纯执行动作就 Sub。读数量需要拿到数字，是 Function;显示结果不需要回值，是 Sub。", { code: "Private Function ReadQuantity() As Integer\n    Return CInt(txtQty.Text)\nEnd Function\n\nPrivate Sub ShowTotal(ByVal total As Double)\n    lblResult.Text = \"应付：\" & total.ToString(\"0.00\")\nEnd Sub", caption: "读用 Function 取回数字，显示用 Sub 产生副作用", tip: "现在已经分出三层:CalcTotal 是纯逻辑层(不碰控件),ReadQuantity / ShowTotal 是界面适配层(专门碰控件)。这种分层让纯逻辑可测、界面可换。" }),
      sec("ByVal:默认就该传拷贝", "ShowTotal 的参数写成 ByVal total As Double。ByVal 的意思是「传值」——调用时把 total 复制一份给函数，函数里怎么改这份拷贝都波及不到外面的原值。这正是绝大多数参数该有的样子:函数只是「读取」你给的数据，不该偷偷改掉调用方的变量。它的反面是 ByRef(传引用)，那会让函数能改动外部变量——只在你明确需要「让函数往回写」时才用，比如返回多个结果。默认 ByVal，需要才 ByRef，这是减少 bug 的纪律。"),
      ],
    }),
    lesson({
      title: "数据和行为打包:Class",
      badge: "类",
      icon: "VB",
      objective: "把订单的「单价 + 数量 + 算总价」收进一个 Class Order:两个属性 Price、Quantity，一个方法 Function Total() As Double 内部用 Me.Price/Me.Quantity 计算。",
      starterCode: "Public Class Form1\n\n    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click\n        Dim price As Double = CDbl(txtPrice.Text)\n        Dim qty As Integer = CInt(txtQty.Text)\n        Dim total As Double = CalcTotal(price, qty)\n        lblResult.Text = \"应付：\" & total.ToString(\"0.00\")\n    End Sub\n\n    Private Function CalcTotal(price As Double, qty As Integer) As Double\n        Dim total As Double = price * qty\n        If total >= 200 Then total = total * 0.9\n        total = total + 6\n        Return total\n    End Function\n\nEnd Class\n\n' TODO: 在 Form1 的 End Class 之外，新建一个公开的订单类型（类名用 Order）。\n' TODO: 给它两个公开属性：单价(Double) 和 数量(Integer)。\n' TODO: 给它一个无参、返回 Double 的方法，名字叫 Total。\n' TODO: 方法体里用 Me 取自己的单价与数量来算价（满 200 打九折，加 6 运费），Return 结果。\n",
      hints: ["类的价值是「把总在一起出现的数据和操作它们的逻辑」捆成一个东西。单价、数量、算总价天生属于「一张订单」。", "VB 的自动属性一行就够:Public Property Price As Double，编译器替你生成背后的字段。", "方法 Total() 不用参数——它要的数据已经是自己的属性，用 Me.Price * Me.Quantity 直接取。这就是「数据和行为在一起」的好处。"],
      tests: [
      test("vb-refactor-module-4-class", "定义 Public Class Order", matches(new RegExp("public\\s+class\\s+order\\b", "i")), "在 Form1 外写 Public Class Order ... End Class。", 25),
      test("vb-refactor-module-4-props", "声明 Price 和 Quantity 两个属性", matchesAll([new RegExp("property\\s+price\\s+as\\s+double"), new RegExp("property\\s+quantity\\s+as\\s+integer")]), "写 Public Property Price As Double 和 Public Property Quantity As Integer。", 25),
      test("vb-refactor-module-4-method", "类里有 Function Total() As Double", matches(new RegExp("function\\s+total\\s*\\(\\s*\\)\\s+as\\s+double", "i")), "在 Order 类里写 Public Function Total() As Double。", 25),
      test("vb-refactor-module-4-me", "Total 内部用 Me.Price 与 Me.Quantity", matchesAll([new RegExp("me\\.price"), new RegExp("me\\.quantity")]), "用 Me.Price 和 Me.Quantity 取自己的数据来算，别再接收外部参数。", 25),
      ],
      sections: [
      sec("函数拆得再细，数据还是散的", "前面三节把逻辑拆成了 ReadQuantity、CalcTotal、ShowTotal,已经清爽很多。但还有个隐患:单价和数量是两个孤零零的局部变量,在 Click 里飘着,谁都能传错顺序——CalcTotal(qty, price) 编译照样通过,结果全错。这说明「单价」和「数量」本就该绑在一起:它们描述的是同一张订单。当一组数据总是结伴出现、还共享一套计算规则时,信号很明确——该把它们升级成一个 Class 了。这才是「模块拆分」从函数走向类的关键一跃。", { code: "Public Class Order\n    Public Property Price As Double\n    Public Property Quantity As Integer\n\n    Public Function Total() As Double\n        Dim t As Double = Me.Price * Me.Quantity\n        If t >= 200 Then t = t * 0.9\n        t = t + 6\n        Return t\n    End Function\nEnd Class", caption: "一张订单:数据(属性)和算法(方法)住在一起", tip: "类一定写在 Form1 的 End Class 之外,做成独立类型。写在窗体里面会变成窗体的内部成员,失去「可被任何地方复用」的意义。" }),
      sec("Me 是「这一个对象自己」", "注意 Total() 方法没有任何参数,它要的单价和数量从哪来?从 Me.Price、Me.Quantity——Me 指「正在被调用的这一个 Order 实例」。这就是类比裸函数高明的地方:数据已经存在对象身上,方法直接取自己的属性,再也不会传错参数顺序。CalcTotal 那种「把数据当参数传来传去」的写法,在类里变成了「数据就是我的一部分」。把数据和操作它的方法收进同一个类,正是面向对象封装的本质。"),
      ],
    }),
    lesson({
      title: "收尾:瘦身后的事件处理器",
      badge: "整合",
      icon: "VB",
      objective: "把 Button1_Click 改写成「建对象 → 填属性 → 调方法 → 显示」四步:New Order、设 .Price/.Quantity、调 .Total()、写回 lblResult，让事件处理器只负责协调。",
      starterCode: "Public Class Form1\n\n    ' TODO: 把 Click 改写成四步协调（不再有任何业务算式）：\n    '   1) Dim o As New Order\n    '   2) o.Price = CDbl(txtPrice.Text) : o.Quantity = CInt(txtQty.Text)\n    '   3) Dim total As Double = o.Total()\n    '   4) lblResult.Text = \"应付：\" & total.ToString(\"0.00\")\n    Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click\n        Dim price As Double = CDbl(txtPrice.Text)\n        Dim qty As Integer = CInt(txtQty.Text)\n        Dim total As Double = price * qty\n        If total >= 200 Then total = total * 0.9\n        total = total + 6\n        lblResult.Text = \"应付：\" & total.ToString(\"0.00\")\n    End Sub\n\nEnd Class\n\nPublic Class Order\n    Public Property Price As Double\n    Public Property Quantity As Integer\n\n    Public Function Total() As Double\n        Dim t As Double = Me.Price * Me.Quantity\n        If t >= 200 Then t = t * 0.9\n        t = t + 6\n        Return t\n    End Function\nEnd Class\n",
      hints: ["瘦身后的 Click 里不该再出现 * 0.9、+ 6 这类业务算式——它们已经全搬进 Order.Total()，留在这里就是没拆干净。", "Dim o As New Order 一句话就建好对象。然后用 o.Price = ... 给属性赋值，像填表格一样。", "调用方法用 o.Total()——算法藏在对象里，Click 只管「要结果」，不管「怎么算」。"],
      tests: [
      test("vb-refactor-module-5-new", "Click 里创建 Order 对象", matches(new RegExp("\\bnew\\s+order\\b", "i")), "用 Dim o As New Order 创建对象。", 25),
      test("vb-refactor-module-5-fill", "给 Price 和 Quantity 属性赋值", matchesAll([new RegExp("\\.price\\s*=\\s*cdbl\\s*\\(\\s*txtprice\\.text"), new RegExp("\\.quantity\\s*=\\s*cint\\s*\\(\\s*txtqty\\.text")]), "写 o.Price = CDbl(txtPrice.Text) 和 o.Quantity = CInt(txtQty.Text)。", 25),
      test("vb-refactor-module-5-call-total", "调用对象的 Total() 方法", matches(new RegExp("\\w+\\.total\\s*\\(\\s*\\)", "i")), "用 o.Total() 取应付金额，别在 Click 里重算。", 25),
      test("vb-refactor-module-5-no-math", "Click 里不再残留业务算式", matches(new RegExp("Handles\\s+Button1\\.Click(?:[\\s\\S](?!\\*\\s*0\\.9))*?End\\s+Sub", "i")), "删掉 Click 里所有 * 0.9 / + 6 的算式，业务计算只能存在于 Order.Total()。", 25),
      ],
      sections: [
      sec("事件处理器的最终归宿:只协调，不干活", "把前四节攒下的零件拼起来,你会看到一个理想的事件处理器长什么样:它不读控件细节(交给适配)、不算业务(交给 Order)、只做四件事——建一个对象、把界面上的值填进去、让对象算出结果、把结果显示出来。这种「薄薄一层、只负责把各部分串起来」的处理器,正是模块拆分的终点。回头看第一节那个三百行预备役的 Click,现在它干净得一眼能读懂,而真正的业务逻辑被安全地锁在 Order 里,能单独测、能在别处复用。", { code: "Private Sub Button1_Click(sender As Object, e As EventArgs) Handles Button1.Click\n    Dim o As New Order\n    o.Price = CDbl(txtPrice.Text)\n    o.Quantity = CInt(txtQty.Text)\n    Dim total As Double = o.Total()\n    lblResult.Text = \"应付：\" & total.ToString(\"0.00\")\nEnd Sub", caption: "建对象 → 填属性 → 调方法 → 显示，四步收尾", tip: "现在改打折规则只改 Order.Total() 一处;新增一个「批量结算」窗体,直接 New Order 复用同一套算法。这就是拆分换来的回报:改一处、用多处。" }),
      sec("重构没改任何功能——这正是它该有的样子", "从第一节到现在,程序对用户的表现一字未变:输入单价数量、点按钮、看到同样的应付金额。变的全在代码内部——逻辑分了层、数据进了类、入口变薄了。这就是重构的定义:在不改变外部行为的前提下,改善内部结构。所以每拆一步都要跑一遍、对一次结果,确保数字没变。能做到「行为不变、结构变好」,你就真正掌握了模块拆分,而不是把代码搬来搬去碰运气。"),
      ],
    }),
  ],
  "vb-add-tests": [
    lesson({
      title: "先把旧函数抠成可测的",
      badge: "可测",
      icon: "VB",
      objective: "把价格计算逻辑从按钮事件里搬进一个纯函数 CalculateTotal(quantity, unitPrice) As Decimal,只算不碰界面。",
      starterCode: "Public Class FormOrder\n\n    ' 这是一段你接手的旧代码：计算逻辑全塞在按钮事件里，\n    ' 直接读控件、直接写 Label，根本没法单独测。\n    Private Sub btnCalc_Click(sender As Object, e As EventArgs) Handles btnCalc.Click\n        Dim qty As Integer = CInt(txtQty.Text)\n        Dim price As Decimal = CDec(txtPrice.Text)\n        Dim total As Decimal = qty * price\n        If qty >= 10 Then\n            total = total * 0.9D   ' 满 10 件打九折\n        End If\n        lblTotal.Text = total.ToString(\"0.00\")\n    End Sub\n\n    ' TODO: 写一个纯函数，把上面的「算钱」逻辑原样搬进来。\n    ' 签名固定为：\n    '   Public Function CalculateTotal(quantity As Integer, unitPrice As Decimal) As Decimal\n    ' 要求：函数体里只做计算和 Return，不准出现 txt / lbl / .Text 这些界面成分。\n\nEnd Class\n",
      hints: ["测试没法点按钮、也没法读 TextBox。要测的逻辑必须能用普通参数喂进去、用返回值收回来——这就是「纯函数」。", "原样搬：qty*price 还是 qty*price，满 10 件九折还是九折。这一步只搬位置，绝不改行为，否则你测的就不是旧逻辑了。", "折扣用 0.9D 这种带 D 后缀的 Decimal 字面量，别写 0.9（那是 Double），算钱要全程 Decimal。"],
      tests: [
      test("vb-add-tests-1-signature", "存在 CalculateTotal(quantity, unitPrice) As Decimal", matches(new RegExp("Function\\s+CalculateTotal\\s*\\(\\s*quantity\\s+As\\s+Integer\\s*,\\s*unitPrice\\s+As\\s+Decimal\\s*\\)\\s+As\\s+Decimal", "i")), "按固定签名写 Public Function CalculateTotal(quantity As Integer, unitPrice As Decimal) As Decimal。", 34),
      test("vb-add-tests-1-returns", "函数用 Return 把结果交出去", matches(new RegExp("Return\\s+\\w", "i")), "纯函数靠 Return 返回算出来的金额，不要去写 Label。", 33),
      test("vb-add-tests-1-no-ui", "函数体不碰界面（无 .Text）", matches(new RegExp("Function\\s+CalculateTotal[\\s\\S]*?(quantity\\s*\\*\\s*unitPrice|unitPrice\\s*\\*\\s*quantity)", "i")), "把 quantity、unitPrice 当参数算，别在函数里读 txt 或写 lbl.Text。", 33),
      ],
      sections: [
      sec("测试测不了「混在事件里的逻辑」", "接手旧 VB 项目，最常见的样子就是：算钱、判断、改状态，全挤在 btnXxx_Click 里，张口就读 txtQty.Text、闭口就写 lblTotal.Text。这种代码人能跑，测试跑不了——自动化测试既点不了按钮，也没有窗体上的控件。想给旧逻辑加保护，第一步永远是把「算什么」从「在哪显示」里抠出来，单独做成一个吃参数、吐返回值的函数。", { code: "' 改造前：逻辑和界面焊死在一起\nDim total As Decimal = CInt(txtQty.Text) * CDec(txtPrice.Text)\nlblTotal.Text = total.ToString(\"0.00\")", caption: "读控件、算、写控件三件事黏在一行——测试无从下手", tip: "判断一段逻辑「可不可测」有个土办法：把它复制到一个空控制台项目里，缺控件就编译不过的，就是不可测。" }),
      sec("纯函数：同样的输入，永远同样的输出", "把逻辑搬进 CalculateTotal(quantity, unitPrice) 后，它只依赖两个参数，不读控件、不读全局、不弹窗，给一组数就还你一个确定的数。这种函数叫纯函数，是最好测的东西——你喂 (10, 5D) 就该拿到 45D，永远如此。注意：这一步是「搬家」不是「装修」，逻辑必须和旧代码一模一样，连那个九折规则都要原样保留，否则后面的测试保护的就不是真实的旧行为了。", { code: "Public Function CalculateTotal(quantity As Integer, unitPrice As Decimal) As Decimal\n    Dim total As Decimal = quantity * unitPrice\n    If quantity >= 10 Then\n        total = total * 0.9D\n    End If\n    Return total\nEnd Function", caption: "搬完后的纯函数：只算钱，原样保留九折规则", warning: "别趁搬家「顺手修 bug」。哪怕你觉得九折规则不对，也先原样搬、原样测，确认现状后再单独提改动。先固化，再改。" }),
      ],
    }),
    lesson({
      title: "第一个样例：用断言钉住一个输出",
      badge: "断言",
      icon: "VB",
      objective: "在 RunTests 里调用 CalculateTotal(2, 5D)，期望 10D，不相等就用 Throw New Exception 报错。",
      starterCode: "Module Program\n\n    ' 被测的旧逻辑（已抠成纯函数）\n    Public Function CalculateTotal(quantity As Integer, unitPrice As Decimal) As Decimal\n        Dim total As Decimal = quantity * unitPrice\n        If quantity >= 10 Then\n            total = total * 0.9D\n        End If\n        Return total\n    End Function\n\n    ' 一个最朴素的测试运行器：跑断言，不崩就算过。\n    Sub RunTests()\n        ' TODO（准备）：用 2 件、单价 5 这组样例调用被测函数，把结果存进 actual\n        ' TODO（期望）：定义这组样例应得的正确总价为 expected\n        ' TODO（断言）：actual 与 expected 不相等时，抛出异常，\n        '              并把期望值和实际值都写进异常消息\n        Console.WriteLine(\"全部通过\")\n    End Sub\n\n    Sub Main()\n        RunTests()\n    End Sub\n\nEnd Module\n",
      hints: ["一个测试就是三步：准备输入 → 调用 → 比对。比对失败就 Throw，让运行器当场停下报警。", "期望值写成 Decimal 字面量 10D，别写 10。和 Decimal 结果比时类型要一致，省得隐式转换给你埋坑。", "异常消息里一定要带上 expected 和 actual 的具体数值，否则测试红了你还得回来加打印——一步到位。"],
      tests: [
      test("vb-add-tests-2-call", "调用 CalculateTotal(2, 5D)", matches(new RegExp("CalculateTotal\\s*\\(\\s*2\\s*,\\s*5D\\s*\\)", "i")), "用 CalculateTotal(2, 5D) 触发被测逻辑，并把结果接住。", 34),
      test("vb-add-tests-2-expected", "定义期望值 10D", matches(new RegExp("10D", "i")), "写出期望值 10D，明确这次样例应该得到多少。", 33),
      test("vb-add-tests-2-throw", "不相等时 Throw 报错", matchesAll([new RegExp("<>"), new RegExp("Throw\\s+New\\s+Exception")]), "If actual <> expected Then Throw New Exception(...)，断言失败要抛出去。", 33),
      ],
      sections: [
      sec("样例 = 输入 + 你认定正确的输出", "「补样例」补的到底是什么？就是一对对「喂进去的输入」和「应该吐出来的输出」。比如「2 件、单价 5，总价应当是 10」。把这对关系写成代码、让程序自己去验，就是一个测试样例。这一对一旦写下，旧逻辑的这个行为就被钉死了：以后谁改代码让 (2,5) 不再等于 10，测试立刻变红。", { code: "Dim actual As Decimal = CalculateTotal(2, 5D)\nDim expected As Decimal = 10D", caption: "先把「输入」和「我认定的正确输出」摆出来", tip: "Arrange-Act-Assert（准备-执行-断言）是测试的通用三段式，记住它，你写的每个测试都能套这个骨架。" }),
      sec("断言：不对就让它「炸」", "光算出 actual 没用，你得让程序自己判断对不对。最朴素的断言就是：actual 和 expected 一不相等，立刻 Throw 一个异常。为什么是抛异常？因为异常会中断执行、打印调用栈，运行器一眼就看到「这里红了」。消息里务必写清期望多少、实际多少——红测试的价值全在它能不能一句话告诉你哪错了。", { code: "If actual <> expected Then\n    Throw New Exception($\"CalculateTotal(2,5) 期望 {expected}，实际 {actual}\")\nEnd If", caption: "断言失败立刻抛异常，并带上期望/实际数值", warning: "别用 If actual <> expected Then Console.WriteLine(\"错了\") 这种写法——只打印不抛异常，程序照样「正常退出」，CI 会误判为通过。" }),
      ],
    }),
    lesson({
      title: "多样例覆盖：把边界都钉住",
      badge: "覆盖",
      icon: "VB",
      objective: "用一个 (输入, 期望) 的数组驱动多组样例，循环里逐组比对，必须覆盖到 9 件不打折与 10 件打折这两侧边界。",
      starterCode: "Module Program\n\n    Public Function CalculateTotal(quantity As Integer, unitPrice As Decimal) As Decimal\n        Dim total As Decimal = quantity * unitPrice\n        If quantity >= 10 Then\n            total = total * 0.9D\n        End If\n        Return total\n    End Function\n\n    Sub RunTests()\n        ' 一行一组样例：{ 数量, 单价, 期望总价 }\n        Dim cases = {\n            New Decimal() {1D, 5D, 5D},\n            New Decimal() {2D, 5D, 10D}\n            ' TODO: 补上边界两侧——\n            '   9 件、单价 10：不打折，期望 90\n            '   10 件、单价 10：打九折，期望 90（10*10*0.9）\n            ' 自己把这两行加进数组\n        }\n\n        ' TODO: For Each c In cases\n        '   调用 CalculateTotal(CInt(c(0)), c(1))，与 c(2) 比对\n        '   不相等就 Throw New Exception(把这组输入和期望/实际写进消息)\n        ' Next\n\n        Console.WriteLine(\"全部通过\")\n    End Sub\n\n    Sub Main()\n        RunTests()\n    End Sub\n\nEnd Module\n",
      hints: ["一个样例只能证明一个点。旧逻辑里有个 quantity >= 10 的分支，就必须分别测 9（不打折）和 10（打折），否则你根本没碰到那条 If。", "用数组 + For Each 驱动，加样例就只是多写一行数据，不用复制粘贴一整段断言。这叫数据驱动测试。", "边界值最容易出 bug：>= 还是 >，差一个等号结果就反。专门拿临界点的两侧各打一枪，最能逼出 off-by-one。"],
      tests: [
      test("vb-add-tests-3-loop", "用 For Each 遍历样例数组", matches(new RegExp("For\\s+Each\\s+\\w+\\s+In\\s+cases", "i")), "写 For Each c In cases，让一套断言跑遍所有样例。", 34),
      test("vb-add-tests-3-boundary", "覆盖 9 件与 10 件两侧边界", matchesAll([new RegExp("9D\\s*,"), new RegExp("10D\\s*,")]), "数组里要同时出现 9D 和 10D 这两个数量的样例，把折扣分支两侧都钉住。", 33),
      test("vb-add-tests-3-assert", "循环里逐组断言并 Throw", matchesAll([new RegExp("CalculateTotal\\s*\\("), new RegExp("<>"), new RegExp("Throw\\s+New\\s+Exception")]), "拿 CalculateTotal 的结果和 c(2) 比，不等就 Throw New Exception。", 33),
      ],
      sections: [
      sec("一个样例只盖一个点", "上一课的 (2,5)=10 只证明了「不打折的普通情况」算对了。可旧逻辑里藏着一句 If quantity >= 10 Then 打九折——这条分支你一次都没跑到。要真正保护旧逻辑，得让样例覆盖所有「会走不同路」的情况：普通、打折、还有那个最危险的临界点。覆盖不到的分支，等于没被保护，下次别人改坏了你也发现不了。", { code: "' 三类必测情形\n' 普通：2 件          → 不打折\n' 边界下侧：9 件      → 仍不打折\n' 边界上侧：10 件     → 开始打折", caption: "围着 >= 10 这条分支，两侧各留一个样例", tip: "看到 If / Select Case，就问自己「每个分支都被某个样例走到了吗」。这就是分支覆盖的朴素版。" }),
      sec("数据驱动：加样例只加一行数据", "如果每个样例都复制一整段「调用 + 断言」，样例一多代码就臃肿，还容易漏改。更专业的做法是把样例写成一张数据表（数组），断言逻辑只写一遍，用 For Each 跑遍整张表。以后想加情形，只在数组里多写一行 {数量, 单价, 期望}。这就是数据驱动测试——表越长，旧逻辑被保护得越严实。", { code: "For Each c In cases\n    Dim qty As Integer = CInt(c(0))\n    Dim actual As Decimal = CalculateTotal(qty, c(1))\n    If actual <> c(2) Then\n        Throw New Exception($\"qty={qty}, price={c(1)} 期望 {c(2)}，实际 {actual}\")\n    End If\nNext", caption: "一套断言跑遍整张样例表", warning: "别只测「好走的」样例。专挑边界、空值、最大最小这些刁钻输入，旧 bug 往往就藏在这些角落里。" }),
      ],
    }),
    lesson({
      title: "金标准：让运行时自己生成期望值",
      badge: "金标准",
      icon: "VB",
      objective: "用 GetGoldenTotal 模拟「现状基线」，把 CalculateTotal 的当前输出当成金标准逐组比对，确保重构后行为不变。",
      starterCode: "Module Program\n\n    ' 你正打算重构的旧逻辑\n    Public Function CalculateTotal(quantity As Integer, unitPrice As Decimal) As Decimal\n        Dim total As Decimal = quantity * unitPrice\n        If quantity >= 10 Then\n            total = total * 0.9D\n        End If\n        Return total\n    End Function\n\n    ' 金标准：把「重构前」算出的、已经核对过的正确结果固化在这里。\n    ' 这些数字是先跑一遍现状、人工确认无误后抄下来的「快照」。\n    Private Function GetGoldenTotal(quantity As Integer, unitPrice As Decimal) As Decimal\n        If quantity = 5 AndAlso unitPrice = 10D Then Return 50D\n        If quantity = 10 AndAlso unitPrice = 10D Then Return 90D\n        If quantity = 20 AndAlso unitPrice = 3D Then Return 54D\n        Throw New Exception(\"金标准里没有这组输入\")\n    End Function\n\n    Sub RunTests()\n        Dim inputs = {\n            New Decimal() {5D, 10D},\n            New Decimal() {10D, 10D},\n            New Decimal() {20D, 3D}\n        }\n\n        ' TODO: 遍历 inputs 里的每组输入：\n        '   取当前实际输出 actual（调用被测函数）\n        '   取冻结的基线 golden（调用金标准函数）\n        '   两者不一致时抛出异常，消息里写清这组输入与两个数值\n\n        Console.WriteLine(\"行为未变，重构安全\")\n    End Sub\n\n    Sub Main()\n        RunTests()\n    End Sub\n\nEnd Module\n",
      hints: ["金标准（golden master）的核心思想：旧逻辑当下输出什么，就先认它是「对的」，把这批输出冻成基线。重构后只要不偏离基线，就没改坏。", "金标准适合你看不太懂、又不敢改的老逻辑——你不需要理解它为什么对，只需要保证它「之后和之前一样」。", "对每组输入，都要拿 CalculateTotal 的实际输出和 GetGoldenTotal 的基线比，不等就抛——这才是「行为没变」的证据。"],
      tests: [
      test("vb-add-tests-4-call-actual", "对每组输入调用 CalculateTotal", matches(new RegExp("CalculateTotal\\s*\\(", "i")), "在循环里用 CalculateTotal 算出当前实际输出 actual。", 34),
      test("vb-add-tests-4-golden", "取金标准基线 GetGoldenTotal", matches(new RegExp("GetGoldenTotal\\s*\\(", "i")), "用 GetGoldenTotal(...) 取出冻结的期望基线，和实际值对比。", 33),
      test("vb-add-tests-4-compare", "实际与基线不符时 Throw", matchesAll([new RegExp("<>"), new RegExp("Throw\\s+New\\s+Exception")]), "actual <> golden 时 Throw New Exception，证明行为发生了偏移。", 33),
      ],
      sections: [
      sec("看不懂的旧逻辑，也能先「锁住」", "前几课你知道正确答案该是多少，所以能手写 expected。但真实的旧代码常常是一团你看不懂、也不敢动的逻辑——你根本说不清它「应该」输出什么。金标准（golden master）测试就是为这种情况发明的：先信任现状，把旧逻辑当下的输出当成「正确答案」记下来，冻成基线。你不需要理解它，只需要保证之后改完，输出和之前一字不差。", { code: "' 第一次：跑现状，把核对过的输出抄成基线\n' CalculateTotal(5, 10)  → 50   ← 记下来\n' CalculateTotal(10, 10) → 90   ← 记下来\n' 之后这些数字就是不可动摇的「金标准」", caption: "先固化现状输出，再谈改动", tip: "真实项目里基线常存成一个文件（.txt/.json），跑测试时读出来比对。这里用 GetGoldenTotal 函数模拟同一件事。" }),
      sec("重构的安全网：前后输出必须一致", "有了金标准，重构就有了底气。你可以放手把那段 If quantity >= 10 改写成 Select Case、抽小函数、改名字——只要每组输入算出来的 actual 还等于基线 golden，就证明你「只改了写法，没改行为」。一旦某组对不上，测试立刻红，告诉你哪组输入的行为被你不小心改掉了。这就是用样例保护旧逻辑的终极形态。", { code: "For Each c In inputs\n    Dim qty As Integer = CInt(c(0))\n    Dim actual As Decimal = CalculateTotal(qty, c(1))\n    Dim golden As Decimal = GetGoldenTotal(qty, c(1))\n    If actual <> golden Then\n        Throw New Exception($\"行为偏移：qty={qty}, price={c(1)} 基线 {golden}，现在 {actual}\")\n    End If\nNext", caption: "actual 一旦偏离 golden，立刻报警", warning: "建基线时一定要确认现状是对的再冻结。如果你把一个本来就错的输出抄成金标准，那你保护的就是个 bug——金标准只保「不变」，不保「正确」。" }),
      ],
    }),
    lesson({
      title: "整合：一个会汇报的小测试运行器",
      badge: "运行器",
      icon: "VB",
      objective: "写 AssertEqual 收集失败而不立即崩，RunAllTests 跑完所有样例后打印「通过 N / 失败 M」，全过返回 True、有失败返回 False。",
      starterCode: "Module Program\n\n    Public Function CalculateTotal(quantity As Integer, unitPrice As Decimal) As Decimal\n        Dim total As Decimal = quantity * unitPrice\n        If quantity >= 10 Then\n            total = total * 0.9D\n        End If\n        Return total\n    End Function\n\n    ' 收集所有失败信息，最后统一汇报\n    Private failures As New List(Of String)\n\n    ' TODO（软断言）：写一个名为 AssertEqual 的过程，接收 name/actual/expected 三个参数。\n    '   实际与期望不相等时，不要抛异常，而是把一条带名字、期望、实际的消息收进 failures 列表。\n\n    ' TODO（运行器）：写一个返回 Boolean 的 RunAllTests。\n    '   先清空 failures，再对「普通 / 边界下 / 边界上」三组样例分别软断言；\n    '   跑完后打印「通过 N / 失败 M」并逐条列出失败；\n    '   全部通过返回 True，否则返回 False（提示：判断失败列表是否为空）。\n\n    Sub Main()\n        If Not RunAllTests() Then Environment.Exit(1)\n    End Sub\n\nEnd Module\n",
      hints: ["上几课一失败就 Throw，第一个红的之后全停了。整批样例时更想要「软断言」：失败先记下来，全跑完再汇总，一次看清几处坏了。", "用一个 List(Of String) 收集失败消息，AssertEqual 只往里 Add，不打断流程。最后看 failures.Count 是不是 0 就知道整体过没过。", "运行器要给 CI 一个清晰信号：全过 Return True，否则 Return False，Main 里据此 Environment.Exit(1)。退出码非 0，流水线才知道挂了。"],
      tests: [
      test("vb-add-tests-5-assertequal", "定义软断言 AssertEqual", matchesAll([new RegExp("Sub\\s+AssertEqual\\s*\\("), new RegExp("failures\\.Add")]), "写 Sub AssertEqual(name, actual, expected)，不等时把消息 Add 进 failures。", 34),
      test("vb-add-tests-5-runner", "RunAllTests 汇总并打印通过/失败数", matchesAll([new RegExp("Function\\s+RunAllTests\\s*\\(\\s*\\)\\s+As\\s+Boolean"), new RegExp("AssertEqual\\s*\\("), new RegExp("failures\\.Count")]), "Function RunAllTests() As Boolean 里调用多个 AssertEqual，再打印 failures.Count。", 33),
      test("vb-add-tests-5-exit", "按结果返回布尔并设置退出码", matchesAll([new RegExp("Return\\s+failures\\.Count\\s*=\\s*0"), new RegExp("Environment\\.Exit\\s*\\(\\s*1\\s*\\)")]), "全过 Return True、有失败 Return False；Main 里用 Environment.Exit(1) 给 CI 信号。", 33),
      ],
      sections: [
      sec("从「一炸就停」到「跑完再汇报」", "前几课用 Throw 做硬断言，简单直接，但缺点是第一个失败之后全停摆——你修了一个跑一次，再修下一个。回归一大批旧样例时，更想要的是「软断言」：每条失败先记进一个清单，所有样例照样跑完，最后一次性告诉你「3 过 2 败，败在这两条」。这样一轮就能看清全部破口，效率高得多。", { code: "Private failures As New List(Of String)\n\nPrivate Sub AssertEqual(name As String, actual As Decimal, expected As Decimal)\n    If actual <> expected Then\n        failures.Add($\"[{name}] 期望 {expected}，实际 {actual}\")\n    End If\nEnd Sub", caption: "软断言：失败只记录，不打断整批运行", tip: "给每个样例起个名字（\"普通\"/\"边界上\"），失败汇报里带上名字，一眼就知道是哪条坏了，不用数第几个。" }),
      sec("运行器要给 CI 一个明确信号", "这就是整个节点的落点：一个能独立跑、还会汇报的小测试运行器。它把前几课的样例集中起来，跑完打印「通过 N / 失败 M」并列出每条失败，最后用返回值表态——全过 True、有败 False。Main 里据此调用 Environment.Exit(1)：退出码非 0，CI 流水线才会判定这次构建失败、挡住坏改动。这一步把「人工看输出」升级成「机器自动把关」，旧逻辑从此有了真正的护栏。", { code: "Function RunAllTests() As Boolean\n    failures.Clear()\n    AssertEqual(\"普通\", CalculateTotal(2, 5D), 10D)\n    AssertEqual(\"边界下\", CalculateTotal(9, 10D), 90D)\n    AssertEqual(\"边界上\", CalculateTotal(10, 10D), 90D)\n    Dim passed As Integer = 3 - failures.Count\n    Console.WriteLine($\"通过 {passed} / 失败 {failures.Count}\")\n    For Each f In failures : Console.WriteLine(\"  \" & f) : Next\n    Return failures.Count = 0\nEnd Function", caption: "跑完汇总、列出失败、按结果表态", warning: "别让测试失败了进程还返回 0。CI 只认退出码——你打印得再红，退出码是 0 它照样判通过，坏改动就溜进主干了。" }),
      ],
    }),
  ],
  "vb-capstone-modernize": [
    lesson({
      title: "先上 Option Strict 收住老代码",
      badge: "收口",
      icon: "VB",
      objective: "在模块顶部打开 Option Strict On 和 Option Explicit On，把旧报表里隐式转换、未声明变量这些坑全暴露成编译错误。",
      starterCode: "' 旧报表模块：没有任何编译期约束，Variant 满天飞\nModule LegacyReport\n\n    ' TODO: 在文件最顶部打开「严格类型」开关（Option ... 严格模式），禁止隐式窄化转换\n    ' TODO: 再打开「显式声明」开关，禁止使用未声明的变量\n\n    Sub Run()\n        ' 这段旧代码全靠隐式转换硬撑，开了 Option Strict 后必须显式转换\n        Dim total = \"0\"\n        total = total + 100\n        Console.WriteLine(\"合计：\" & total)\n    End Sub\n\nEnd Module\n",
      hints: ["Option 语句必须写在文件最前面，连 Imports 都要排在它后面，否则编译器不认。", "Option Strict On 禁止 String 和 Integer 这种「窄化/混合」隐式转换；Option Explicit On 禁止用没 Dim 过的变量。", "现代化第一步不是改逻辑，而是先让编译器替你找出所有可疑的地方。"],
      tests: [
      test("vb-capstone-modernize-1-strict", "开启 Option Strict On", matches(new RegExp("Option\\s+Strict\\s+On", "i")), "在文件最顶部写一行 Option Strict On。"),
      test("vb-capstone-modernize-1-explicit", "开启 Option Explicit On", matches(new RegExp("Option\\s+Explicit\\s+On", "i")), "再加一行 Option Explicit On 禁止未声明变量。"),
      ],
      sections: [
      sec("旧报表为什么难维护", "拿到一个跑了十年的 VB 报表模块，第一感觉往往是「不敢动」：变量随手就用、字符串和数字混着算、一个 Variant 从头传到尾。真正让它脆弱的，是编译器什么都不拦——错误全留到运行时、留到月底跑报表那天才爆。现代化的第一步不是重写逻辑，而是先把编译期约束打开，让机器替你把所有隐患标红。"),
      sec("Option Strict / Explicit 是两道闸", "Option Explicit On 要求每个变量都得先 Dim，杜绝「打错一个名字就悄悄新建了个变量」的经典 bug。Option Strict On 更狠：禁止任何会丢数据或类型不明的隐式转换——比如把字符串 \"0\" 直接当数字加。开了它们之后，旧代码可能一下冒出几十个红线，这是好事：每条红线都是一处过去靠运气没出事的地方。", { code: "Option Strict On\nOption Explicit On\n\nModule LegacyReport\n    ' …\nEnd Module", caption: "两行 Option 必须排在文件最前", warning: "别一开 Strict 就慌着改逻辑——先只加这两行，跑一遍编译，把红线当成「待办清单」。逻辑修复是后面几课的事。" }),
      ],
    }),
    lesson({
      title: "把 Variant 行换成 Record 类型",
      badge: "类型",
      icon: "VB",
      objective: "定义一个 Structure ReportRow，用 Category、Amount、OrderDate 三个带类型的字段，取代过去那种「靠下标 data(i, 2)」访问的 Variant 数组。",
      starterCode: "Option Strict On\n\nModule ReportTypes\n\n    ' 旧写法：每一行是 Variant 数组，靠记下标取值，谁也不知道第 2 列是金额\n    ' row(0) = 类别, row(1) = 金额, row(2) = 日期 …… 全靠注释和记忆\n\n    ' TODO: 定义一个 Structure ReportRow，包含三个公开字段：\n    '   Category As String\n    '   Amount As Decimal\n    '   OrderDate As Date\n\nEnd Module\n",
      hints: ["Structure 名称 … End Structure 里用 Public 字段，比 Variant 数组多了名字和类型两重自我说明。", "金额一定要用 Decimal，不要用 Double——钱算多了会出现 0.1+0.2 那种浮点误差。", "日期用 Date 类型，而不是字符串，后面排序、比较、按月分组才不用一遍遍 Parse。"],
      tests: [
      test("vb-capstone-modernize-2-structure", "定义 ReportRow 结构", matches(new RegExp("Structure\\s+ReportRow", "i")), "用 Structure ReportRow … End Structure 定义这个记录类型。", 34),
      test("vb-capstone-modernize-2-fields", "三个带类型的字段", matchesAll([new RegExp("Category\\s+As\\s+String"), new RegExp("Amount\\s+As\\s+Decimal"), new RegExp("OrderDate\\s+As\\s+Date")]), "字段应为 Category As String、Amount As Decimal、OrderDate As Date。", 33),
      test("vb-capstone-modernize-2-public", "字段公开可访问", matches(new RegExp("Public\\s+Amount\\s+As\\s+Decimal", "i")), "至少 Amount 字段要写成 Public Amount As Decimal。", 33),
      ],
      sections: [
      sec("「记下标」是维护的天敌", "旧报表里最常见的写法是 data(i, 2) 取金额、data(i, 1) 取类别。问题在于：第 2 列到底是金额还是日期，只有写代码那个人当时知道。半年后改需求、插了一列，所有下标全错位，而编译器一声不吭。把一行数据升级成有名字、有类型的记录，等于把「靠记忆」变成「靠声明」。"),
      sec("Structure：轻量的记录类型", "对「一行报表数据」这种纯数据载体，Structure 正合适：字段有名字 row.Amount，有类型，写错就编译报错。金额坚持用 Decimal——它按十进制精确存储，不会像 Double 那样把 0.1 存成近似值，月底对账时差几分钱往往就是这里出的。日期用 Date 而非字符串，后面才能直接比较和按月分组。", { code: "Structure ReportRow\n    Public Category As String\n    Public Amount As Decimal\n    Public OrderDate As Date\nEnd Structure", caption: "一行报表 = 三个有名有姓的字段", tip: "字段名用业务词（Category/Amount），别再叫 col1/col2——名字本身就是给下一个维护者的文档。" }),
      ],
    }),
    lesson({
      title: "把解析逻辑抽成纯函数",
      badge: "拆分",
      icon: "VB",
      objective: "写一个 Function ParseRow(fields As String()) As ReportRow，只做「字符串数组 → ReportRow」的转换，不读文件、不碰界面，便于单独测试。",
      starterCode: "Option Strict On\n\nModule ReportParser\n\n    ' 旧报表把「读文件 + 解析 + 算总数 + 弹窗」全塞在一个 Sub 里，根本没法单测。\n    ' 这一课只抽出最里层、最纯的一步：把一行的字符串字段转成 ReportRow。\n\n    ' fields(0)=类别  fields(1)=金额  fields(2)=日期(yyyy-MM-dd)\n    Function ParseRow(fields As String()) As ReportRow\n        Dim row As ReportRow\n        ' TODO: row.Category = fields(0)（去掉首尾空格用 Trim）\n        ' TODO: row.Amount  用 Decimal.Parse(fields(1)) 转成 Decimal\n        ' TODO: row.OrderDate 用 Date.Parse(fields(2)) 转成 Date\n        Return row\n    End Function\n\nEnd Module\n",
      hints: ["纯函数 = 输入定则输出定，不读全局、不写文件、不弹窗。这样的函数最好测。", "Decimal.Parse 把 \"1234.50\" 这种字符串转成精确的 Decimal；Date.Parse 把 \"2026-06-16\" 转成 Date。", "用 fields(0).Trim() 去掉导出文件里常见的多余空格，避免类别分组时「办公」和「办公 」被当成两类。"],
      tests: [
      test("vb-capstone-modernize-3-signature", "函数签名返回 ReportRow", matches(new RegExp("Function\\s+ParseRow\\s*\\(\\s*fields\\s+As\\s+String\\s*\\(\\s*\\)\\s*\\)\\s+As\\s+ReportRow", "i")), "签名应为 Function ParseRow(fields As String()) As ReportRow。", 34),
      test("vb-capstone-modernize-3-decimal", "用 Decimal.Parse 转金额", matches(new RegExp("Decimal\\.Parse\\s*\\(\\s*fields\\s*\\(\\s*1\\s*\\)\\s*\\)", "i")), "用 row.Amount = Decimal.Parse(fields(1)) 转金额。", 33),
      test("vb-capstone-modernize-3-date", "用 Date.Parse 转日期并 Trim 类别", matchesAll([new RegExp("Date\\.Parse\\s*\\(\\s*fields\\s*\\(\\s*2\\s*\\)\\s*\\)"), new RegExp("fields\\s*\\(\\s*0\\s*\\)\\s*\\.Trim\\s*\\(\\s*\\)")]), "用 Date.Parse(fields(2)) 转日期，并用 fields(0).Trim() 处理类别。", 33),
      ],
      sections: [
      sec("一个巨型 Sub 没法测", "旧报表常见的样子是一个几百行的 Sub：打开文件、逐行切分、转换、累加、格式化、弹 MsgBox，全混在一起。这种代码无法单测——你没法只测「解析」而不真的去读硬盘、不真的弹窗。现代化的核心动作之一，就是把里面那段纯计算单独抽出来，做成输入输出明确的函数。"),
      sec("纯函数：可测性的基石", "ParseRow 只做一件事：给它一组字符串字段，还你一个 ReportRow。它不读文件、不写界面，所以可以在测试里直接喂任意字段、断言结果，毫秒级、可重复。Trim 掉类别两端空格是个真实的坑：导出 CSV 经常带尾随空格，不清理的话「差旅」和「差旅 」会被算成两个类别，合计就对不上。", { code: "Function ParseRow(fields As String()) As ReportRow\n    Dim row As ReportRow\n    row.Category = fields(0).Trim()\n    row.Amount = Decimal.Parse(fields(1))\n    row.OrderDate = Date.Parse(fields(2))\n    Return row\nEnd Function", caption: "纯转换：字符串进，类型化记录出", warning: "真实生产里 Parse 遇到坏数据会抛异常，下一课专门处理。本课先把「纯转换」这一层独立出来——一次只前进一步。" }),
      ],
    }),
    lesson({
      title: "用 TryParse 替代会崩的解析",
      badge: "容错",
      icon: "VB",
      objective: "把会抛异常的 Decimal.Parse 改成 Decimal.TryParse，写成 Function TryParseRow(fields As String(), ByRef row As ReportRow) As Boolean，遇到坏数据返回 False 而不是崩溃。",
      starterCode: "Option Strict On\n\nModule SafeParser\n\n    ' 旧报表一遇到一行脏数据（金额是空的、日期格式怪）就整个崩，前面几千行白算。\n    ' 改成「能转就转、转不了就报告失败」，让流程能跳过坏行继续跑。\n\n    Function TryParseRow(fields As String(), ByRef row As ReportRow) As Boolean\n        Dim amount As Decimal\n        Dim orderDate As Date\n\n        ' TODO: 用 Decimal.TryParse(fields(1), amount) 试转金额，失败就 Return False\n        ' TODO: 用 Date.TryParse(fields(2), orderDate) 试转日期，失败也 Return False\n\n        row.Category = fields(0).Trim()\n        row.Amount = amount\n        row.OrderDate = orderDate\n        Return True\n    End Function\n\nEnd Module\n",
      hints: ["TryParse 返回 Boolean，转换结果通过第二个 ByRef 参数带出来；它不会抛异常。", "标准写法：If Not Decimal.TryParse(fields(1), amount) Then Return False。", "整套流程因此从「一行坏数据全盘崩溃」变成「跳过坏行、好行照常」，这对老报表是质的提升。"],
      tests: [
      test("vb-capstone-modernize-4-signature", "返回 Boolean、用 ByRef 带出结果", matches(new RegExp("Function\\s+TryParseRow\\s*\\(\\s*fields\\s+As\\s+String\\s*\\(\\s*\\)\\s*,\\s*ByRef\\s+row\\s+As\\s+ReportRow\\s*\\)\\s+As\\s+Boolean", "i")), "签名应为 Function TryParseRow(fields As String(), ByRef row As ReportRow) As Boolean。", 34),
      test("vb-capstone-modernize-4-tryparse", "用 Decimal.TryParse 与 Date.TryParse", matchesAll([new RegExp("Decimal\\.TryParse\\s*\\(\\s*fields\\s*\\(\\s*1\\s*\\)\\s*,\\s*amount\\s*\\)"), new RegExp("Date\\.TryParse\\s*\\(\\s*fields\\s*\\(\\s*2\\s*\\)\\s*,\\s*orderDate\\s*\\)")]), "用 Decimal.TryParse(fields(1), amount) 和 Date.TryParse(fields(2), orderDate)。", 33),
      test("vb-capstone-modernize-4-returnfalse", "转换失败时返回 False", matches(new RegExp("If\\s+Not\\s+Decimal\\.TryParse[^\\n]*Then\\s+Return\\s+False", "i")), "用 If Not Decimal.TryParse(...) Then Return False 在失败时退出。", 33),
      ],
      sections: [
      sec("脏数据是常态，不是意外", "导出文件里出现空金额、半截日期、多一个逗号，是再正常不过的事。旧报表用 Decimal.Parse 直接转，一遇到坏行就抛异常、整个 Sub 中断，前面几千行的累加全部作废。对于「每月必须出」的报表，这种「一坏全崩」是不可接受的。我们要的是：好行照算，坏行记录下来跳过。"),
      sec("TryParse：不抛异常的转换", "TryParse 是 .NET 的标准容错转换模式：返回值是「成功与否」的布尔，转好的结果通过 ByRef 参数带回来。它从不抛异常，所以你能用一个普通的 If 判断成败，而不用 Try/Catch 套住每一行。把 ParseRow 升级成 TryParseRow，整个流程就从「崩溃驱动」变成「布尔驱动」——这正是现代化里最实用的一招。", { code: "If Not Decimal.TryParse(fields(1), amount) Then Return False\nIf Not Date.TryParse(fields(2), orderDate) Then Return False\nrow.Amount = amount", caption: "转不动就早退，绝不抛异常", tip: "调用方拿到 False 时，可以把这一行原文记进日志，月底再回头看哪些数据需要清洗——既不丢报表，也不丢线索。" }),
      ],
    }),
    lesson({
      title: "用 Enum 表达处理结果",
      badge: "结果码",
      icon: "VB",
      objective: "定义 Enum ImportResult（Ok / EmptyFile / BadFormat / NoData），让导入函数返回一个含义清晰的结果码，取代过去用 0/1/-1 或布尔表达成败的写法。",
      starterCode: "Option Strict On\n\nModule ImportContracts\n\n    ' 旧报表用魔法数字表达结果：Return 0 成功、Return -1 文件空、Return -2 格式错……\n    ' 调用方得记住每个数字的含义，错一个就排查半天。改用具名枚举，让结果自己说话。\n\n    ' TODO: 定义一个名为 ImportResult 的枚举类型，依次包含四个成员：\n    '   成功、文件为空、格式错误、无有效数据（分别用英文标识符命名）\n\n    Function DescribeResult(result As ImportResult) As String\n        ' TODO: 用分支语句对枚举的每个成员返回一句中文说明，末尾兜底返回\"未知结果\"\n        Return \"未知结果\"\n    End Function\n\nEnd Module\n",
      hints: ["Enum 名 … End Enum 里每行一个成员名，引用时写 ImportResult.Ok，自带可读性与编译期检查。", "魔法数字（return -2）最大的问题是「意义只在写代码人脑子里」；Enum 把意义写进了类型。", "Select Case 配 Enum 很搭：漏掉某个成员时，配上 Case Else 兜底，可读性也好过一串 If。"],
      tests: [
      test("vb-capstone-modernize-5-enum", "定义 ImportResult 枚举", matches(new RegExp("Enum\\s+ImportResult", "i")), "用 Enum ImportResult … End Enum 定义结果码。", 34),
      test("vb-capstone-modernize-5-members", "四个具名成员齐全", matchesAll([new RegExp("\\bOk\\b"), new RegExp("\\bEmptyFile\\b"), new RegExp("\\bBadFormat\\b"), new RegExp("\\bNoData\\b")]), "成员应包含 Ok、EmptyFile、BadFormat、NoData。", 33),
      test("vb-capstone-modernize-5-select", "用 Select Case 解释结果码", matchesAll([new RegExp("Select\\s+Case\\s+result"), new RegExp("Case\\s+ImportResult\\.Ok")]), "在 DescribeResult 里用 Select Case 对各成员返回说明。", 33),
      ],
      sections: [
      sec("魔法数字是债，不是简洁", "老报表里到处是 Return 0、Return -1、Return -2 这样的结果约定，注释还经常和代码对不上。调用方必须背下「-2 是格式错」，背错一个就排查半天。这类「意义只活在文档和记忆里」的代码，是维护成本的大头。把结果升级成 Enum，意义就从注释挪进了类型本身。"),
      sec("Enum：给状态起名字", "ImportResult.BadFormat 一眼就懂，IDE 还能自动补全、防止你写出不存在的成员。配合 Select Case，处理每种结果时也更清楚——而且当你日后给 Enum 加一个新成员，那些没覆盖到它的 Select Case 会更容易被发现。把「成功/失败/为什么失败」用类型表达出来，是写出可迁移代码的关键一步。", { code: "Enum ImportResult\n    Ok\n    EmptyFile\n    BadFormat\n    NoData\nEnd Enum\n\nSelect Case result\n    Case ImportResult.Ok : Return \"导入成功\"\n    Case ImportResult.EmptyFile : Return \"文件为空\"\n    Case Else : Return \"未知结果\"\nEnd Select", caption: "具名结果码 + Select Case 分支", tip: "不要给 Enum 成员硬塞数字值（Ok = 0），除非要和外部系统对接固定编码——多数情况下让编译器自动编号最省心。" }),
      ],
    }),
    lesson({
      title: "用接口隔离数据来源",
      badge: "接口",
      icon: "VB",
      objective: "定义 Interface IRowSource，含 Function ReadLines() As IEnumerable(Of String)，让报表逻辑依赖这个接口而不是写死 File.ReadAllLines，为测试和迁移留出替换点。",
      starterCode: "Option Strict On\nImports System.Collections.Generic\n\nModule SourceAbstraction\n\n    ' 旧报表里数据来源写死成 File.ReadAllLines(\"C:\\report\\data.csv\")，\n    ' 既没法在测试里喂假数据，将来要从数据库或网络取数也得改一大片。\n\n    ' TODO: 定义 Interface IRowSource，声明一个方法：\n    '   Function ReadLines() As IEnumerable(Of String)\n\n    ' TODO: 写一个实现 CsvRowSource，Implements IRowSource，\n    '   构造时存下文件路径，ReadLines 返回 IO.File.ReadAllLines(path)\n\nEnd Module\n",
      hints: ["Interface 名 … End Interface 里只写方法签名，不写实现；实现类用 Implements 接口名。", "方法实现处要写 Function ReadLines() As IEnumerable(Of String) Implements IRowSource.ReadLines。", "报表逻辑只认 IRowSource，真正读文件、读数据库还是读内存数组，由传进来的实现决定。"],
      tests: [
      test("vb-capstone-modernize-6-interface", "定义 IRowSource 接口", matchesAll([new RegExp("Interface\\s+IRowSource"), new RegExp("Function\\s+ReadLines\\s*\\(\\s*\\)\\s+As\\s+IEnumerable\\s*\\(\\s*Of\\s+String\\s*\\)")]), "用 Interface IRowSource … End Interface 声明数据来源接口。"),
      test("vb-capstone-modernize-6-impl", "CsvRowSource 实现该接口", matchesAll([new RegExp("Class\\s+CsvRowSource"), new RegExp("Implements\\s+IRowSource"), new RegExp("Implements\\s+IRowSource\\.ReadLines")]), "类要写 Implements IRowSource，方法实现处写 Implements IRowSource.ReadLines。"),
      ],
      sections: [
      sec("写死的数据源是迁移的拦路虎", "旧报表几乎都把数据来源钉死在代码里：File.ReadAllLines(\"C:\\report\\data.csv\")。这带来两个痛：一是测试时没法喂一批可控的假数据，二是哪天要改成从数据库或 API 取数，得把读取逻辑在十几处一一改掉。解法是引入一个接口，把「从哪读」和「怎么算」彻底分开。"),
      sec("面向接口编程：留一个替换点", "IRowSource 只声明一件事：ReadLines 给我一串字符串行。报表逻辑只依赖这个接口，不在乎背后是 CSV、数据库还是内存数组。生产环境传 CsvRowSource，测试里传一个返回固定几行的假实现，迁移到数据库时再写个 SqlRowSource——上层逻辑一行都不用改。这就是「依赖抽象、不依赖具体」带来的可迁移性。", { code: "Interface IRowSource\n    Function ReadLines() As IEnumerable(Of String)\nEnd Interface\n\nClass CsvRowSource\n    Implements IRowSource\n    Private ReadOnly _path As String\n    Public Sub New(path As String)\n        _path = path\n    End Sub\n    Public Function ReadLines() As IEnumerable(Of String) _\n        Implements IRowSource.ReadLines\n        Return IO.File.ReadAllLines(_path)\n    End Function\nEnd Class", caption: "接口 + 一个 CSV 实现", tip: "返回 IEnumerable(Of String) 而不是 String() ，调用方就只能顺序遍历、不能就地改数组，意图更明确，也方便将来换成流式读取。" }),
      ],
    }),
    lesson({
      title: "为汇总写第一个单元测试",
      badge: "测试",
      icon: "VB",
      objective: "把按类别求和的逻辑做成纯函数 SumByCategory(rows As List(Of ReportRow)) As Dictionary(Of String, Decimal)，再写一个用 Assert 验证两行同类别金额相加的测试方法。",
      starterCode: "Option Strict On\nImports System.Collections.Generic\n\nModule Aggregation\n\n    ' 旧报表的求和混在巨型 Sub 里，从没被单独验证过。\n    ' 这一课把它抽成纯函数，再写一个测试钉死它的行为，以后重构就有了安全网。\n\n    Function SumByCategory(rows As List(Of ReportRow)) As Dictionary(Of String, Decimal)\n        Dim totals As New Dictionary(Of String, Decimal)\n        For Each r In rows\n            ' TODO: 如果 totals 还没有 r.Category 这个键，先 totals(r.Category) = 0\n            ' TODO: 把 r.Amount 累加到 totals(r.Category) 上\n        Next\n        Return totals\n    End Function\n\n    ' 一个极简测试：两行「差旅」金额应合并为一个键、金额相加\n    Sub TestSumByCategory()\n        Dim rows As New List(Of ReportRow)\n        rows.Add(New ReportRow With {.Category = \"差旅\", .Amount = 100D})\n        rows.Add(New ReportRow With {.Category = \"差旅\", .Amount = 50D})\n\n        Dim result = SumByCategory(rows)\n\n        ' TODO: 用 Debug.Assert 验证 result(\"差旅\") = 150D\n        ' TODO: 再用 Debug.Assert 验证 result.Count = 1\n    End Sub\n\nEnd Module\n",
      hints: ["字典累加的标准两步：没键先建 0 桶（ContainsKey 判断），再 totals(key) = totals(key) + 值。", "Debug.Assert(条件) 在条件为 False 时会中断并提示——这是 VB 里最轻量的断言。", "金额字面量加 D 后缀（150D）表示 Decimal，和字段类型对齐，避免和 Double 比较出误差。"],
      tests: [
      test("vb-capstone-modernize-7-sumsig", "SumByCategory 返回字典", matches(new RegExp("Function\\s+SumByCategory\\s*\\([^)]*\\)\\s+As\\s+Dictionary\\s*\\(\\s*Of\\s+String\\s*,\\s*Decimal\\s*\\)", "i")), "签名应为 SumByCategory(rows As List(Of ReportRow)) As Dictionary(Of String, Decimal)。", 34),
      test("vb-capstone-modernize-7-accumulate", "按类别键累加金额", matches(new RegExp("totals\\s*\\(\\s*r\\.Category\\s*\\)\\s*=\\s*totals\\s*\\(\\s*r\\.Category\\s*\\)\\s*\\+\\s*r\\.Amount", "i")), "用 totals(r.Category) = totals(r.Category) + r.Amount 累加。", 33),
      test("vb-capstone-modernize-7-assert", "用 Debug.Assert 验证求和结果", matchesAll([new RegExp("Debug\\.Assert\\s*\\(\\s*result\\s*\\(\\s*\"差旅\"\\s*\\)\\s*=\\s*150D"), new RegExp("Debug\\.Assert\\s*\\(\\s*result\\.Count\\s*=\\s*1")]), "用 Debug.Assert(result(\"差旅\") = 150D) 和 Debug.Assert(result.Count = 1)。", 33),
      ],
      sections: [
      sec("没测试，就不敢重构", "现代化的真正目的不是「代码好看」，而是「敢改」。可没有测试，每次改动都像拆弹——你不知道哪行依赖了哪行。前几课已经把求和逻辑抽成了不读文件、不弹窗的纯函数 SumByCategory，正好可以钉一个测试上去：给定两行同类别，断言它们合并成一个键、金额相加。"),
      sec("最小可行的断言", "测试不必一上来就搬正式框架。Debug.Assert 就够起步：条件为真悄悄通过，为假就中断报警。准备两行「差旅」、各 100 和 50，跑 SumByCategory，断言 result(\"差旅\") 等于 150D、且字典只有 1 个键。这一个测试看似简单，却同时锁住了「同类别会合并」和「金额会累加」两条核心契约——以后再怎么重构内部，只要它还绿，行为就没变。", { code: "Dim result = SumByCategory(rows)\nDebug.Assert(result(\"差旅\") = 150D)\nDebug.Assert(result.Count = 1)", caption: "断言合并后的键数与金额", warning: "用 Decimal 做相等比较是安全的；但若哪天把 Amount 误改成 Double，150D 和 150.0 的比较可能因浮点误差失败——这正是第 2 课坚持 Decimal 的原因。" }),
      ],
    }),
    lesson({
      title: "组装成可注入依赖的报表服务",
      badge: "整合",
      icon: "VB",
      objective: "写一个 Class ReportService，构造时注入 IRowSource，提供 Function Build() As ImportResult：从数据源读行 → TryParse → SumByCategory，空数据返回 NoData，成功返回 Ok。",
      starterCode: "Option Strict On\nImports System.Collections.Generic\n\n' 终点：把前 7 课的零件装成一个可读、可测、可迁移的服务。\n' 数据来源通过构造函数注入（IRowSource），逻辑不再写死从哪取数。\nClass ReportService\n\n    Private ReadOnly _source As IRowSource\n    Public Totals As Dictionary(Of String, Decimal)\n\n    ' TODO: 构造函数 New(source As IRowSource)，把 source 存进 _source\n\n    Function Build() As ImportResult\n        Dim rows As New List(Of ReportRow)\n\n        For Each line In _source.ReadLines()\n            Dim fields = line.Split(\",\"c)\n            Dim row As ReportRow\n            If TryParseRow(fields, row) Then\n                rows.Add(row)\n            End If\n        Next\n\n        ' TODO: 如果 rows.Count = 0，返回 ImportResult.NoData\n\n        Totals = SumByCategory(rows)\n        ' TODO: 一切正常，返回 ImportResult.Ok\n    End Function\n\nEnd Class\n",
      hints: ["构造函数注入：Public Sub New(source As IRowSource) 把外部传入的数据源存成 _source 字段。", "服务自己不决定数据从哪来——测试传假源、生产传 CsvRowSource，同一个 Build 逻辑通吃。", "用第 5 课的 Enum 返回结果：空数据 Return ImportResult.NoData，成功 Return ImportResult.Ok。"],
      tests: [
      test("vb-capstone-modernize-8-ctor", "构造函数注入 IRowSource", matchesAll([new RegExp("Sub\\s+New\\s*\\(\\s*source\\s+As\\s+IRowSource\\s*\\)"), new RegExp("_source\\s*=\\s*source")]), "写 Public Sub New(source As IRowSource)，并把 source 存进 _source。", 34),
      test("vb-capstone-modernize-8-nodata", "空数据返回 NoData", matches(new RegExp("rows\\.Count\\s*=\\s*0\\s*Then\\s*Return\\s+ImportResult\\.NoData", "i")), "用 If rows.Count = 0 Then Return ImportResult.NoData。", 33),
      test("vb-capstone-modernize-8-ok", "成功路径返回 Ok", matches(new RegExp("Return\\s+ImportResult\\.Ok", "i")), "汇总完后 Return ImportResult.Ok。", 33),
      ],
      sections: [
      sec("把零件装成一台机器", "走到这里，所有零件都已现代化：Option Strict 收住了类型、ReportRow 取代了 Variant、TryParse 处理了脏数据、ImportResult 表达了结果、IRowSource 隔离了来源、SumByCategory 有了测试护体。最后一步是把它们装进一个 ReportService：读行 → 逐行 TryParse 收进列表 → 汇总 → 用结果码报告成败。每个零件都能单独换、单独测，整体却是一条清晰的流水线。"),
      sec("构造注入 = 可测 + 可迁移", "ReportService 不自己去 new 一个文件读取器，而是在构造函数里「接收」一个 IRowSource。这一个小决定带来两个大收益：测试时塞一个返回固定几行的假源，毫秒级跑完且不碰硬盘；将来要从数据库取数，只需在外面换成 SqlRowSource，Build 里一行都不动。这就是从「旧报表」到「现代模块」的本质跃迁——依赖被显式注入，而不是藏在函数内部。", { code: "Public Sub New(source As IRowSource)\n    _source = source\nEnd Sub\n\nFunction Build() As ImportResult\n    ' … 读行、TryParse、收集 rows …\n    If rows.Count = 0 Then Return ImportResult.NoData\n    Totals = SumByCategory(rows)\n    Return ImportResult.Ok\nEnd Function", caption: "注入来源 + 结果码收尾的服务", tip: "调用方拿到 ImportResult 后，用第 5 课的 DescribeResult 翻成人话提示用户；UI 与界面只在最外层一处出现，核心逻辑全程不弹窗——这正是可测的前提。" }),
      ],
    }),
  ],
}

export type { Level }
